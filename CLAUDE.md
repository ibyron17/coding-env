# 전역 개발 지침 (~/.claude/CLAUDE.md)

모든 프로젝트에 공통 적용되는 워크플로우와 코드 품질 기준.
프로젝트별 컨텍스트(개요, 아키텍처, 병목, 빌드 명령)는
각 프로젝트 루트의 CLAUDE.md에 정의하고, 이 문서와 함께 적용한다.
두 문서가 충돌하면 프로젝트 CLAUDE.md를 우선한다.

---

## 응답 언어

모든 응답은 **한국어**로 작성한다. 코드·명령어·고유명사·기술 용어는 원어 그대로 사용하되, 설명과 서술은 모두 한국어로 한다.

---

## 개발 워크플로우 (역할 분리)

작업 규모에 따라 경로를 나눈다. 어느 경로든 **검수는 생략하지 않는다.**

### 작업 분류 (경로 선택)

**전체 경로 (설계 → 구현 → 검수)** — 다음 중 하나라도 해당하면:
- 새 모듈/레이어 추가 또는 디렉토리 구조 변경
- 데이터 모델이나 공개 인터페이스(함수 시그니처, API 스펙)의 변경
- 3개 이상 파일에 걸친 로직 변경
- 새 외부 의존성 도입
- 인증/결제/권한/데이터 삭제 등 민감 영역, 또는 성능 병목 관련 변경

**축약 경로 (구현 → 검수)** — 위 기준에 해당하지 않는 작업:
- 기존 동작 범위 내의 버그 수정
- 단일 모듈 안에서 끝나는 소규모 기능 추가/수정
- 테스트 추가, 국소적 리팩토링

**워크플로우 생략** — 오타, 주석, 설정값 1줄 변경 등 로직이 없는 수정.

분류 규칙:
- 분류가 애매하면 **전체 경로를 기본값**으로 한다.
- 작업 시작 시 어느 경로로 진행하는지 한 줄로 보고한다.
- 축약 경로로 시작했더라도 구현 중 전체 경로 기준에 해당함이 드러나면
  구현을 중단하고 설계 단계로 돌아간다 (에스컬레이션).

### 1단계 — 설계 (Design, 전체 경로만)
- 전체 경로로 분류된 작업은 코드를 작성하기 전에 `design-architect`
  서브에이전트에게 설계를 위임한다. 설계자는 `/prp-plan` 스킬(설치된 경우)을 사용해
  설계 문서(PRP)를 생성한다.
- 산출물: `docs/prps/{기능명}.md`
  (요구사항, 영향 범위, 파일 구조, 데이터 모델, 인터페이스, 테스트 계획 포함)
- 설계 문서에 대해 **사용자 승인을 받기 전에는 구현을 시작하지 않는다.**

### 2단계 — 구현 (Implement)
- **전체 경로**: 승인된 PRP 문서를 입력으로 `implementer` 서브에이전트가
  `/prp-implement` 스킬(설치된 경우)을 사용해 구현한다.
- **축약 경로**: 설계 문서 없이 `implementer`가 바로 구현하되,
  코드 작성 전에 접근 방식과 수정 범위(파일 목록)를 2~3줄로 보고한다.
- 구현 중 설계와 다른 결정이 필요해지면 임의로 변경하지 말고,
  PRP 문서 수정을 먼저 제안하고 승인을 받는다.
- 구현 완료 기준: 코드 + 단위 테스트 + lint/format/type check 전부 통과

### 3단계 — 검수 (Review)
- 구현이 끝나면 `code-reviewer` 서브에이전트에게 검수를 위임한다.
  검수자는 `/code-review` 스킬을 사용하고, 전역·프로젝트 CLAUDE.md의
  코드 품질 기준 준수 여부를 항목별로 점검한다.
- 검수자는 코드를 **직접 수정하지 않고** 지적 사항만 보고한다.
- Critical/Major 지적 사항은 `implementer`가 수정하고,
  재검수를 통과해야 완료로 간주한다.
- **재검수를 요청할 때는 1회차 지적 항목(심각도·위치·문제)을 함께 넘긴다.**
  검수자는 서브에이전트라 라운드 간 컨텍스트가 없어 자기가 뭘 지적했는지 모른다 —
  넘기지 않으면 재검수가 전체 검수의 되풀이가 된다. 기준 커밋(1회차 시점)도 함께
  알려주면 검수자가 diff 를 그 이후로 좁힐 수 있다.
- **검수를 통과하지 못한 코드는 커밋하지 않는다.**

### 공통 규칙
- 각 단계의 산출물(설계 문서, 구현 diff 요약, 검수 보고서)은
  사용자에게 보고한 뒤 다음 단계로 넘어간다.
- `/prp-plan`, `/prp-implement` 스킬이 설치되지 않은 프로젝트에서는
  동일한 산출물 기준을 지키며 일반 지시로 설계/구현을 수행한다.
- **전체 경로 작업은 진행 상황을 `/dashboard` 로 기록한다**: 착수 보고 시 `init`,
  단계 전환마다 `step`·`log` 를 호출한다. 갱신 주체는 **메인 세션뿐** —
  서브에이전트는 대시보드 파일에 접근하지 않는다. 축약 경로에는 적용하지 않는다.
  **끄는 방법**: 이 프로젝트 전체에서 끄려면 이 항목 자체를 삭제하거나 주석 처리한다
  (팀 전체에 적용, 커밋 대상). 나만 끄고 싶다면(팀원에게는 강제하지 않음) 커밋되지 않는
  `.claude/settings.local.json` 에 `"dashboard_enabled": false` 를 추가한다(`/dashboard off` 가 대신 해준다) — 메인 세션은
  `/dashboard` 를 호출하기 전에 이 파일이 있으면 그 값을 확인하고, `false` 면 이번 세션의
  전체 경로 작업에서 `/dashboard` 호출을 생략한다(파일이 없거나 필드가 없으면 기본값 사용).

### 모델 운용 원칙
- **설계 (design-architect): opus** — 판단 오류의 파급이 가장 큰 단계
- **구현 (implementer): sonnet** — 토큰 소비가 가장 큰 단계, 품질/비용 균형
- **검수 (code-reviewer): sonnet 기본** — 인증/결제/권한/데이터 삭제 등
  민감한 변경은 검수 요청 시 opus 승격을 지시
- **탐색 (Explore): haiku** — 수집·요약 전용
- **haiku는 소극적으로 사용한다**: 판단이 필요한 작업(설계, 구현, 검수 판정)에는
  사용하지 않는다. 각 에이전트는 직접 읽기를 기본값으로 하고,
  넓은 범위 탐색이 필요할 때만 Explore에 위임하며,
  판단의 근거가 되는 파일은 요약에 의존하지 않고 직접 읽는다.

---

## 코드 품질 가이드라인

### 구조와 모듈화
- 관심사를 레이어로 분리하고, 레이어 간에는 명시적인 데이터 모델
  (dataclass/TypedDict/interface 등 언어에 맞는 타입)로만 통신한다.
  구체적인 레이어 구성은 프로젝트 CLAUDE.md의 아키텍처 섹션을 따른다.
- 외부 세계(네트워크, DB, 파일시스템)에 닿는 코드와 순수 로직을 분리한다.
  순수 로직은 입력→출력만 있는 함수로 작성해 단위 테스트가 가능해야 한다.
- 함수는 한 가지 책임만 갖고 50줄을 넘지 않는다. 넘으면 분리한다.
- 순환 의존이 생기면 구현을 멈추고 구조를 먼저 재검토한다.

### 가독성
- 이름은 축약하지 않는다 (`grp` ✕ → `feature_group` ○).
  이름만 읽고 역할을 알 수 있어야 한다.
- 중첩은 최대 3단계. 그 이상이면 early return이나 함수 추출로 평탄화한다.
- 주석은 "무엇을"이 아니라 "왜"를 설명할 때만 쓴다.
  코드로 표현 가능한 것은 코드로 표현한다.
- 매직 넘버/문자열은 이름 있는 상수로 선언한다.
- 모든 공개 함수에 타입 명시와 한 줄 docstring(또는 언어 관례의 문서 주석)을 붙인다.

### 설계 원칙
- 기본은 가장 단순한 구조다. 디자인 패턴은 구체적인 문제가 있을 때만 도입하고,
  도입 시 어떤 문제 때문인지 그 이유를 모듈 상단 주석으로 남긴다.
- 지금 필요 없는 확장성을 미리 만들지 않는다(YAGNI). 추측성 추상화 금지.
- 상속보다 조합을 우선한다. Singleton, 전역 가변 상태는 사용하지 않는다.
- 새 의존성 추가는 표준 라이브러리로 불가능할 때만, 이유와 함께 제안한다.

### 성능
- 프로젝트의 실제 병목을 기준으로 최적화한다.
  병목이 무엇인지는 프로젝트 CLAUDE.md의 성능 섹션에 정의한다.
  (예: 외부 API 호출, DB 쿼리, 대용량 파일 I/O, 렌더링)
- 정의된 병목이 없으면: 알고리즘 복잡도가 명백히 나쁜 경우
  (불필요한 중첩 루프, N+1 쿼리 패턴)만 바로잡고 그 외 최적화는 하지 않는다.
- 측정 없는 마이크로 최적화는 하지 않는다.
  성능 개선은 병목을 측정한 근거가 있을 때만 수행한다.
- 외부 API를 호출하는 코드는 호출 횟수 최소화, 실패 시 재시도,
  rate limit 대응을 기본으로 갖춘다.

### 강제 수단
- 프로젝트 언어에 맞는 linter/formatter/type checker를 설정하고
  모든 커밋 전에 통과시킨다. 실제 사용하는 도구와 명령은
  프로젝트 CLAUDE.md의 빌드/검증 섹션을 따른다.
- 경고를 무시하는 주석(`# noqa`, `eslint-disable` 등)은 사유 없이 추가하지 않는다.
- 핵심 순수 로직은 단위 테스트를 함께 작성한다.
- 기능 구현 전에 파일 구조와 데이터 모델을 먼저 제안하고 승인을 받는다.

---

## 프로젝트 CLAUDE.md에 정의해야 할 항목 (체크리스트)

각 프로젝트 루트의 CLAUDE.md에는 최소한 다음을 정의한다:
1. 프로젝트 개요와 목적 (도구가 하지 말아야 할 것 포함)
2. 아키텍처: 레이어/모듈 구성과 각 책임
3. 성능: 이 프로젝트의 실제 병목과 최적화 우선순위
4. 빌드/검증 명령: 실행, 테스트, lint/format, type check의 실제 명령
5. 프로젝트 고유 컨벤션 (커밋 형식, 브랜치 규칙 등이 있다면)
