# PRP — 프로젝트 헤더 도입 + 진행 단계에서 커밋 배제

> 상태: **구현·검수 완료 (2026-08-14)** — §11 의 결정 4건 전부 권장안(1-A · 2-A · 3-A · 4번 일괄)으로 확정.
> 구현 완료 후 code-reviewer 1차 검수에서 HIGH 1건(스크린샷이 구 템플릿 반영)·MEDIUM 1건("우상단"
> 낡은 표현)을 지적, 오케스트레이터가 직접 수정 후 재검수 PASS.
> 브랜치: `feature/dashboard-hub-visual-parity` (직전 PRP `dashboard-hub-visual-parity.md` 의
> 미커밋 작업 위에 얹는다)
> 선행 PRP: [`dashboard-hub-visual-parity.md`](dashboard-hub-visual-parity.md) — 색 토큰·다크 모드·
> `#dz-top-actions` 클러스터를 도입한 직전 작업. 이 문서는 그 결과물을 전제로 한다.

## 사용자 승인 — 확정 (2026-08-14)

AskUserQuestion 4문항 전부 권장안 채택:

1. **프로젝트 명 값 출처 = 1-A** — `basename "$PWD"` 자동 유도. 호출 규약 불변.
2. **`#dz-top-actions` 배치 = 2-A** — `.wrap` 바깥 `#dz-page-head` 신설. PiP JS 무변경.
3. **진행 단계 가드 = 3-A** — 명문 규칙 + `init` 3단계 자동 제외 + 회귀 검사 2개.
4. **부수 결정 3건 = 전부 권장안** — 허브 모달에서 헤더 숨김 / `position:sticky` 미적용(일반 흐름) /
   `docs/images/dashboard-sample.png` 재촬영.

§0 의 P0 결함(PiP 복귀 `insertBefore(wrap, pipButton)` → `NotFoundError`) 수정은 승인 대상이
아니라 필수 사항으로 구현에 포함한다.

> **파일명 근거**: `dashboard-header-and-progress.md` 대신 이 이름을 썼다. 이 저장소에는
> `dashboard-*.md` PRP 가 이미 8개 있고, "header" 는 HTML `<head>`(직전 PRP 가 만진 FOUC 스크립트)와
> 혼동되며 "progress" 는 진행률 **기능**처럼 읽힌다. 실제 내용은 **프로젝트 헤더 행 신설**과
> **진행 단계 가드**다.

---

## 0. 착수 전 필수 — 미커밋 작업에 P0 결함이 있다

**이 PRP 의 어떤 결정을 고르든 먼저 고쳐야 하는 버그를 설계 조사 중 발견했다.**

직전 작업(미커밋)이 `#dz-pip-btn` 을 `#dz-top-actions` 로 **감쌌는데**, PiP 복귀 코드는 그대로다:

```js
// commands/dashboard.md:1414 (현재 워킹트리)
document.body.insertBefore(wrap, pipButton);
```

`insertBefore(newNode, referenceNode)` 는 `referenceNode` 가 **호출 대상의 자식이 아니면**
`NotFoundError` 를 던진다. HEAD(`git show HEAD:commands/dashboard.md:1184`)에서는
`<button id="dz-pip-btn">` 이 `<body>` 직계 자식이라 성립했지만, 지금은 부모가
`#dz-top-actions` 다 — **`document.body` 의 자식이 아니다.**

| | HEAD | 현재 워킹트리 |
|---|------|--------------|
| `pipButton.parentNode` | `document.body` | `#dz-top-actions` |
| `document.body.insertBefore(wrap, pipButton)` | 정상 | **NotFoundError** |

**증상**: 플로팅 창을 닫으면 `pagehide` 핸들러가 첫 줄에서 던지고, 그 뒤의
`pipButton.textContent = '플로팅'`·`setHint`·`poll()` 이 전부 실행되지 않는다. `.wrap` 은 파괴되는
PiP 문서에 남아 **opener 페이지가 영구히 빈 화면이 된다**(새로고침 전까지 복구 불가).
이 도구의 핵심 기능이 왕복 1회로 죽는다.

**테스트가 이 버그를 잡지 못한 이유**: `tests/run.sh` T22-34 가
`grep -qF 'insertBefore(wrap, pipButton)'` 로 **깨진 호출을 리터럴로 고정**하고 있다. 문자열 존재
검사라 부모-자식 관계는 볼 수 없다. 지금 `bash tests/run.sh` 는 24/24 통과한다 — 안전망이 오히려
회귀를 굳혔다.

**이 PRP 의 §5.4 가 수정과 T22-34 개정을 함께 다룬다.** 이 항목은 승인 대상이 아니라 **필수**다.

---

## 1. 요구사항 요약

프로젝트 대시보드에 (1) **최상단 프로젝트 명 헤더**를 만들고 그 옆에 `#dz-top-actions`
(플로팅 버튼 + 테마 토글)를 배치하며, (2) **진행 단계 목록에 「커밋」이 끼어드는 것을 막아
「검수」 완료가 곧 100% 가 되도록** 호출 규약에 가드를 명문화한다. (1)은 대시보드가 어느
프로젝트의 것인지 화면만 보고 알 수 없다는 문제(같은 머신에서 8791~8793 세 포트로 서로 다른
프로젝트 대시보드가 동시에 열린다)를 풀고, 허브(`hub_template.html`)의 `.project-head` 패턴과
같은 디자인 언어를 쓴다. (2)는 이미 문서화돼 있으나 **강제 장치가 없어** 실제로 어긋난 관례를
호출 지점에서 막는다.

**비목표**: 진행률 산술·`step`/`impl`/`log` 절차의 변경, 허브 파일 수정, `<title>` 변경,
새 색 토큰 도입, 레이아웃 개편(카드 구성·정보 구조).

---

## 2. 확정 전제 (재론하지 않는다)

1. **허브가 시각 정본이다.** 직전 PRP 의 전제를 그대로 잇는다 — 프로젝트 명 표기는 허브의
   `.project-name`(`font-size:15px;font-weight:800;color:var(--head)`) 을 그대로 쓴다.
2. **`commands/dashboard.md` 는 문서이자 스펙이다.** 템플릿 HTML/CSS 를 고치면 같은 파일 안의
   산문·표·CSS 사본을 같은 커밋에서 함께 고친다(§7).
3. **허브 파서(`hub/bin/hub_parse.py`)의 6개 정규식이 걸린 줄은 한 글자도 바꾸지 않는다.**
   특히 `_TITLE_PATTERN = r'<h1 id="dz-title">(?P<text>.*?)</h1>'` — `#dz-title` 을 `<h2>` 로
   강등하거나 태그를 바꾸면 이 프로젝트가 허브에서 **조용히 티어 2로 강등**된다(§9 GOTCHA 1).
4. **`hub/` 는 읽기 참고 대상이며 이번 변경에서 수정하지 않는다.**
5. **프로젝트 명은 `init` 이 1회만 쓰고 그 뒤로 아무도 바꾸지 않는다** — 동적 6 셀렉터도,
   폴링 동기화 대상도 아니다(§4.3).

---

## 3. 영향 범위

### 3.1 `commands/dashboard.md` (1513행) — 주 변경 대상

| 구간(현재 행) | 무엇 | 변경 |
|--------------|------|------|
| 19-38 | 「호출 규약」 | `init` 인자 설명 아래에 **진행 단계 가드 문구** 추가(§6.1) |
| 40-52 | CLAUDE.md 트리거 표 | 표 아래 주석에 권장 단계 목록 1줄 추가(§6.1) |
| 97-101 | "init 시점에만 생성되고 이후 치환되지 않는 구조 요소" 목록 | `#dz-project-name` 행 추가(§4.3) |
| 203-215 | 「정적 요소 추가」 표 | `#dz-page-head` 행 추가, `#dz-top-actions`·`#dz-pip-hint` 행의 위치 설명 갱신 |
| 217-219 | 「왜 `.wrap` 바깥인가」 절 | 배치가 `position:fixed` → 헤더 행으로 바뀌었으나 **`.wrap` 바깥이라는 사실은 그대로**임을 명시. T22-37 의 판정 방식이 바뀐 것도 함께 기록 |
| 228-241 | 폴링 동기화 계약 표 | `#dz-project-name` 을 "치환하지 않는다" 행에 추가 |
| 376-390 | `init` 1~2단계 | 1단계 Bash 에 `basename "$PWD"` 이어붙임, 2단계에 프로젝트 명 치환 + 이스케이프 규칙(§5.1) |
| 392-397 | `init` 3단계(인자 파싱) | 커밋/푸시 세그먼트 처리 규칙 추가(§6.2) |
| 1069-1099 | 템플릿 `DZ:DASHBOARD` 주석 | `#dz-page-head`·`#dz-project-name` 항목 추가 |
| 1182 · 1187 | `#dz-top-actions`·`#dz-pip-hint` CSS | `position:fixed` 제거 → 헤더 행 흐름 배치(§5.3) |
| (신규) | — | `#dz-page-head`·`.project-name` CSS 추가(§5.3) |
| 1194-1199 | `body.dz-embedded` 규칙 블록 | `body.dz-embedded #dz-page-head{display:none}` 추가(§5.5). **기존 나열 규칙은 그대로 둔다**(§9 GOTCHA 3) |
| 1225-1254 | `<body>` 마크업 | 헤더 행 신설, `#dz-top-actions`·`#dz-pip-hint` 를 그 안으로 이동(§5.2) |
| 1271 부근 | 스크립트 지역 변수 선언부 | `wrapReturnAnchor` 캡처 1줄 추가(§5.4) |
| 1414 | PiP 복귀 | `insertBefore(wrap, pipButton)` → `insertBefore(wrap, wrapReturnAnchor)`(§5.4, **P0**) |

### 3.2 `tests/run.sh` (3262행)

| 행 | 무엇 | 변경 |
|----|------|------|
| 999 · 1002 | `T22-1~T22-114` 범위 문구 2곳 | 새 최대 번호로 갱신 |
| 1271-1275 | **T22-34** `insertBefore(wrap, pipButton)` 리터럴 | **개정 필수** — 지금 P0 버그를 고정하고 있다(§0) |
| 1297-1305 | **T22-37** 줄 번호 "뒤" 비교 | **개정 필수** — 헤더가 `.wrap` **앞**에 오면 이 비교가 깨진다(§8.1) |
| 1913-1921 | **T22-108** 줄 번호 "뒤" 비교 | **개정 필수** — 같은 이유 |
| T22-114 직후(`log_ok` 앞) | 신규 하위 검증 삽입 지점 | T22-115~121 추가(§8.2) |

`total_tests=24` 는 바뀌지 않는다(하위 검증만 늘어난다).

### 3.3 `README.md`

| 행 | 변경 | 필수 |
|----|------|------|
| 120 | 커맨드 표의 `dashboard` 줄 수 `1510` | **이미 실제(1513)와 어긋난 선행 드리프트**. 이번 변경으로 더 벌어지므로 같이 고친다 |
| 140-155 | `/dashboard` 설명 | "상단에 프로젝트 명(디렉토리 이름)이 표시된다" 1줄. 스크린샷 재촬영 여부는 §11 결정 4 |

### 3.4 `hub/` — **읽기 참고만, 수정 없음**

- `hub/bin/hub_template.html` — `.project-head`(116행)·`.project-name`(127-133행)·
  `renderProjectNameEl`(727-731행) 패턴을 **참고**한다. 파일은 건드리지 않는다.
- `hub/bin/hub_model.py:456` `_display_name(path)` — 프로젝트 표시 이름 = 경로 마지막 세그먼트.
  §5.1 의 값 출처가 이 규칙과 **일치해야 하는 근거**다. 파일은 건드리지 않는다.
- `hub/bin/hub_parse.py` — 정규식 6종의 대상 줄이 이번 변경에서 하나도 안 바뀐다(전제 3).
  **새 패턴을 추가하지 않는다** — 허브는 프로젝트 명을 이미 경로에서 얻는다.
- `hub/README.md` — 사용자에게 보이는 허브 쪽 동작 변화가 없다. 무변경.

### 3.5 미영향

- `tests/hub/fixtures/*.html` — 파서 계약 불변이므로 갱신 불필요.
- `install.sh` — 파일 수·경로 불변.
- 루트 `CLAUDE.md` — 이미 4단계(설계/승인/구현/검수)만 `step` 으로 세도록 돼 있다. §6 의 가드는
  **호출 지점**인 `commands/dashboard.md` 에 둔다(§6.3 근거).

---

## 4. 데이터 모델

### 4.1 새 셀렉터

```
#dz-project-name : Text        # 프로젝트 표시 이름. basename(cwd) 과 같다
#dz-page-head    : Container   # 프로젝트 명 + #dz-top-actions + #dz-pip-hint 를 담는 행
```

| 속성 | 값 |
|------|-----|
| 타입 | 순수 텍스트 노드(`<span>`). **헤딩이 아니다** — 문서의 유일한 `<h1>` 은 `#dz-title` 로 남는다(전제 3) |
| 값의 출처 | `basename "$PWD"` — 허브 `_display_name(path)` 과 **같은 규칙**(§5.1) |
| 치환 시점 | `init` 2단계, **1회뿐** |
| 이스케이프 | `&`→`&amp;`, `<`→`&lt;`, `>`→`&gt;` 순서(`log`·`step` 과 동일 규칙) |
| 위치 | `.wrap` **바깥**(§5.2) — PiP 로 이동하지 않고, 폴링 동기화 영역 밖이다 |

### 4.2 절차별 처리

| 절차 | `#dz-project-name` 처리 |
|------|------------------------|
| `init` | Edit 1회로 텍스트를 채운다(§5.1). **Edit 총 횟수 4→5** |
| `step` | 읽지도 쓰지도 않는다 |
| `impl` | 읽지도 쓰지도 않는다 |
| `log` | 읽지도 쓰지도 않는다 |
| 폴링(`apply()`) | **`syncText` 호출을 추가하지 않는다** — §4.3 |

### 4.3 왜 「동적(치환 대상) — 6 셀렉터」 표에 넣지 않는가

넣지 않는다. 그 표의 정의는 "`init` **이후에도** 절차가 치환하는 대상"이고, 소비자 측 대응표가
곧 폴링 동기화 계약이다. 프로젝트 명은 `init` 이 1회 쓰고 끝나므로 **파일과 라이브 DOM 이
발산할 수 없다** — 동기화할 것이 없다. 이미 같은 성격의 요소가 둘 있고(`#dz-matrix`,
`#dz-group-{g}`), 문서는 그것들을 별도 목록
(「`init` 시점에만 생성되고 이후 치환되지 않는 구조 요소」, 97-101행)에 두고 있다.
**`#dz-project-name` 을 그 목록에 추가한다.** "6 셀렉터"라는 숫자와 폴링 계약표의 행 구성은
그대로 유지된다.

폴링 계약표에는 **"치환하지 않는다" 행을 추가**한다 — 근거란에
"`.wrap` 바깥 = 동기화 영역 밖 + `init` 이 1회만 쓰고 이후 불변"을 적는다. 두 근거를 모두 적는
이유: 앞의 근거는 §11 결정 2 에서 안 B 를 고르면 무너지지만 뒤의 근거는 어떤 배치에서도 참이다.

---

## 5. 요청 1 — 설계

### 5.1 프로젝트 명의 값 출처

**권장: `init` 1단계의 기존 Bash 호출에 `basename "$PWD"` 를 이어 붙인다.**

```bash
mkdir -p .claude && rm -f .claude/dashboard.html && basename "$PWD"
```

| 근거 | 내용 |
|------|------|
| 호출 규약 불변 | `init "<제목>" "<단계…>"` 2-인자 규약이 그대로다. CLAUDE.md 트리거 표·`argument-hint` 프론트매터·기존 호출부를 **하나도 고치지 않는다** |
| 추가 비용 0 | `init` 1단계가 **이미 Bash 를 1회** 부른다. 같은 호출에 얹으므로 도구 호출이 늘지 않는다 |
| 허브와 일치 | `hub_model._display_name(path)` 이 경로의 마지막 세그먼트를 쓴다. 같은 프로젝트가 허브 카드·허브 모달 제목·대시보드 헤더에서 **같은 문자열**로 보인다 |
| 기억 의존 없음 | 이 문서의 관례("기억에 의존하지 않는다", `step` 2단계 참조)를 지킨다. 모델이 아는 cwd 를 옮겨 적지 않고 셸이 준 값을 쓴다 |

- `init` 2단계를 "「`#dz-title`」 텍스트를 `<제목>` 인자로, 「`#dz-project-name`」 텍스트를
  1단계가 출력한 디렉토리 이름으로 치환한다(Edit 2회)"로 개정한다.
- **이스케이프를 명시한다.** 디렉토리 이름에 `&`(`R&D` 등)가 들어갈 수 있다. `log`·`step` 과
  같은 순서(`&`→`&amp;`, `<`→`&lt;`, `>`→`&gt;`)를 적용한다.
- 이름이 길면 CSS 가 말줄임 처리한다(`text-overflow:ellipsis` + `min-width:0`).

기각한 대안은 §10 에, 선택지는 §11 결정 1 에 있다.

### 5.2 마크업 — 권장안 A (헤더 행을 `.wrap` 바깥에 신설)

```html
<body>
<div id="dz-page-head">
  <span class="project-name" id="dz-project-name">프로젝트</span>
  <div id="dz-top-actions">
    <button id="dz-pip-btn" type="button">플로팅</button>
    <button id="dz-theme-toggle" class="icon-btn" type="button"
            aria-label="다크 테마로 전환" title="다크 테마로 전환">☾</button>
  </div>
  <div id="dz-pip-hint" hidden></div>
</div>
<div class="wrap">
  <div class="card">
    <h1 id="dz-title">세션 제목</h1>
    …
```

설계 판단:

- **`#dz-top-actions` 는 여전히 `.wrap` 바깥이다.** PiP 가 `.wrap` 을 통째로 옮기는 메커니즘은
  **한 줄도 바뀌지 않는다**. 요구("프로젝트 명 옆")와 제약("PiP 로 딸려가면 안 된다")이
  **헤더 자체를 `.wrap` 밖에 두는 것**으로 동시에 만족된다 — 둘을 조정할 새 장치가 필요 없다.
- **`#dz-pip-hint` 도 헤더 안으로 들어간다.** 지금은 `position:fixed;top:54px;right:18px` 로
  클러스터 바로 아래에 떠 있다. 클러스터가 흐름으로 내려오면 이 좌표는 근거를 잃는다. 같은
  헤더의 두 번째 행(`flex-basis:100%`)으로 두면 "버튼 아래 한 줄"이라는 현재의 시각 관계가
  그대로 유지된다.
- **`<span class="project-name">` 이지 `<h1>`/`<h2>` 가 아니다.** `<h1 id="dz-title">` 이 허브
  파서의 고정 앵커라 태그를 바꿀 수 없고(전제 3), `<h1>` 이 둘이 되면 문서 개요가 망가진다.
  허브도 `.project-name` 을 `<span>`/`<button>` 으로 렌더한다(`renderProjectNameEl`) — 같은 선택이다.
- **클래스명 `.project-name` 을 허브에서 그대로 가져온다.** 직전 PRP 가 `.icon-btn` 에 쓴 것과
  같은 근거다 — 두 파일을 나란히 읽을 때 같은 이름이 같은 모양을 뜻한다.
- 헤더 컨테이너는 `id` 만 갖는다(`class` 없음). 허브의 `.project-head` 는 드래그 핸들·티어
  배지·경과 시간까지 담는 행이라 규칙이 다르다 — 이름만 빌리면 거짓말이 된다.

### 5.3 CSS

```css
  /* 페이지 헤더 — .wrap 과 같은 폭 상자를 써서 카드와 좌우가 정렬된다.
     .wrap 바깥에 두는 이유는 아래 「왜 .wrap 바깥인가」 절과 같다(PiP 이동 대상 제외). */
  #dz-page-head{max-width:860px;margin:28px auto 10px;padding:0 16px;
                display:flex;align-items:center;gap:10px;flex-wrap:wrap;row-gap:6px}
  .project-name{font-size:15px;font-weight:800;color:var(--head);
                overflow:hidden;text-overflow:ellipsis;white-space:nowrap;min-width:0;flex:1 1 auto}
  #dz-top-actions{display:flex;align-items:center;gap:8px;flex:0 0 auto}
  #dz-pip-hint{flex-basis:100%;font-size:11px;line-height:1.5;color:var(--muted);
               background:var(--surface);border:1px solid var(--line);border-radius:8px;
               padding:8px 10px;box-shadow:var(--shadow)}
```

- `.wrap` 의 위쪽 여백을 헤더가 가져간다: `.wrap{margin:32px auto}` → `margin:0 auto 32px`.
  `body.dz-pip .wrap{margin:10px auto}` 는 이미 오버라이드라 영향 없다.
- **`position:fixed`·`top`·`right`·`z-index` 4선언이 `#dz-top-actions` 와 `#dz-pip-hint` 에서
  사라진다.** 이것이 이번 변경의 시각적 본체다.
- `#dz-pip-btn`·`.icon-btn` 규칙은 **손대지 않는다**(색·hover·focus 는 직전 PRP 가 이미 정렬했다).
- `#dz-pip-hint[hidden]{display:none}` 규칙은 그대로 유지한다 — `flex-basis` 가 붙은 flex 아이템은
  `hidden` 만으로는 `display:flex` 계열 UA 스타일과 충돌할 수 있어 명시 규칙이 여전히 필요하다.

**부작용 하나를 명시한다**: 클러스터가 흐름에 들어가므로 **스크롤하면 화면에서 사라진다**(지금은
`position:fixed` 라 항상 보인다). 로그가 길어진 세션에서 테마 토글·플로팅 버튼에 닿으려면 위로
올라가야 한다. 완화 수단(`position:sticky;top:0;background:var(--bg)`)은 §11 결정 4-b 로 올린다 —
기본은 **일반 흐름**이다(가장 단순하고, 스태킹 컨텍스트·카드 그림자와의 상호작용이 없다).

### 5.4 PiP 복귀 앵커 — §0 의 P0 수정 (필수)

```js
  var wrap = document.querySelector('.wrap');
  // .wrap 은 PiP 창으로 '이동'하므로 돌아올 자리를 미리 잡아 둔다. pipButton 을 앵커로 쓰면
  // 안 된다 — #dz-top-actions 의 자식이라 document.body.insertBefore 가 NotFoundError 를 던진다.
  var wrapReturnAnchor = wrap.nextSibling;
```

```js
        win.addEventListener('pagehide', function(){
          pipWindow = null;
          document.body.insertBefore(wrap, wrapReturnAnchor);   // ← pipButton 이 아니다
```

- `wrap.nextSibling` 은 `</div>` 뒤의 공백 텍스트 노드다. **아무도 제거하지 않으므로 안정적인
  앵커**이며, 헤더를 앞에 두든 뒤에 두든(§11 결정 2 의 어느 안을 고르든) 그대로 성립한다.
- `document.body.appendChild(wrap)` 로도 화면은 같지만 `<script>` 뒤로 밀린다 — 앵커 캡처가
  의도를 코드로 말한다.
- **T22-34 를 함께 개정한다**(§8.1). 지금 그 검사가 깨진 호출을 리터럴로 고정하고 있다.

### 5.5 세 가지 뷰에서의 검증

| 뷰 | 헤더 | `#dz-top-actions` | 판단 |
|----|------|------------------|------|
| 단독 탭 (`http://localhost:879x`) | 카드 위에 `프로젝트명 ……… [플로팅] [☾]` | 흐름 배치 | 요구 그대로 |
| PiP 압축 뷰 (`body.dz-pip`) | **PiP 창에 없다**(헤더가 `.wrap` 밖) | opener 에 남는다 | 압축 뷰는 원래 최소 정보다. opener 는 빈 화면 대신 `프로젝트명 [플로팅 닫기] [☾]` 가 남아 **어느 프로젝트의 플로팅인지 알 수 있다** — 지금보다 낫다 |
| 허브 모달 (`body.dz-embedded`) | **숨긴다** | 자식 3종이 이미 숨음 | 허브 모달 헤드가 `modalTitleEl.textContent = displayName`(hub_template.html:1102)으로 **같은 문자열을 이미 표시**한다. 8px 아래에 같은 이름이 또 나오면 중복이다 |

```css
  body.dz-embedded #dz-page-head{display:none}
```

- **기존 나열 규칙(`body.dz-embedded #dz-pip-btn,#dz-pip-hint,#dz-theme-toggle`)을 지우지 않는다.**
  T22-97·T22-109 가 그 리터럴을 검사하고(§9 GOTCHA 3), 그것은 "이 세 컨트롤은 임베드에서
  숨는다"는 **컨트롤 단위 계약**이라 헤더 구조가 또 바뀌어도 살아남는다. 새 규칙은 빈 헤더
  띠가 남지 않게 **행 자체**를 없앤다. 역할이 다르므로 중복이 아니다.

---

## 6. 요청 2 — 진행 단계에서 커밋 배제

### 6.0 왜 관례가 지켜지지 않았는가 (조사 결과)

문서 세 곳이 이미 4단계만 세도록 적고 있다:

| 위치 | 내용 |
|------|------|
| `commands/dashboard.md:42-52` 트리거 표 | `step 1~4`(설계·승인·구현·검수)만 `step`, 「커밋·푸시」는 `log commit` |
| 루트 `CLAUDE.md:73-75` | "착수 보고 시 `init`, 단계 전환마다 `step`·`log`" |
| `docs/prps/session-dashboard.md` | 동일 |

**그런데 어느 문서에도 "단계 목록에 커밋을 넣지 마라"는 문장이 없다.** 트리거 표는 *언제 부르는가*
를 말할 뿐 *`init` 의 두 번째 인자에 무엇을 넣는가* 는 말하지 않는다. 그 인자는 자유 문자열이고,
`init` 절차 3단계의 검증은 **"모든 세그먼트에 `:` 가 있는가/없는가"** 하나뿐이다 — 단계 **이름**은
전혀 보지 않는다. 즉 `"설계|승인|구현|검수|커밋"` 은 문법적으로 완벽히 유효하며, 그 순간
분모가 5가 되어 검수 완료가 80% 로 표시된다.

**가드는 값이 만들어지는 지점(= `init` 의 인자 파싱)에 있어야 한다.** 트리거 표에 한 줄 더 적는
것으로는 같은 실패가 반복된다(그 표가 이미 있는데 어긋났다).

### 6.1 문구 ① — 「호출 규약」 절 (19-38행), `init` 설명 아래

> **진행 단계 목록에 `커밋`·`푸시`를 넣지 않는다.** 커밋은 진행 단계가 아니라 결과 기록이며
> `log commit` 으로만 남긴다. **검수 완료가 곧 100% 다** — 커밋은 분모에 들어가지 않는다.
> 전체 경로의 표준 단계 목록은 `"설계|승인|구현|검수"` 다.

- 표준 목록을 리터럴로 박아 **복사해 쓸 수 있게** 한다. 관례를 지키는 가장 값싼 수단이다.
- 「호출 규약」에 두는 이유: 이 절이 `init` 인자 문법을 정의하는 유일한 곳이고, 트리거 표(40행)
  바로 위라 두 문서 조각이 한 화면에 들어온다.

### 6.2 문구 ② — `init` 절차 3단계(392-397행), 파싱 규칙 마지막 항목

> - 세그먼트가 `커밋`·`푸시`·`commit`·`push` 중 하나면 **그 세그먼트를 목록에서 제외하고 진행**하고,
>   "커밋은 진행 단계가 아니라 `log commit` 으로 기록되므로 단계 목록에서 제외했습니다"를 한 줄
>   보고한다. **중단하지 않는다** — 착수 보고를 막을 만한 오류가 아니다.

- **중단이 아니라 제외**인 이유: 이 절차의 "보고 후 중단"은 **해석이 모호할 때**의 규칙이다
  (섞인 문법 → 어느 쪽으로 읽을지 알 수 없음). 여기는 폐쇄 어휘 4개에 대한 **결정적** 판정이라
  모호함이 없고, 추측이 아니다. 대시보드는 보조 도구이므로 착수 자체를 막지 않는다.
- 제외 후 남은 목록이 비면(전부 커밋/푸시) 그때는 인자가 통째로 잘못된 것이므로 **보고 후
  중단**한다 — 이 예외는 한 줄로 함께 적는다.

### 6.3 범위를 여기서 끊는다 (과설계 방지)

- **루트 `CLAUDE.md` 는 고치지 않는다.** 이미 4단계 워크플로우를 정의하고 있고, 실패 지점은
  워크플로우 정의가 아니라 `init` 호출이다. 배포되는 전역 지침을 이 정도 사유로 흔들지 않는다.
- **런타임 검증 코드를 템플릿 JS 에 넣지 않는다.** 진행률은 `init` 이 확정한 분모를 `step` 이
  ±1 로 옮기는 산술이며, 여기에 "커밋 단계 감지" 로직을 넣으면 분모의 출처가 둘이 된다.
- **`step` 절차·진행률 산술은 한 글자도 바꾸지 않는다.** 이미 커밋을 세지 않는다.
- 산출물은 **문구 2개 + 회귀 검사 2개**다. 그 이상은 이 요청의 무게를 넘는다.

---

## 7. `commands/dashboard.md` 산문 동시 개정 (문서=스펙 제약)

| 위치 | 지금 | 개정 |
|------|------|------|
| 19-38 「호출 규약」 | 인자 문법만 | §6.1 문구 추가 |
| 40-52 트리거 표 아래 | — | "표준 단계 목록 = `설계\|승인\|구현\|검수`" 1줄 |
| 97-101 구조 요소 목록 | `#dz-matrix`·`#dz-group-{g}` 2행 | `#dz-project-name` 1행 추가(§4.3) |
| 203-215 정적 요소 표 | 7행 | `#dz-page-head` 1행 추가 + `#dz-top-actions`·`#dz-pip-hint` 설명을 "헤더 행 안"으로 갱신 |
| 217-219 「왜 `.wrap` 바깥인가」 | "T22-37 이 **줄 번호 비교**로 강제" | "T22-37 이 **`.wrap` 범위 밖 판정**으로 강제"로 개정(§8.1 과 동기화) |
| 228-241 폴링 계약표 | 8행 | `#dz-project-name` "치환하지 않는다" 1행 추가 |
| 376-390 `init` 1~2단계 | Bash 1회 + Edit(제목) | Bash 에 `basename` 추가, Edit 2회로 확대, 이스케이프 규칙 명시 |
| 392-397 `init` 3단계 | 판정 3갈래 | §6.2 항목 추가 |
| 1069-1099 템플릿 주석 | 정적 목록 | `#dz-page-head`·`#dz-project-name` 추가 |

---

## 8. 테스트 계획

### 8.1 개정이 필요한 기존 하위 검증

| 검사 | 상태 | 조치 |
|------|------|------|
| **T22-34**(1271-1275) `insertBefore(wrap, pipButton)` 리터럴 | **깨진 코드를 고정 중** | `insertBefore(wrap, wrapReturnAnchor)` 존재 **+ 역방향** `insertBefore(wrap, pipButton)` **부재** + 앵커 캡처(`wrap.nextSibling`) 존재. 세 갈래로 재작성 |
| **T22-37**(1297-1305) `#dz-pip-btn` 줄 > `#dz-updated` 줄 | **반드시 깨진다**(헤더가 `.wrap` 앞) | 판정을 "**`.wrap` 범위 밖**"으로 일반화: `wrap_open_line`(`<div class="wrap">` 마지막 등장) ≤ `pip_button_line` ≤ `wrap_end_line`(`id="dz-updated"` 마지막 등장) 이면 실패. 안쪽 범위를 직접 배제하므로 **안전망의 강도는 그대로**이고 앞/뒤 배치 모두 허용한다 |
| **T22-108**(1913-1921) 토글 줄 > `#dz-updated` 줄 | **반드시 깨진다** | T22-37 과 같은 방식으로 재작성 |
| T22-97(`body.dz-embedded #dz-pip-btn` 리터럴) | 통과 유지 | 나열 규칙을 지우지 않는다(§5.5, GOTCHA 3) |
| T22-109(`body.dz-embedded #dz-theme-toggle`) | 통과 유지 | 같은 이유 |
| T22-99(`isEmbedded` **정확히 2줄**) | 통과 유지 | 새 코드에 이 문자열을 쓰지 않는다(GOTCHA 4) |
| T22-2 셀렉터 6종 · T22-3 배지 · T22-4 필터 · T22-27 색 토큰 · T22-111 리터럴 역방향 | 통과 유지 | 새 색·새 토큰을 만들지 않는다 |
| T22-31/32(`requestWindow` **1줄**) · T22-33(스타일 복사) | 통과 유지 | PiP 진입 경로는 손대지 않는다 |
| T22-38 역방향(`[id="dz-` 부재) | 통과 유지 | 프로젝트 명은 JS 가 참조하지 않는다 |
| T22-92 역방향(`dz-subtitle` 부재) | 통과 유지 | 새 id 는 `dz-project-name` 이다. **부제를 되살리는 것이 아니다**(GOTCHA 5) |
| T22-105~107·110·112·114(테마·FOUC·PiP 테마 전파·inset ring) | 통과 유지 | 테마 코드·색 규칙 무변경 |
| T22-113(문서 정합) | 통과 유지 | 검사 대상 문자열이 그대로 남는다 |
| 999·1002 범위 문구 | 갱신 | `T22-1~T22-114` → `T22-1~T22-121` |

### 8.2 신규 하위 검증 (T22-115~121, T22-114 뒤 `log_ok` 직전)

| 번호 | 검사 | 막는 회귀 |
|------|------|----------|
| T22-115 | `id="dz-project-name"` 존재 **그리고** 같은 줄에 `class="project-name"` | 요소가 사라지거나 허브와 다른 클래스로 갈라지는 회귀 |
| T22-116 | 줄 번호 순서 `dz-page-head` < `dz-project-name` < `dz-top-actions` < `<div class="wrap">` | ① 클러스터가 헤더 밖으로 이탈(요청 1 원상복귀) ② 헤더가 `.wrap` 안으로 들어가 PiP 로 딸려가는 회귀. **한 검사로 "같은 행 안 + `.wrap` 바깥"을 동시에 고정한다** |
| T22-117 | 역방향 — `#dz-top-actions{` 로 시작하는 CSS 줄에 `position:fixed` 부재 | 흐름 배치로 옮겼는데 fixed 가 남아 헤더에 빈 칸만 생기고 클러스터는 우상단에 뜨는 회귀 |
| T22-118 | `body.dz-embedded #dz-page-head` 규칙 존재 | 허브 모달에서 프로젝트 명이 모달 제목과 중복 표시되는 회귀 |
| T22-119 | `init` 절에 `basename "$PWD"` 존재 **그리고** 「정적 요소」/구조 요소 목록에 `#dz-project-name` 행 존재 | 값 출처가 사라져 헤더가 `프로젝트` 플레이스홀더로 남는 회귀 + 코드만 바뀌고 스펙 문서가 뒤처지는 회귀(T22-113 대칭) |
| T22-120 | 「호출 규약」의 가드 문구 존재 — `진행 단계 목록에 ` + `커밋` + `log commit 으로만` 를 포함하는 줄, **그리고** `검수 완료가 곧 100%` 문구 | 요청 2 의 명문 규칙이 편집 중 유실되는 회귀 |
| T22-121 | 표준 단계 목록 리터럴 `설계\|승인\|구현\|검수` 존재 **그리고** `init` 3단계에 `제외하고 진행` 문구 존재 | 복사용 표준 목록·파싱 시 제외 규칙이 사라지는 회귀 |

### 8.3 순수 로직 단위 테스트

새 순수 함수가 없다. 프로젝트 명은 셸(`basename`)이 만들고 Edit 이 옮기는 값이며, 새 JS 함수는
추가되지 않는다(§5.4 의 앵커 캡처는 변수 1개다). 이 저장소에서 `commands/*.md` 의 등가 안전망은
`tests/run.sh` 의 grep 계약이다 — 직전 PRP §9.3 과 같은 판단이며 새 하네스를 만들지 않는다.

### 8.4 수동 확인 (자동화 대상 아님)

1. `/dashboard init "제목" "설계|승인|구현|검수"` → 헤더에 `coding-env` 가 뜨고 오른쪽에
   `[플로팅] [☾]` 가 같은 행에 정렬된다.
2. 라이트/다크 각각에서 헤더 텍스트·버튼 대비 확인.
3. **플로팅 진입 → 즉시 닫기**(§0 P0 회귀 확인): opener 에 `.wrap` 이 정확히 헤더 아래로
   돌아오고 콘솔에 예외가 없다. 두 번 반복해도 같다.
4. 플로팅 창에는 헤더가 없고 opener 에는 `프로젝트명 [플로팅 닫기] [☾]` 가 남는다.
5. 허브 카드 클릭 → 모달 안 대시보드에 프로젝트 명 헤더가 **보이지 않는다**(모달 제목이 대신 표시).
6. 디렉토리 이름이 긴 프로젝트에서 말줄임 처리 확인, `&` 가 든 이름에서 `&amp;` 가 그대로
   보이지 않는지(이스케이프 역방향) 확인.
7. `/dashboard init "제목" "설계|승인|구현|검수|커밋"` → 커밋이 제외되고 분모가 4, 한 줄 보고가 온다.
8. `step 4 done` 후 진행률이 `4/4 · 100%` 다.

---

## 9. 구현 순서와 GOTCHA

**순서**: ① §5.4 PiP 복귀 앵커 수정 + T22-34 개정 → `bash tests/run.sh` (P0 를 먼저 격리한다) →
② 마크업 헤더 신설(§5.2) → ③ CSS(§5.3, §5.5) → ④ `init` 절차 개정(§5.1) →
⑤ 요청 2 문구 2건(§6.1·§6.2) → ⑥ 산문·표 동기화(§7) → ⑦ `tests/run.sh` T22-37·108 개정 +
T22-115~121 추가 + 범위 문구(§8) → ⑧ `README.md`(§3.3) → ⑨ `bash tests/run.sh` 전체.

- **GOTCHA 1 — `<h1 id="dz-title">` 줄을 건드리지 않는다.** `hub_parse._TITLE_PATTERN` 이
  `<h1 id="dz-title">` 를 **리터럴로** 찾는다. 태그를 바꾸거나 프로젝트 명과 한 줄로 합치면
  이 프로젝트가 허브에서 조용히 **티어 2로 강등**된다. 프로젝트 명은 **별도 요소**다.
- **GOTCHA 2 — P0 수정을 뒤로 미루지 않는다.** ①을 먼저 하고 테스트를 돌린다. 마크업을 먼저
  바꾸면 T22-37 실패와 P0 가 뒤엉켜 어느 쪽이 원인인지 흐려진다.
- **GOTCHA 3 — `body.dz-embedded` 나열 규칙을 통합하지 않는다.** `#dz-page-head` 하나로 묶어
  숨기고 나열 규칙을 지우면 T22-97·T22-109 의 `grep -qF` 두 개가 동시에 깨진다. **두 규칙을
  공존시킨다**(근거는 §5.5).
- **GOTCHA 4 — 새 코드에 `isEmbedded` 문자열을 쓰지 않는다.** T22-99 가 `grep -c` 로 **정확히
  2줄**을 요구한다. 부분 문자열이라 어떤 식별자에 포함돼도 카운트에 걸린다.
- **GOTCHA 5 — `#dz-subtitle` 을 되살리는 변경이 아니다.** T22-92 가 `dz-subtitle` 문자열의
  **완전 부재**를 검사한다(부제는 커밋 9f32074 에서 제거됐다). 새 id 는 `dz-project-name` 이며,
  프로젝트 명은 부제(세션 요약)와 성격이 다르다 — 값이 세션마다 바뀌지 않는다.
- **GOTCHA 6 — `init` 의 Edit `old_string` 은 "템플릿 줄 전문 그대로"다.** 헤더를 추가하면
  `#dz-project-name` 줄이 새 앵커가 된다. `#dz-title`·`#dz-progress-bar`·`#dz-progress-pct`·
  `#dz-updated`·`<ol class="steps" id="dz-steps">` **줄의 문자열은 바꾸지 않는다** — §5 의 어떤
  변경도 이 줄들을 건드리지 않는 것은 우연이 아니다.
- **GOTCHA 7 — 헤더 안 요소는 `.wrap` 안이 아니므로 `syncText` 대상이 될 수 없다.** 폴링의
  `syncText(fresh, id)` 는 `wrap.querySelector('#'+id)` 로 **`.wrap` 안에서만** 찾는다.
  나중에 프로젝트 명을 동기화하고 싶어지면 함수부터 고쳐야 한다 — 지금은 필요 없다(§4.3).
- **GOTCHA 8 — `.wrap` 의 `margin-top` 을 헤더로 옮기는 것을 잊지 않는다.** 잊으면 헤더와 카드
  사이가 42px 로 벌어진다. `body.dz-pip .wrap` 오버라이드는 별도라 영향받지 않는다.
- **GOTCHA 9 — T22-37/108 재작성 시 `tail -1` 관례를 유지한다.** 이 grep 들은 **산문에도
  등장하는 문자열**을 찾으므로 마지막 등장(= 템플릿)을 써야 한다. `<div class="wrap">` 은 현재
  파일 전체에서 1회(1226행)뿐이지만, 산문에 인용될 가능성에 대비해 같은 관례를 쓴다.

---

## 10. 리스크와 채택하지 않은 대안

### 리스크

| # | 리스크 | 완화 |
|---|--------|------|
| R1 | 클러스터가 흐름으로 내려가 **스크롤하면 사라진다**(지금은 항상 보임) | §11 결정 4-b. 필요하면 `position:sticky` 2선언으로 되돌릴 수 있고, 실제 사용 패턴(모달·PiP 중심)에서는 영향이 작다 |
| R2 | 디렉토리 이름이 프로젝트 이름과 다른 경우(`repo` · `src` 같은 일반명) 헤더가 무의미해진다 | 허브가 **이미 같은 규칙**으로 표시 중이라 새로 생기는 문제가 아니다. 두 화면이 최소한 **일치**한다. 커스텀 이름이 필요하면 §11 결정 1 의 안 2 |
| R3 | T22-37/108 의 "줄 번호 뒤" 안전망을 푸는 것이 배치 회귀의 문을 연다 | "범위 밖" 판정이 **더 정확한 안전망**이다 — 기존 검사는 `.wrap` 앞에 두는 정당한 배치까지 막았을 뿐, 안쪽 배치 차단력은 동일하다 |
| R4 | `init` 의 Edit 이 4→5회로 늘어난다 | `init` 은 기능당 1회다. 이 도구의 비용 관심사는 세션 중 수십 번 불리는 `step`·`log` 이며 그 절차는 무변경이다 |
| R5 | 프로젝트 명이 `<h1>` 이 아니어서 문서 개요상 최상위가 세션 제목이다 | 전제 3(허브 파서)의 직접 결과다. 시각적 위계는 CSS 로 표현되고, 스크린리더 사용자에게도 텍스트는 헤더 행 첫 요소로 먼저 읽힌다 |
| R6 | §0 P0 수정이 이번 PRP 범위 밖의 코드를 건드린다는 인상 | 직전 작업이 만든 결함이고 같은 브랜치의 미커밋 변경이다 — 같은 커밋에서 고치는 것이 맞다. 별도 이슈로 남기면 깨진 채로 커밋된다 |

### 검토했으나 채택하지 않은 대안

- **대안 1 — `<title>` 에도 프로젝트 명을 넣는다.** 매력적이다(8791~8793 세 탭이 지금 구분되지
  않는다). 기각 사유: `init` Edit 이 하나 더 늘고, `<title>` 은 PiP 창이 `pipDocument.title =
  document.title` 로 복사하는 값이라 영향 범위가 한 곳 더 열린다. 헤더가 이미 같은 정보를 준다.
  **탭 구분이 실제로 불편하다고 판명되면 그때 1줄로 추가한다**(YAGNI).
- **대안 2 — 프로젝트 명을 `.card` 안, `<h1 id="dz-title">` 위에 둔다.** 기각. PiP 압축 뷰에도
  따라가 좁은 창에서 한 줄을 잡아먹고, 허브 모달에서는 모달 제목과 중복된다. 무엇보다
  `#dz-top-actions` 를 "옆에" 두려면 §11 결정 2 의 안 B(detach/reattach)가 강제된다.
- **대안 3 — `#dz-top-actions` 를 `position:absolute` 로 `.wrap` 과 같은 860px 상자에 겹쳐
  정렬한다(§11 결정 2 안 C).** 기각. `.card{padding:26px 30px}`·`.wrap{padding:0 16px}` 에서
  유도한 매직 넘버(`top:57px`·`padding-right:46px`)로 정렬이 유지되며, 카드 패딩을 바꾸는
  순간 조용히 어긋난다. 근거 없는 상수를 두 개 만든다.
- **대안 4 — 커밋 단계를 진행률 **분모에서만** 빼고 화면에는 남긴다.** 기각. `step` 의 ±1 전이
  산술이 "모든 칸이 분모"라는 단일 규칙 위에 서 있다. 예외 칸을 만들면 `step`·`impl`·감사용
  재계수 세 곳이 동시에 복잡해진다. 커밋은 애초에 `log commit` 이라는 자리가 있다.
- **대안 5 — `init` 이 커밋 세그먼트를 발견하면 보고 후 중단한다.** 보류(=§11 결정 3 의 안 3).
  기존 "섞인 문법 → 중단"과 강도가 같아 일관적이지만, 그쪽은 **해석이 모호할 때**의 규칙이고
  여기는 결정적 판정이다. 착수 보고를 막는 비용이 이득보다 크다고 보아 권장안에서 제외했다.

---

## 11. 사용자 승인이 필요한 항목

### 결정 1 — 프로젝트 명의 값 출처

| 안 | 내용 | 대가 |
|----|------|------|
| **1-A (권장)** | `init` 1단계 Bash 에 `basename "$PWD"` 를 이어 붙여 자동 유도 | 호출 규약 불변, 추가 도구 호출 0회, 허브 표시 이름과 항상 일치. 디렉토리명 = 표시명이 강제된다 |
| 1-B | `init` 에 **선택적 3번째 인자** 추가(`init "<제목>" "<단계>" ["<프로젝트명>"]`) | 이름을 자유롭게 정할 수 있다. 공개 호출 규약·`argument-hint`·트리거 표가 함께 바뀌고, **아무도 넘기지 않을 인자**가 생긴다(YAGNI) |
| 1-C | `git rev-parse --show-toplevel` 의 basename | worktree 에서 원 저장소 이름이 나온다. 허브 표시 이름(`cwd` 기준)과 **어긋난다** — 같은 프로젝트가 두 화면에서 다른 이름이 된다 |

### 결정 2 — `#dz-top-actions` 배치 방식

| 안 | 내용 | 대가 |
|----|------|------|
| **2-A (권장)** | `.wrap` **바깥**에 헤더 행(`#dz-page-head`)을 신설하고 프로젝트 명·`#dz-top-actions`·`#dz-pip-hint` 를 함께 담는다. `position:fixed` 폐기 | PiP 메커니즘 **무변경**(JS 추가 0줄). T22-37·T22-108 을 "범위 밖" 판정으로 재작성. 클러스터가 스크롤과 함께 사라진다(R1) |
| 2-B | `#dz-top-actions` 를 `.card` 안 헤더 행으로 옮기고, PiP 진입 시 detach → 종료 시 reattach | 프로젝트 명이 PiP 압축 뷰에도 보인다. 대신 PiP 경로에 DOM 이동 2개 + "분리 상태" 전용 CSS 클래스가 늘고, **reattach 실패 시 창을 닫을 수단이 사라지는** 새 실패 모드가 생긴다. T22-37·T22-108 은 폐기하고 detach/reattach 존재 검사로 대체 |
| 2-C | 구조는 그대로 두고 `position:absolute` 로 카드 우상단에 시각 정렬만 | 마크업 변경 최소. 대신 `.card`/`.wrap` 패딩에서 유도한 매직 넘버 2개가 생기고, 패딩이 바뀌면 조용히 어긋난다. 프로젝트 명과 "같은 행"이 아니라 "겹쳐 보이는" 배치다 |

### 결정 3 — 진행 단계 가드의 강도

| 안 | 내용 | 대가 |
|----|------|------|
| **3-A (권장)** | 「호출 규약」 명문 규칙 + `init` 3단계에서 커밋/푸시 세그먼트 **제외 후 진행 + 1줄 보고** + 회귀 검사 2개 | 값이 만들어지는 지점에서 막는다. 문서 4줄 + 테스트 2개 |
| 3-B | 명문 규칙 문구만 추가(런타임 처리 없음) + 회귀 검사 1개 | 가장 작다. 다만 **트리거 표가 이미 있는데도 어긋났다** — 문장만으로 같은 실패를 막을 수 있다는 근거가 약하다 |
| 3-C | 커밋 세그먼트 발견 시 **보고 후 중단** | 기존 "섞인 문법" 처리와 강도가 같다. 대신 대시보드 실수가 착수 보고 자체를 막는다 |

### 결정 4 — 부수 항목 3건

- **4-a. 허브 모달에서 프로젝트 명 헤더를 숨길지** → **권장: 숨긴다.** 허브 모달 헤드가 같은
  문자열을 이미 표시한다(`hub_template.html:1102`). 남기면 8px 간격으로 같은 이름이 두 번 보인다.
- **4-b. 헤더를 `position:sticky` 로 둘지** → **권장: 아니오(일반 흐름).** 가장 단순하고
  스태킹 컨텍스트 문제가 없다. R1 이 실제로 불편하면 CSS 2선언으로 되돌릴 수 있다.
- **4-c. `README.md` 스크린샷(`docs/images/dashboard-sample.png`) 재촬영 여부** → 화면 최상단이
  바뀌므로 기존 이미지가 구식이 된다. 줄 수 표기(`1510` → 실제)는 어차피 고친다.
