# 대시보드 UI 정리 — 세션 탭 제거 · `init` 항상 재발행 · 「지금」 카드 재설계 · 부제 제거 (PRP)

## 요구사항 요약

`/dashboard` 산출물(`.claude/dashboard.html`)에서 **쓰이지 않거나 중복되는 UI 를 걷어내고, `init`
한 번으로 브라우저에 곧바로 보이게** 한다. 구체적으로는 (1) 초기 발행 시 브라우저 탭이 열리는 데서
그치지 않고 **그 탭이 전면으로 올라오게** 하고, (2) **메인 세션 구분 UI(세션 탭·구분선·세션 번호)를
전부 제거**하고 `init` 이 불릴 때마다 대시보드를 **처음부터 새로** 만들며, (3) 단계 목록과 내용이
겹치던 **「지금」 카드를 목록에 없는 정보만 담도록 재설계**하고 내부 정렬을 바로잡고, (4) 정보량이
낮은 **`#dz-subtitle` 을 제거**한다.

이 저장소에서 `commands/dashboard.md` 는 **문서이자 곧 구현**이다 — 오케스트레이터가 따르는 절차와
산출물 HTML 템플릿 전문이 한 파일에 들어 있다. 따라서 이 PRP 의 구현은 그 파일(+ 회귀 테스트 + README)
편집이다.

---

## 기준선

| 항목 | 값 |
|------|-----|
| 브랜치 | `feature/dashboard-ui-cleanup` (`main` 의 `57ac55d` 에서 분기) |
| `commands/dashboard.md` | **1512줄** |
| `tests/run.sh` | **2070줄** (T22-1~T22-90, T23-1~T23-11, `total_tests=22`) |
| `README.md` | **342줄** (L120 의 줄 수 `1512` 는 현재 정확하다) |
| 워킹 트리 | `hub/`·`tests/hub/`(미추적)만 존재 — 이번 작업과 무관, **건드리지 않는다** |

### 착수 전에 반드시 알아야 할 두 가지 사전 발견

> **발견 1 — 이 파일에는 자동화 회귀 테스트가 있다.**
> 요청서에는 "자동화 테스트가 없다"고 적혀 있으나 사실이 아니다. `tests/run.sh` 의
> `test_dashboard_template_integrity()`(L998~L1890)가 **T22-1~T22-90** 하위 검증으로
> `commands/dashboard.md` 를 grep 정합성 검사한다. 이번 변경은 그중 **최소 13개를 직접 깨뜨린다**
> (세션 탭·부제·`init` 재사용 분기를 존재 검증하는 항목들). 테스트 수정은 **선택이 아니라
> 구현 범위의 일부**다. 상세 목록은 [테스트 계획](#테스트-계획)에 있다.
> 실행: `bash tests/run.sh` (프로젝트 루트).

> **발견 2 — Write 도구는 "세션에서 읽지 않은 기존 파일"을 덮어쓰지 못한다.**
> 요구 2의 권장안("`init` 은 항상 템플릿을 새로 Write 한다")은 이 제약에 그대로 걸린다.
> 그래서 새 `init` 1단계는 **Bash 로 파일을 먼저 지운 뒤** Write 한다
> (`mkdir -p .claude && rm -f .claude/dashboard.html`). 이 한 줄이 (a) 디렉토리 보장,
> (b) 도구 제약 회피, (c) 낡은 바이트 잔존 방지를 동시에 처리하며 **존재 여부 분기를 없앤다.**
> 이 줄을 빠뜨리면 두 번째 `init` 이 도구 오류로 실패한다 — 구현 중 가장 밟기 쉬운 함정이다.

---

## 요청 4건과 최종 결정

| # | 사용자 요청 | 최종 결정 | 승인 필요 |
|---|------------|----------|:---:|
| 1 | 초기 발행 시 브라우저 자동 열기 + **그 탭으로 포커스** | 열기 절차는 **이미 있다**. 빠진 것은 **세션 내장 브라우저 도구 경로의 탭 활성화**뿐 — 그 경로에 "탭 선택/포커스 계열 도구가 함께 있으면 navigate 직후 한 번 더 호출한다"를 추가한다. OS `open`/`xdg-open` 경로는 **기본이 이미 포커스**이므로 무변경, 대신 `open -g`(백그라운드) 금지 한 줄을 가드로 남긴다 | 아니오 |
| 2 | 세션 탭 UI 전면 제거 + 매 메인 세션마다 새로 발행 | **권장안 A**: `init` 은 파일 존재 여부와 무관하게 **항상 처음부터 새로 만든다**. 로그 보존 로직·grep 앵커 기반 부분 치환·세션 번호·세션 탭·구분선을 **전부 제거**한다 | **예** (아래 트레이드오프) |
| 3 | 「지금」 카드가 단계 목록과 중복·정렬 불량 | 카드에서 **단계 번호·이름을 없애고**, 목록에 없는 값 3개(`경과` / `마지막 기록` / `다음`)만 **라벨-값 고정폭 2열**로 정렬한다. 불변식 7(파생 뷰)과 CSS 자동 숨김은 유지 | 아니오 |
| 4 | `#dz-subtitle` 제거 | 셀렉터·마크업·CSS(`.sub`, `body.dz-pip .sub`)·`init` 5단계·폴링 동기화 호출·헤더 주석까지 **전부 제거**. 동적 셀렉터 **7 → 6**. 그룹 문법의 그룹명은 **버린다**(다른 곳으로 옮기지 않는다) | 아니오 |

### 요구 2의 트레이드오프 — 승인이 필요한 유일한 결정

**권장안 A — `init` 은 언제나 완전히 새로 시작한다.**

- 얻는 것: 절차가 한 분기로 줄어든다. 사라지는 자산이 크다 — 「이미 존재하면」 분기(a~e),
  세션 번호 확정·갱신, 세션 구분 항목 prepend, 「세션 탭 갱신」 하위 절 전체(38줄), 템플릿 세대
  확인, 구현 세부 작업 초기화, 그리고 그 분기 때문에 필요했던 **grep 앵커 기반 부분 치환**이
  통째로 없어진다(방금 우리가 쓴 파일이므로 `old_string` 이 템플릿 그대로다).
  `<style>` 에 세션 규칙을 주입하던 경로가 사라져 **모든 대시보드의 CSS 가 템플릿과 동일**해지고,
  그 덕에 폴링의 `sessionTabsChanged()` 전체 리로드 경로도 함께 소멸한다.
- **잃는 것(이것이 승인 대상이다)**: 같은 메인 세션에서 `init` 을 두 번 부르면 **이전 작업의
  작업 추적 로그가 사라진다.** 지금은 보존됐다.
- 완화: `init` 은 CLAUDE.md 트리거 표상 **전체 경로 착수 시 1회**만 부르는 명령이고,
  `.claude/dashboard.html` 은 git 미추적 임시 산출물이다. 보고에 "이전 내용은 남지 않습니다"를
  한 줄 넣어 사고를 예방한다.

**대안 B — 세션 구분 UI 만 제거하고 로그는 계속 누적 보존한다.**

「이미 존재하면」 분기를 남기되 a~d(세션 번호·구분선·탭)만 지우고 e(상태 갱신)와 "9단계 건너뛰기"는
유지하는 안이다. 로그가 사라지지 않는다는 것이 유일한 장점이며, 대가가 셋이다. (1) "메인 세션마다
**항상 새로 발행**"이라는 요청 문구를 문자 그대로 만족하지 못한다 — 화면이 이전 작업에서 이어진다.
(2) 재사용 분기의 복잡성(grep 앵커 10종, 템플릿 세대 확인, impl 목록 초기화, "여기서 종료")이
전부 남는다. (3) **세션 구분 수단이 사라진 채 로그만 섞인다** — 서로 다른 작업의 항목이 시간순으로
뒤엉키는데, 그걸 풀려고 만든 것이 바로 지금 제거하려는 세션 탭이다. 즉 대안 B 는 세션 탭이 원래
풀던 문제를 되살린다.

> 참고로 검토했다가 기각한 절충안(대안 C): 로그를 보존하되 무명 구분선(`<hr>`) 하나만 남긴다.
> "세션 구분 UI 를 모두 제거"라는 요청과 정면으로 충돌하고, 결국 세션 개념을 반쯤 남긴다.

**→ 권장: A.** 사용자가 B 를 고르면 이 PRP 의 요구 2 절만 교체하면 되고, 요구 1·3·4 는 영향이 없다.

---

## 스코프와 비목표

### 스코프 안

- `commands/dashboard.md` 의 데이터 모델 표·불변식·폴링 계약·`init`/`step`/`log` 절차·자동 발행
  절차·「로그 UI 규격」 절·템플릿 전문(HTML/CSS/JS) 편집.
- `tests/run.sh` T22 하위 검증의 삭제·역전·신설.
- `README.md` 의 줄 수와 설명 문단 2곳.

### 비목표 (명시적으로 만들지 않는 것)

| 제외 항목 | 사유 |
|-----------|------|
| `.claude/dashboard.html` 자체의 수정·마이그레이션 | git 미추적 임시 산출물이다. 다음 `init` 이 새 템플릿으로 재생성한다 |
| 옛 세대 대시보드 호환 처리 | 권장안 A 는 항상 새로 쓴다 — **호환 개념 자체가 소멸**한다. 옛 파일용 폴백 문구도 함께 지운다 |
| 세션 구분을 대체할 새 UI(작업 이력, 타임라인 등) | 요청 범위 밖이다. 필요해지면 그때 만든다(YAGNI) |
| 「지금」 카드에서의 `.step-detail` 재노출 | `dashboard-current-next-card.md` 확정 사항 3. 활성 단계 줄에 이미 보이고 PiP 압축 뷰에서도 보인다 — 중복이다 |
| 브라우저 탭 중복 방지(같은 URL 탭 재사용) | 브라우저마다 동작이 다르고 제어 수단이 없다. `init` 은 작업당 1회라 실害가 낮다(리스크 3) |
| 새 하위 명령·새 인자·새 색 토큰·새 의존성 | 하나도 만들지 않는다 |
| `docs/prps/dashboard-*.md` 기존 문서 수정 | 과거 이력이다. 이 PRP 가 이어 붙는다 |
| `~/Desktop/dashboard.html` 원본 경로 언급(L1078) | 다섯 라운드 연속 "언급만 하고 지우지 않는다"로 처리됐다. 이번에도 동일 |

---

## UX 전환

### Before

```
┌─ 세션 진행 상황 ────────────────────────────────────┐
│ 대시보드 UI 정리                                     │
│ 설계 → 구현 → 검수 · 전체 경로            ← 부제(제거) │
│ ▇▇▇▇░░░░  2/5 · 40%                                 │
│ 1 설계                                     [완료]    │
│ 3 구현                                   [진행중]    │
│     implementer 디스패치 중                          │
│ 4 검수                                     [대기]    │
└─────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────┐
│ 지금  3. 구현      42분 경과 · 마지막 기록 6분 전     │ ← 「3. 구현」이 위 목록과 중복
│       다음 → 4. 검수                                 │ ← 38px 들여쓰기(step-detail 상속) → 어긋남
└─────────────────────────────────────────────────────┘
┌─ 작업 추적 ─────────────────────────────────────────┐
│ [모든 세션][세션 2][세션 1]        ← 세션 탭(제거)    │
│ [전체][구현][검수]                                   │
│ ── 세션 2 시작 · 14:03 ──          ← 구분선(제거)     │
└─────────────────────────────────────────────────────┘
```

### After

```
┌─ 세션 진행 상황 ────────────────────────────────────┐
│ 대시보드 UI 정리                                     │
│ ▇▇▇▇░░░░  2/5 · 40%                                 │
│ 1 설계                                     [완료]    │
│ 3 구현                                   [진행중]    │
│     implementer 디스패치 중                          │
│ 4 검수                                     [대기]    │
└─────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────┐
│ 지금                                                 │
│ 경과          42분                                   │ ← 라벨 열 고정폭 → 값이 세로로 정렬
│ 마지막 기록    6분 전                                 │
│ 다음          4. 검수                                 │
└─────────────────────────────────────────────────────┘
┌─ 작업 추적 ─────────────────────────────────────────┐
│ [전체][구현][검수]                                   │
└─────────────────────────────────────────────────────┘
```

### 상호작용 변화

| 접점 | Before | After | 비고 |
|------|--------|-------|------|
| `/dashboard init "…" "A\|B\|C"` (첫 호출) | 템플릿 Write 후 값 채움 | **동일** | 인자 무변경 |
| `/dashboard init` (파일이 이미 있을 때) | 로그 보존 + 세션 탭 추가 + 상태만 새로 그림 | **파일을 지우고 처음부터 새로 만든다** | **관측 가능한 동작 변경** |
| 브라우저 열기 | 도구 또는 `open`/`xdg-open` 으로 navigate | **동일 + 도구 경로에서 탭 활성화 1회** | OS 경로는 원래 포커스된다 |
| `step` / `impl` / `log` | — | **완전히 동일** | 인자·절차 한 글자도 안 바뀐다 |
| `log` 항목 | `data-session` 부여 | **부여하지 않는다** | `data-seq`·`data-kind` 는 그대로 |
| 로그 필터 | 유형 3개 + 세션 N개 | **유형 3개만** | `dzf` 라디오 무변경 |
| 부제 | `설계 → 구현 → 검수 · 전체 경로` | **없음** | 그룹명도 함께 사라진다 |
| 「지금」 카드 | 단계명 + 합쳐진 경과 문자열 + `다음 →` | **라벨-값 3행** | 새 인자 0개 |
| PiP 압축 뷰 | 로그 카드만 숨김 | **동일** | `body.dz-pip .sub` 만 삭제 |
| 폴링 | 세션 탭 개수 변화 시 전체 리로드 | **전체 리로드 경로 없음** | 부분 치환만 남는다 |

---

## 영향 범위

### 수정 파일 (3개)

| 파일 | 수정 내용 | 이유 |
|------|----------|------|
| `commands/dashboard.md` | 아래 [정확한 편집 지도](#정확한-편집-지도-commandsdashboardmd) 참조 | 이 커맨드가 유일한 실행 주체다. 절차·규약·템플릿이 전부 이 한 파일에 있다 |
| `tests/run.sh` | T22 하위 검증 **7개 삭제 · 4개 수정 · 5개 신설**, 선행 주석(L998~999)·`test_desc`(L1002)의 `T22-1~T22-90` → `T22-1~T22-95` | 이 저장소의 회귀 방지 수단은 grep 기반 문서 정합성 테스트뿐이다. `total_tests=22` 는 **유지**한다(하위 검증만 늘고 준다) |
| `README.md` | L120 줄 수 `1512` → 실제 `wc -l` 값 · L144 「지금」 카드 문장 수정 · L139~141 에 "`init` 은 매번 새로 발행한다" 취지 **한 절** 추가 | 줄 수는 사실이어야 하고, 재발행 동작은 사용자 가시 변화다 |

### 정확한 편집 지도 (`commands/dashboard.md`)

기준 줄 번호는 **편집 전** 파일 기준이다. 위에서부터 순서대로 편집하면 번호가 밀리므로,
**아래에서 위로** 편집하거나 매번 grep 으로 다시 찾는다.

| 구역 | 줄 | 조치 |
|------|-----|------|
| 동적 셀렉터 표 제목 | 74 | `— 7 셀렉터` → `— 6 셀렉터` |
| 부제 행 | 79 | **삭제** |
| `#dz-log` 행 | 84 | `data-current-session` 속성·값 설명 제거. `data-server-port` 만 남긴다 |
| 분기 각주 | 87 | "셀렉터 개수는 여전히 7이며" → `6` |
| 세션 번호 문단 | 106~109 | **문단 전체 삭제** |
| 구현 슬롯 절 머리말 | 118 | `「동적(치환 대상) — 7 셀렉터」` → `6 셀렉터` |
| 로그 항목 스키마 | 149 | `data-session="2"` **삭제** |
| `data-session` 설명 | 165~166 | **삭제** |
| 정적 요소 표(지금 카드) | 208~210 | `#dz-now-phase` 행 삭제, `#dz-now-elapsed`·`#dz-now-next` 값 설명 수정, `#dz-now-since` 행 신설 |
| 폴링 계약 머리말 | 230 | `7 셀렉터` → `6 셀렉터` |
| 폴링 계약 1행 | 234 | `#dz-subtitle` 제거 |
| 폴링 계약 `#dz-log` 행 | 237 | `data-current-session·` 제거 |
| 폴링 계약 라디오 행 | 238 | `input[name="dzs"]`·`label[for^="dz…"]` → `input[name="dzf"]`·`label[for^="dzf-"]`. 근거에서 "세션 탭"·"개수가 달라지면(=새 세션) 전체 리로드" **삭제**(유형 필터는 템플릿 고정 3개라 개수가 변하지 않는다) |
| 그룹 1개 각주 | 296~297 | "잃어버릴 뻔한 그룹명은 `#dz-subtitle` 앞머리에 들어간다" → "그룹명은 화면에 표시되지 않는다(선형 렌더링에는 행 머리가 없다)" |
| 매트릭스 마크업 헤딩 | 309 | `(init 6단계가 생성)` → `(init 5단계가 생성)`. **앵커가 바뀌므로 L494 링크도 함께 고친다** |
| `init` 절차 | 371~505 | **전면 재작성** — 아래 [새 `init` 절차](#새-init-절차-초안) 참조 |
| 세션 탭 갱신 하위 절 | 507~542 | **절 전체 삭제** |
| `step` 절 서두 인용 | 546~548 | "세션 2 가 기존 대시보드를 이어받는 경우(단계명을 모른다)" → 세션 언급 없는 근거로 교체(예: "칸이 16개로 늘면 줄 전문을 기억한다는 가정이 자주 깨진다"). **grep 앵커 방식 자체는 유지** |
| `log` 0-a | 702~705 | `data-current-session="{C}"` 판정 문장 삭제. **부분일치 grep 의 근거를 `data-server-port` 로 바꿔 유지한다**(완전일치 금지 규칙은 그대로) |
| `log` 2단계 | 721 | `data-session="C"` **삭제** |
| 포트 각인 2번 | 911 | "`data-current-session` 앞으로 끼워 넣지 않는다" → 그 문장만 삭제(삽입 위치는 `>` 바로 앞 그대로) |
| 자동 발행 머리말 | 776 | "`init` 의 **두 분기 모두**(신규 생성 후 10단계 / 기존 파일 조기 종료)" → "`init` 의 마지막 단계(9단계)" |
| 자동 발행 4번 | 865~884 | 경로 1에 **탭 활성화 지시** 추가, 경로 2에 `open -g` 금지 한 줄 추가 — [브라우저 열기 개정안](#브라우저-열기-개정안-자동-발행-4번) 참조 |
| 로그 UI 규격 · 필터 표 | 1038~1041 | 세션 탭 행 **삭제**(표가 1행이 되면 표를 없애고 산문 한 줄로 바꿔도 좋다) |
| 로그 UI 규격 · 예시 HTML | 1043~1054 | `dzs` 라디오 3줄·주석·`<br>` 삭제, `#dz-log` 속성 정리 |
| 로그 UI 규격 · 예시 CSS | 1056~1071 | 세션 탭 블록(1062~1070) **삭제** |
| 로그 UI 규격 · 산문 | 1073~1076 | "유형 필터는 알약, 세션 탭은 밑줄 탭…`<br>` 로 고정" → 유형 필터만 서술 |
| 「이 구조가 성립하는 이유」 | 1080~1096 | 세션 관련 3개 불릿 삭제. **래퍼 `div` 금지 불릿만 남긴다**(유형 필터에도 그대로 유효한 제약). 헤딩은 없애고 필터 절 산문으로 흡수 |
| 「탭이 많아지면」 | 1098~1106 | **절 전체 삭제** |
| 「세션 구분」 | 1108~1120 | **절 전체 삭제** |
| 템플릿 헤더 주석 | 1137 · 1146 · 1161 | 부제 줄 삭제 / `data-current-session` 언급 삭제 / 「지금」 카드 줄의 "세 요소" 표현 갱신 |
| 템플릿 CSS | 1172 · 1189~1194 · 1216~1220 · 1230~1231 · 1232 · 1241 | `.sub` 삭제 / 「지금」 카드 규칙 교체 / `dzs` 4줄+마커 삭제 / `.session-head` 2줄 삭제 / `.log-title,.impl-title` 에 `.now-title` 추가 / `body.dz-pip .sub` 삭제 |
| 템플릿 body | 1252 · 1258~1261 · 1273 | 부제 `<div>` 삭제 / 「지금」 카드 마크업 교체 / `#dz-log` 여는 태그에서 `data-current-session="1"` 삭제 |
| 템플릿 script | 1307 · 1313~1315 · 1337~1358 · 1396~1399 · 1433~1441 · 1442 · 1499 | `reloadPending` 삭제 / 카드 변수 교체 / `elapsedLabel` 삭제 + `renderNowCard` 재작성 / `sessionTabsChanged` 삭제 / `apply()` 의 세션 탭 리로드 블록·주석 삭제 / `syncText(fresh,'dz-subtitle')` 삭제 / `pagehide` 의 `if(reloadPending)…` → `poll();` |

### 미영향 (전수 확인)

| 대상 | 근거 | 결과 |
|------|------|------|
| 불변식 1~6, 8 | 세션·부제·「지금」 카드와 무관한 규칙이다 | **번호·문구 그대로** |
| 불변식 7(「지금」 카드는 파생 뷰) | 카드 마크업이 바뀌어도 **쓰는 주체는 여전히 `renderNowCard()` 하나**다 | **유지** |
| `data-server-port` 각인(포트 스캔·REUSE) | 세션 탭과 무관한 별개 개념. `#dz-log` 여는 태그의 같은 자리에 그대로 남는다 | **손대지 않는다** |
| `log commit` 자동 종료(`sleep 6 && pkill`) | 각인이 유지되므로 경로 무손상 | 유지 |
| `log` 0-a 의 부분일치 grep | `data-server-port` 가 여전히 붙으므로 완전일치 금지 근거가 살아 있다 | 유지 (T22-12) |
| `step` · `impl` 절차 | 인자·grep 앵커·전이 산술 무변경 | 유지 |
| 진행 시각화(선형/매트릭스) 분기 | `init` 절차 번호만 6 → 5 로 밀린다 | 로직 무변경 |
| `serve` · `on`/`off` 절차 | 브라우저 열기 하위 절차를 공유해 **개선을 자동으로 물려받는다**(그 외 무변경) | 유지 |
| `install.sh` `COMMANDS_FILE_COUNT` | 새 커맨드 파일 없음 | 변경 불필요 |
| 프로젝트·전역 `CLAUDE.md` | 트리거 표(`init`/`step`/`log` 호출 형태)가 바뀌지 않는다 | **수정하지 않는다** |
| T23(설치·on/off 문서 정합성) | `argument-hint` 를 바꾸지 않는다 | 계속 통과 |

---

## 데이터 모델

### 동적(치환 대상) — **6 셀렉터** (기존 7에서 `#dz-subtitle` 1행 삭제)

| 셀렉터 | 치환 대상 | 값 |
|--------|----------|-----|
| `#dz-title` | 텍스트 | 세션 제목 |
| `#dz-progress-bar` | inline `style="width:N%"` | 완료 단계 / 전체 단계 |
| `#dz-progress-pct` | 텍스트 | `3/6 · 50%` |
| `#dz-step-{n}` (그룹 1개) | `class` + `data-started-at` + `.chip` + `.step-detail` | (무변경) |
| `#dz-cell-{g}-{p}` (그룹 2개 이상) | `data-state` + 칸 텍스트 | (무변경) |
| `#dz-log` | 자식 `<li>` **prepend** + **`data-server-port` 속성** | 로그 항목(최신이 위) / 서빙 중인 로컬 서버 포트 |
| `#dz-updated` | 텍스트 | 갱신 시각 |

> 표는 7행처럼 보이지만 `#dz-step-*` 과 `#dz-cell-*` 은 불변식 1에 의해 **한 파일에 하나만**
> 존재한다 — 기존 문서가 "셀렉터 개수 7"을 세던 방식(두 행을 하나로 계산)을 그대로 이어
> **6** 으로 표기한다.

### 로그 항목 스키마 (변경 후)

```html
<li class="entry" data-kind="review" data-seq="12">
  <details open>
    <summary>
      <span class="time">17:07</span>
      <span class="badge round">R2</span>
      <span class="badge fail">검수 FAIL</span>
      <span class="lead">…</span>
    </summary>
    <div class="detail">…</div>
  </details>
</li>
```

`data-session` 이 사라진다. `data-seq`·`data-kind` 의 역할과 필터 대응표는 **무변경**이다.

### 「지금」 카드 정적 요소 (변경 후)

| 요소 | 역할 |
|------|------|
| `#dz-now-card` | 「지금」 카드 `<div>`. 템플릿에 **항상** 존재하며, 진행중 단계가 없으면 CSS 가 통째로 숨긴다. DOM 에서 제거하지 않는다 |
| `#dz-now-elapsed` | 현재 단계 경과 시간(`42분`). **스크립트만** 쓴다 |
| `#dz-now-since` | 마지막 기록 이후 경과(`6분 전`). **스크립트만** 쓴다 |
| `#dz-now-next` | 다음 단계 `번호. 이름`(`4. 검수`). **스크립트만** 쓴다 |

- `#dz-now-phase` 는 **삭제**된다(단계 번호·이름은 바로 위 목록에 이미 있다).
- 세 값 중 어느 하나가 비면 `.now-row:has(.now-value:empty)` 가 **그 행 전체**를 숨긴다 —
  라벨만 홀로 남는 상태가 생기지 않는다.

### 불변식 번호 — **재번호하지 않는다**

기존 불변식 1~8을 전수 확인한 결과 **세션 탭 전용 불변식은 없다.** 따라서 삭제되는 항목이 없고,
번호를 당길 이유도 없다. 1~8 을 **그대로 유지**한다(문서 곳곳과 테스트 주석이 번호로 서로를
참조하므로, 근거 없는 재번호는 순수 비용이다).

| # | 요지 | 이번 변경의 영향 |
|---|------|-----------------|
| 1 | `#dz-step-*` ↔ `#dz-cell-*` 비공존 | 없음 |
| 2 | `<li>`·`<td>` 한 줄에 하나 | 없음 |
| 3 | `.step-detail` 줄바꿈 금지 | 없음 |
| 4 | `#dz-log-card` 는 CSS 로만 숨김 | 없음 |
| 5 | `<li id="dz-impl-…">` 한 줄에 하나 | 없음 |
| 6 | 세부 진행률이 매크로 진행률을 안 바꾼다 | 없음 |
| 7 | 「지금」 카드는 파생 뷰 | **문구 유지**(마크업만 바뀐다) |
| 8 | `data-server-port` 는 오케스트레이터 전용 | 없음 |

---

## 인터페이스 — 호출 규약

**파라미터는 한 글자도 바뀌지 않는다.** 바뀌는 것은 `init` 의 **관측 가능한 동작**이다.

```
/dashboard init "<제목>" "<단계1|단계2|...>"
/dashboard init "<제목>" "<그룹A:단계1,단계2|그룹B:단계1,단계2>"
```

호출 규약 절과 「CLAUDE.md 트리거」 표 아래에 아래 취지의 한 줄을 **명시적으로 추가**한다.

> **`init` 은 기존 `.claude/dashboard.html` 을 보존하지 않는다.** 호출될 때마다 파일을 지우고
> 처음부터 새로 만든다 — 이전 작업의 진행 상태도, 작업 추적 로그도 남지 않는다.
> 전체 경로 착수 시 **1회만** 부른다.

`step`·`impl`·`log`·`serve`·`on`/`off` 의 규약과 동작은 무변경이다.

---

## 새 `init` 절차 (초안)

기존 371~505 를 아래로 교체한다. **분기가 없다.**

````markdown
## `init` — 메인 세션이 수행할 절차

> **`init` 은 언제나 대시보드를 처음부터 새로 만든다.** 기존 `.claude/dashboard.html` 이 있으면
> 로그를 포함해 전부 버리고 이번 인자로 새로 그린다. 오케스트레이터에게는 "새 메인 세션인지 같은
> 세션의 다음 작업인지"를 구분할 수단이 없고 — `init` 호출 자체가 유일한 신호다 — 이 파일은
> 기능 단위로 만들고 끝나면 지우는 임시 산출물이다. 그래서 존재 여부로 분기하지 않는다.

1. **자리를 비우고 템플릿을 쓴다.** 먼저 Bash 1회:
   ```bash
   mkdir -p .claude && rm -f .claude/dashboard.html
   ```
   `mkdir -p` 도 `rm -f` 도 대상이 없을 때 조용히 성공하므로 존재 확인이 필요 없다.
   **이 제거를 생략하지 않는다** — Write 도구는 이 세션에서 읽지 않은 기존 파일을 덮어쓰지 못한다.
   먼저 지우면 Write 는 언제나 "새 파일 생성"이 되어 절차가 결정적이다.

   이어서 [템플릿 전문](#템플릿-전문)을 **한 글자도 고치지 않고** 그대로 `.claude/dashboard.html`
   로 Write 한다. 값 채우기는 2번부터의 Edit 이 한다.

   > 아래 모든 Edit 의 `old_string` 은 **방금 쓴 템플릿의 해당 줄 전문 그대로**다. 파일 내용을
   > 우리가 직접 썼으므로 grep 으로 앵커를 찾을 필요가 없다.

2. `#dz-title` 텍스트를 `<제목>` 인자로 치환한다.

3. **인자 파싱 — 그룹 목록으로 정규화한다.** (기존 3단계 그대로)

4. **열 목록과 전체 칸 수를 확정한다.** (기존 4단계 그대로)

5. **진행 시각화를 렌더링한다 — 분기 기준은 그룹 수 하나뿐이다.** (기존 6단계 그대로)

6. `#dz-progress-bar` 의 `style` 을 `width:0%` 로, `#dz-progress-pct` 텍스트를 `0/N · 0%` 로 치환한다.

7. `#dz-updated` 를 현재 시각(예: `2026-08-12 17:02`)으로 치환한다.

8. `#dz-log` 는 템플릿 그대로 빈 목록
   (`<ul class="log" id="dz-log" data-server-port=""></ul>`)으로 둔다 — **Edit 하지 않는다.**

9. 아래 [자동 발행](#자동-발행--init-의-공통-하위-절차) 절차를 수행한다.
   (기존의 `file://` 안내는 자동 발행이 서버를 띄우지 못했을 때의 **폴백 티어**로 그 안에 살아 있다.)

10. **보고**에 "대시보드를 새로 발행했습니다 — 이전 대시보드의 내용(작업 추적 로그 포함)은
    남지 않습니다"를 한 줄 포함한다.
````

**절차 번호 이동에 따른 상호 참조 수정 3곳** (놓치면 문서가 자기 자신을 잘못 가리킨다):

1. L309 헤딩 `### 매트릭스 마크업 (`init` 6단계가 생성)` → `5단계가 생성`
2. L494 링크 `[매트릭스 마크업](#매트릭스-마크업-init-6단계가-생성)` → `…-5단계가-생성`
3. L491 `8단계에서 #dz-updated 에 쓰는 것과 같은 문자열` → `7단계에서`

---

## 「지금」 카드 재설계 (초안)

### 왜 지금 카드가 중복으로 느껴지는가

현재 카드의 첫 줄은 `지금 3. 구현 · 42분 경과 · 마지막 기록 6분 전`, 둘째 줄은 `다음 → 4. 검수` 다.
이 중 **`3. 구현` 과 `4. 검수` 는 바로 위 `ol#dz-steps` 의 `<li>` 에 번호·이름·상태 칩과 함께 이미
있다.** 카드에만 있는 값은 `42분 경과` 와 `마지막 기록 6분 전` 둘뿐이며, 그 둘이 중복된 이름들
사이에 끼어 눈에 띄지 않는다.

정렬이 어긋나 보이는 원인도 마크업에 그대로 있다.

- `.now-line{display:flex;align-items:baseline}` 에 폰트 크기가 11px(라벨) · 16px(단계명) ·
  12px(경과)로 제각각인 요소가 섞여 있고, 경과만 `margin-left:auto` 로 오른쪽 끝에 밀려 있다.
- 둘째 줄 `#dz-now-next` 가 **`class="step-detail"` 을 재사용**해 `margin-left:38px` 을 상속받는다.
  38px 은 단계 목록의 번호 배지(26px)+간격(12px)에 맞춘 값이라 이 카드 안에서는 아무 의미가 없다 —
  첫 줄은 왼쪽 끝, 둘째 줄은 38px 들여쓰기가 되는 이유가 이것이다.

### 새 마크업 (템플릿 body 1258~1261 교체)

```html
  <div class="card" id="dz-now-card">
    <div class="now-title">지금</div>
    <div class="now-row"><span class="now-label">경과</span><span class="now-value" id="dz-now-elapsed"></span></div>
    <div class="now-row"><span class="now-label">마지막 기록</span><span class="now-value" id="dz-now-since"></span></div>
    <div class="now-row"><span class="now-label">다음</span><span class="now-value" id="dz-now-next"></span></div>
  </div>
```

- 카드 제목 `지금` 은 아래 두 카드(`구현 세부 작업`·`작업 추적`)와 **같은 형태**다 —
  기존 규칙 `.log-title,.impl-title` 에 `.now-title` 을 **덧붙이기만** 한다(새 규칙 0줄).
- 값에 접두어를 넣지 않는다: `42분 경과` → `42분`, `다음 → 4. 검수` → `4. 검수`.
  라벨이 이미 그 뜻을 말하므로 문자열이 짧아지고, 라벨 열의 폭이 값의 정렬을 결정한다.

### 새 CSS (템플릿 `<style>` 1189~1194 교체, 6줄 → 6줄)

```css
  .now-row{display:flex;align-items:baseline;gap:10px;padding:3px 0}
  .now-label{flex:none;width:78px;font-size:11px;font-weight:800;letter-spacing:.3px;color:var(--muted)}
  .now-value{font-size:13.5px;font-weight:700;color:var(--navy);overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
  #dz-now-elapsed{color:var(--blue)}
  .now-row:has(.now-value:empty){display:none}
  .wrap:not(:has(#dz-steps li.active)) #dz-now-card{display:none}
```

그리고 1232 줄을 `.log-title,.impl-title,.now-title{…}` 로 확장한다.

**정렬이 실제로 고쳐지는 지점**

| 문제 | 해법 |
|------|------|
| 라벨·값의 폰트 크기가 제각각이라 baseline 이 들쭉날쭉 | 값은 **전부 13.5px** 로 통일하고, 라벨만 11px 캡션. 행마다 독립적으로 baseline 이 맞는다 |
| 둘째 줄만 38px 들여쓰기 | `.step-detail` 재사용을 끊고 `.now-value` 로 교체 |
| 값의 시작 x 좌표가 행마다 다름 | `.now-label{flex:none;width:78px}` — **고정폭 라벨 열**이 값의 왼쪽 정렬선을 만든다 |
| 경과가 오른쪽 끝에 홀로 떠 있음 | `margin-left:auto` 제거. 값은 전부 같은 열에서 시작한다 |
| 값이 길면 줄바꿈돼 행 높이가 흔들림 | `.now-value` 에 `nowrap`+말줄임(`overflow:hidden` 이 flex 자동 최소 크기를 0으로 만들어 실제로 축약된다) |

> `width:78px` 은 매직 넘버지만 **입력이 고정**이라 안전하다 — 라벨 3종(`경과`·`마지막 기록`·`다음`)은
> 템플릿 상수이지 사용자 데이터가 아니다. 가장 긴 `마지막 기록`(11px/800/자간 .3px)이 약 70px 이라
> 8px 여유를 둔 값이다. 라벨 문구를 바꿀 때만 이 값을 함께 본다.

### 새 스크립트 (템플릿 `<script>`)

변수 선언(1313~1315) 교체:

```js
  var nowElapsed = document.getElementById('dz-now-elapsed');
  var nowSince = document.getElementById('dz-now-since');
  var nowNext = document.getElementById('dz-now-next');
```

`elapsedLabel()`(1337~1348)을 **삭제**하고 `renderNowCard()`(1349~1358)를 아래로 교체한다.
`parseStamp()`·`formatDuration()`·`phaseLabel()` 은 **그대로 둔다**(`phaseLabel` 은 다음 단계 이름에
계속 쓰인다).

```js
  function renderNowCard(){
    // 매트릭스 세션이거나 진행중 단계가 없으면 그릴 것이 없다 — 카드는 CSS 가 숨긴다.
    var active = wrap.querySelector('#dz-steps li.active');
    if(!active) return;
    var now = Date.now();
    var startedAt = parseStamp(active.getAttribute('data-started-at'));
    var updatedAt = parseStamp(wrap.querySelector('#dz-updated').textContent);
    // formatDuration 은 손상·미래 타임스탬프(시계 되감김 등)에 빈 문자열을 반환한다. 값이 비면
    // .now-row:has(.now-value:empty) 가 그 행을 통째로 숨겨 라벨만 남는 상태가 생기지 않는다.
    nowElapsed.textContent = startedAt ? formatDuration(startedAt, now) : '';
    var since = updatedAt ? formatDuration(updatedAt, now) : '';
    nowSince.textContent = since ? since + ' 전' : '';
    nowNext.textContent = active.nextElementSibling ? phaseLabel(active.nextElementSibling) : '';
  }
```

- **`since` 를 한 번 변수에 받는 이유**: 빈 문자열에 `' 전'` 을 붙이면 `" 전"` 만 남아 `:empty` 가
  풀리고 라벨과 함께 의미 없는 행이 나타난다. 기존 `elapsedLabel()` 이 조각 단위로 막던 실패를
  같은 방식으로 유지한다.
- 호출 지점(`renderNowCard(); setInterval(renderNowCard, NOW_TICK_MS);` 와 `apply()` 말미)은
  **위치·순서 모두 무변경**이다 — `if(!isServed)` 조기 반환보다 **앞**, `resizePipToFit()` **직전**.
- `#dz-now-*` 접두어를 유지하므로 `dz-current` 계열 부활을 막는 역방향 테스트(T22-61)를 건드리지 않는다.

---

## 브라우저 열기 개정안 (자동 발행 4번)

### 진단 — 이미 있는 것과 진짜 빠진 것

| 경로 | 현재 | 판정 |
|------|------|------|
| ① 세션에 이미 있는 URL 열기 도구(브라우저 pane·MCP 등) | "그것을 쓴다"까지만 지시. **탭을 전면으로 올리라는 지시가 없다** | **여기가 빠진 부분** — navigate 만 하고 사용자가 보던 탭이 그대로인 경우가 생긴다 |
| ② OS 기본 열기(`open` / `xdg-open`) | 옵션 없이 URL 만 넘긴다 | **이미 포커스된다.** macOS `open` 은 대상 앱을 전면으로 올리는 것이 기본이고, `xdg-open` 도 데스크톱 환경의 기본 핸들러가 창을 띄운다 → **무변경** |
| ③ 포기(URL 출력) | — | 무변경 |
| 발행 시점 | 자동 발행은 `init` 에서만 불린다 | **"초기 발행 시"라는 요구를 이미 만족** |

### 개정 문안 (경로 1·2)

````markdown
1. **이 세션에 URL 을 여는 도구가 이미 있으면 그것을 쓴다.** 브라우저 pane, 브라우저 MCP 서버 등
   **지금 사용 가능한 도구 목록에 실제로 존재하는** 것만 해당한다. **없다고 새로 설치하거나
   설정하지 않는다.**
   - 그 도구가 탭 목록 조회·탭 선택(select/activate/focus 계열) 도구를 **함께 제공하면**,
     navigate 직후 방금 연 탭을 **전면으로 올리는 호출을 한 번만** 더 한다. navigate 만으로는
     탭이 백그라운드에 열려 사용자가 보던 화면이 그대로일 수 있다.
   - 그런 도구가 **없으면 navigate 로 끝낸다.** 없는 기능을 다른 수단으로 흉내 내지 않는다
     (「없으면 설치하지 않는다」와 같은 원칙이다).
   - 두 호출 모두 **한 번씩만** 하고, 실패는 무시하고 5번으로 간다.
2. **없으면 OS 기본 열기 명령을 시도한다.** … (조건 3개 무변경)
   - `open`/`xdg-open` 은 **기본이 곧 포커스**다. macOS 에서 `-g`(백그라운드) 옵션을 **붙이지
     않는다** — 붙이면 탭은 열리지만 창이 뒤에 남아 이번 요구가 무너진다.
````

`serve` 5단계는 이 절차를 그대로 호출하므로 **수동 경로도 같은 개선을 물려받는다**(추가 편집 없음).

---

## 설계 결정과 근거

### 1. 존재 분기를 없앤 것이 이번 변경의 **단순화 본체**다

세션 탭 제거는 UI 삭제로 보이지만, 실제로 가장 크게 줄어드는 것은 `init` 이다. 「이미 존재하면」
분기는 (a) 세션 번호 확정, (b) 태그 치환, (c) 구분선 prepend, (d) 탭·CSS 규칙 주입, (e) 옛 파일을
전제한 grep 앵커 10종 + 템플릿 세대 확인 + impl 목록 초기화로 이루어져 있었고, 그 **전부의 존재
이유가 "로그를 보존하면서 세션을 구분한다"** 하나였다. 구분을 없애면 보존의 값도 사라지고, 보존을
없애면 분기가 사라진다. 남는 절차는 "지우고 → 쓰고 → 채운다"뿐이다. 이것이 KISS/YAGNI 판단이다.

### 2. `rm -f` 후 Write — 도구 제약을 **정면으로** 다룬다

Write 도구는 세션에서 읽지 않은 기존 파일을 덮어쓰지 못한다. 대안 두 가지를 검토했다.
① 파일을 먼저 Read 한 뒤 Write — 로그가 쌓인 파일을 통째로 읽는 가변 비용이 생기고, `log` 절차가
"파일 전체를 Read하지 않는다"로 어렵게 없앤 비용을 `init` 이 되살린다. ② 임시 파일에 쓴 뒤 `mv` —
Bash 호출이 늘고 실패 경로가 둘이 된다. **`rm -f` 는 Bash 1회로 디렉토리 보장까지 겸하고, 낡은
바이트가 남지 않음을 보장**한다. 채택.

### 3. 「지금」 카드에서 단계 번호·이름을 **없앤다**(줄이지 않는다)

카드 바로 위에 활성 `<li>` 가 번호 배지·이름·`[진행중]` 칩·상세와 함께 있고, PiP 압축 뷰에서도
활성 단계는 배경색까지 입혀 강조된다. 즉 "지금 어느 단계인가"는 **어떤 화면에서도 이미 답이
나와 있다.** 이름을 반만 남기는 절충(예: 이름만, 번호 없이)은 중복을 반만 줄이면서 정렬 문제는
그대로 남긴다. `dashboard-current-next-card.md` 의 확정 사항 2("카드는 어디에도 없던 정보를 최소
하나 담아야 한다")를 만족시키는 값은 **경과 시간과 마지막 기록 이후 경과** 두 개이고, 이 둘이
카드의 전부가 되는 것이 그 확정 사항의 가장 순수한 형태다. `다음` 은 사용자가 명시적으로 유지를
요청해 한 행으로 남긴다.

### 4. 정렬은 **고정폭 라벨 열**로 푼다 — grid 를 쓰지 않는 이유

2열 grid(`grid-template-columns:max-content 1fr`)도 같은 정렬을 만들지만, **값이 빈 행을 통째로
숨기는 요구**와 충돌한다. grid 의 라벨·값은 형제라서 `:empty` 로 값만 숨기면 라벨이 홀로 남고,
행을 감싸면 `display:contents` + `:has()` 조합이 필요해 규칙이 늘어난다. 행마다 flex 를 쓰면
`.now-row:has(.now-value:empty){display:none}` **한 줄**로 끝난다. `:has()` 는 이미 템플릿에서
두 번 쓰이고 있어 새 의존성이 아니다.

### 5. 부제가 담던 정보는 **되살리지 않고 버린다**

`#dz-subtitle` 은 `단계 흐름 요약 · 작업 유형`(+그룹 문법일 때 그룹명)을 담았다. 단계 흐름은 바로
아래 단계 목록·매트릭스 헤더에 그대로 있고, 작업 유형은 사실상 상수다(`/dashboard` 는 전체 경로
전용이다). 그룹명은 그룹이 2개 이상이면 매트릭스 행 머리(`#dz-group-{g}`)에 나오고, 1개일 때만
표시처가 없어진다 — 그 한 경우를 위해 제거 대상인 요소를 살려 두거나 제목에 끼워 넣는 것은
"제거해 달라"는 요청과 충돌한다. **버린다.**

### 6. 폴링의 **전체 리로드 경로가 함께 사라진다**

`sessionTabsChanged()` 는 "세션마다 `<style>` 에 CSS 규칙이 추가되고 라디오가 늘어난다"는 전제
위에 있었다. 세션 규칙 주입이 없어지면 **모든 대시보드의 `<style>`·라디오 집합이 템플릿과
동일**해져 전제가 사라진다. 함께 지워지는 것은 `reloadPending` 플래그와 `pagehide` 의 조건 분기,
그리고 "플로팅 중이면 PiP 창을 강제로 닫고 리로드"라는 가장 파괴적인 경로다. 폴링은 부분 치환만
남아 더 조용해진다.

### 7. 브라우저 경로는 **한 줄만** 늘린다

"열리기는 하는데 포커스가 안 된다"는 문제를 위해 별도 하위 명령이나 재시도 루프를 만들지 않는다.
자동 발행 절의 확립된 원칙(한 번씩만 시도 · 실패는 무시 · 없는 수단은 만들지 않는다)을 그대로
따르며, 조건부 한 문장을 얹을 뿐이다. `open -g` 금지는 **역방향 가드**다 — 나중에 "조용히 열자"는
선의의 수정이 이번 요구를 되돌리는 것을 막는다.

### 8. 불변식 번호를 유지한다

세션 탭 전용 불변식이 애초에 없었으므로 재번호할 근거가 없다. 문서 내 상호 참조와 테스트 주석이
번호로 서로를 가리키고 있어, 근거 없는 재번호는 순수 비용이다.

---

## 테스트 계획

### 자동 검증 (`tests/run.sh`, T22)

`bash tests/run.sh` — `total_tests=22` 는 **유지**(하위 검증만 증감).
선행 주석(L998~999)과 `test_desc`(L1002)의 `T22-1~T22-90` → `T22-1~T22-95` 로 갱신.

**A. 삭제 (7개)** — 검증 대상 자체가 사라지므로 남겨 두면 반드시 실패한다.

| 항목 | 현재 검증 | 삭제 사유 |
|------|----------|----------|
| T22-6 | `sed -n '/이미 존재하면/,/존재하지 않으면/p'` 범위 + 로그 보존 문구 2종 | 재사용 분기 소멸. 범위 추출이 빈 값이 되어 **즉시 실패**한다 |
| T22-11 | `id="dz-log" data-current-session="1"` | 속성 제거. T22-80(수정본)이 새 형태를 검증 |
| T22-15 | 새 탭 `checked` 금지 문구 | 탭 소멸 |
| T22-16 | `<br>` 존재 | 세션 탭 줄 분리용이었다 |
| T22-17 | `not(#dzs-all) ~ #dz-log .session-head` | 규칙 소멸 |
| T22-88 | e 단계 grep 블록의 앵커 10종 | e 단계 소멸 |
| T22-89 · T22-90 | 재사용 분기의 `[자동 발행]`·`여기서 종료`·impl 초기화 지시 | 분기 소멸 (2개) |
| T22-13 | 로그 항목 스키마에 `data-session="2"` 포함 | `data-session` 속성 삭제로 필연적 실패. **구현 중 발견**(계획 당시 누락) — 신설되는 T22-91(역방향: `data-session=` 문자열 전무)이 이 검증을 상위 호환으로 흡수하므로 별도 수정 없이 삭제한다. 검수에서 grep 실측으로 커버리지 공백 없음을 확인했다 |

**B. 수정 (4개)**

| 항목 | 변경 |
|------|------|
| T22-2 | 필수 셀렉터 목록에서 `dz-subtitle` 제거 → **6종**(`dz-title` `dz-progress-bar` `dz-progress-pct` `dz-step-` `dz-log` `dz-updated`) |
| T22-8 · T22-14 · T22-18 | **정방향 → 역방향 전환**: `session-head`·`label[for^="dzs-"]`·`DZ:SESSION-RULES`·`#dzs-` 규칙이 **없어야** 통과. (셋을 T22-91 하나로 합쳐도 되나, 번호 이력 추적을 위해 자리에서 뒤집는 편을 권한다) |
| T22-59 | 「지금」 카드 id 4종을 `dz-now-card` · `dz-now-elapsed` · `dz-now-since` · `dz-now-next` 로 교체(`dz-now-phase` 제거) |
| T22-70 · T22-80 | T22-70: 근거 주석에서 "두 분기" 표현을 지우고 **init 절 범위에 `[자동 발행]` 링크 ≥1** 로 완화. T22-80: 앵커를 `id="dz-log" data-server-port=""` 로 교체(**포트 각인 슬롯 검증은 반드시 유지**) |

**C. 신설 (5개)** — 이번 요구가 되돌려지는 것을 막는 가드

| 항목 | 방향 | 검증 |
|------|------|------|
| **T22-91** | 역방향 | 세션 UI 잔재 전무: `data-current-session` · `data-session=` · `sessionTabsChanged` · `dzs-` 중 어느 문자열도 파일에 없다 |
| **T22-92** | 역방향+정방향 | 부제 잔재 전무: `dz-subtitle` · `class="sub"` 부재. 동적 셀렉터 표 제목이 `— 6 셀렉터` |
| **T22-93** | 정방향+역방향 | `init` 단일 분기: init 절 범위(`/^## \`init\`/,/^## \`step\`/`)에 `이미 존재하면` 이 **없고**, `rm -f .claude/dashboard.html` 과 "읽지 않은 기존 파일을 덮어쓰지 못한다" 취지 문구가 **있다** |
| **T22-94** | 정방향+역방향 | 브라우저 포커스: 자동 발행 4번 범위에 탭 활성화(전면) 지시가 있고, `open -g` 문자열이 파일에 없다 |
| **T22-95** | 정방향+역방향 | 「지금」 카드 정렬 계약: `.now-row:has(.now-value:empty){display:none}` 존재. 템플릿 body 범위에 `class="step-detail" id="dz-now-next"` **부재**(`.step-detail` 재사용 회귀 방지) |

**D. 계속 통과해야 하는 것(수정 금지)** — 회귀의 조기 경보다.
T22-12·41(로그 grep 앵커) · T22-38(`[id="dz-` 금지) · T22-44(`.step-detail` + `:empty`) ·
T22-47(PiP 리사이즈) · T22-49~53(impl 카드) · T22-60(카드 자동 숨김) · T22-61(`dz-current` 부활 금지) ·
T22-63~65·67(불변식 7·렌더러 배치·폴링 비치환·PiP 미숨김) · T22-71~87(자동 발행·포트 각인·log commit) ·
T23 전체.

### 수동 검증 시나리오

자동 검증은 **문서 정합성**까지만 본다. 아래는 실제 산출물과 런타임 동작을 사람 눈으로 확인하는
절차다. 임시 프로젝트(`mktemp -d`)에서 `install.sh --scope project` 후 수행한다.

| # | 시나리오 | 기대 결과 |
|---|---------|----------|
| 1 | 새 프로젝트에서 `/dashboard init "테스트" "설계\|구현\|검수"` | 서버 기동 → **브라우저 탭이 자동으로 열리고 그 탭이 전면으로 올라온다**(다른 창을 앞에 두고 실행해 확인) |
| 2 | 같은 화면 확인 | 제목 아래 **부제 줄이 없다**. 「지금」 카드에 단계 번호·이름이 없고 `경과 / 마지막 기록 / 다음` 세 행의 **값 시작 x 좌표가 같다** |
| 3 | 「작업 추적」 카드 | 필터 알약 `[전체][구현][검수]` **한 줄만** 있고 세션 탭·구분선이 전혀 없다 |
| 4 | `step 2 active "…"` → 5초 대기 | 폴링으로 단계·상세·「지금」 카드가 갱신된다. **화면 전체가 리로드되지 않는다** |
| 5 | 30초 이상 방치 | 「지금」 카드의 `경과`·`마지막 기록` 값이 30초 틱으로 증가한다 |
| 6 | `log impl "…"` 여러 건 | 항목이 최신순 prepend, 최신 3건만 펼침. HTML 에 `data-session` 이 없다 |
| 7 | **같은 세션에서 `/dashboard init` 재호출** | 도구 오류 없이 성공. 대시보드가 **완전히 새로 시작**(로그 0건, 진행률 0%)되고 탭 UI 가 전혀 없다. 서버는 `REUSE` 로 재기동하지 않으며 브라우저가 다시 열려 포커스된다 |
| 8 | 7 직후, **이전에 열려 있던 탭** | 폴링으로 새 내용을 받아 정상 갱신된다(무한 리로드·PiP 강제 종료 없음) |
| 9 | grep 확인 | `grep -c 'dz-subtitle\|dzs-\|session-head\|data-current-session\|data-session\|class="sub"' .claude/dashboard.html` → **0** |
| 10 | 그룹 문법 `"A:설계,구현\|B:설계,구현"` | 매트릭스 렌더, 「지금」 카드는 CSS 로 숨김, 그룹명이 행 머리에만 나온다 |
| 11 | `file://` 로 직접 열기 | 「지금」 카드가 30초 틱으로 갱신된다(렌더러가 `if(!isServed)` 조기 반환 **앞**에 있는지의 런타임 확인) |
| 12 | 「플로팅」 버튼 | PiP 창에서 카드 3행 정렬 유지, 로그 카드만 숨김, 창 높이에 빈 여백이 없다 |
| 13 | `data-started-at` 이 없는 상태(매트릭스→선형 등) | `경과` **행 전체**가 사라진다(라벨만 남지 않는다) |
| 14 | `log commit "…"` | 자동 종료 안내가 나오고 `data-server-port` 가 비워진다(포트 각인 경로 무손상) |
| 15 | `bash tests/run.sh` | T22·T23 포함 전체 통과 |

---

## 리스크와 대안

| # | 리스크 | 가능성 | 영향 | 완화 |
|---|--------|:---:|:---:|------|
| 1 | 같은 세션에서 `init` 재호출 시 **이전 로그 소실** | 중 | 중 | 요구 2의 의도된 트레이드오프. 보고 문구로 사전 고지. 되돌리려면 대안 B |
| 2 | `rm -f` 가 권한 프롬프트에 걸린다 | 중 | 낮 | 실패해도 `init` 은 중단되지 않게 문안을 쓰지 **않는다** — 이건 본체다. 대신 최초 1회 안내에 허용 목록 팁을 이미 넣고 있으므로 그 줄에 이 명령을 덧붙인다 |
| 3 | `init` 마다 브라우저 탭이 하나씩 쌓인다 | 중 | 낮 | `init` 은 작업당 1회다. 탭 재사용은 브라우저별로 제어 수단이 없어 비목표 |
| 4 | T22 수정 누락 → 테스트가 **조용히 무력화**(범위 추출 실패 시 `grep -q` 가 그냥 실패) | 중 | 높 | 기존 테스트가 쓰는 "범위가 비었는지 먼저 확인" 패턴을 신설 항목에도 그대로 적용. 구현 후 `bash tests/run.sh` 를 반드시 통과시킨다 |
| 5 | 절차 번호 이동(6→5, 8→7)의 상호 참조 누락 | 중 | 중 | 수정 3곳을 위에 명시. 구현 후 `grep -n '[0-9]단계' commands/dashboard.md` 로 재확인 |
| 6 | 열려 있던 **낡은 세대 탭**이 새 파일을 폴링해 낡은 스크립트로 그린다 | 낮 | 낮 | 기존에도 동일했다(폴링은 `<style>`·`<script>` 를 치환하지 않는다). `init` 이 새 탭을 열고 포커스하므로 사용자는 새 탭을 본다 |
| 7 | `docs/images/dashboard-sample.png` 이 낡은 화면(부제·옛 카드·세션 탭)이 된다 | 확실 | 낮 | 재촬영은 이번 범위 밖. 승인 항목 3에서 사용자에게 확인 |

### 검토했으나 채택하지 않은 대안

1. **세션 탭만 지우고 로그는 누적 보존(대안 B)** — 위 [트레이드오프](#요구-2의-트레이드오프--승인이-필요한-유일한-결정) 참조.
   요청 문구를 문자 그대로 만족하지 못하고, 재사용 분기의 복잡성이 전부 남으며, 세션 탭이 원래
   풀던 "서로 다른 작업의 로그가 뒤섞인다"는 문제를 되살린다.
2. **「지금」 카드를 2열 CSS grid 로 구성** — 정렬은 같은 수준으로 해결되지만, 값이 빈 행을 통째로
   숨기려면 `display:contents` 래퍼 + `:has()` 가 필요해 규칙이 늘어난다(설계 결정 4).
3. **부제를 없애는 대신 제목 줄에 병합**(`제목 — 설계 → 구현 → 검수`) — 제목은 사용자 인자이고,
   병합하면 `#dz-title` 치환 규칙이 인자 외의 값을 합성하게 된다. "제거"라는 요청과도 어긋난다.
4. **`init` 이 파일을 지우기 전에 사용자에게 확인을 받는다** — 착수 보고 턴에 왕복이 하나 늘고,
   임시 산출물을 지우는 데 확인을 요구하는 것은 과하다. 보고 한 줄로 갈음한다.

---

## 구현 순서 (제안)

각 단계 끝에서 `bash tests/run.sh` 를 돌려 **어떤 단계가 무엇을 깨는지** 좁힌다.

1. **요구 4 (부제 제거)** — 가장 국소적이다. 표·폴링 계약·`init` 5단계·템플릿 3곳·`syncText` 1줄.
   → T22-2 수정 + T22-92 신설.
2. **요구 3 (「지금」 카드)** — 템플릿 body/CSS/script + 정적 요소 표. 절차 문서는 건드리지 않는다.
   → T22-59 수정 + T22-95 신설. (T22-60·61·63~65·67 은 그대로 통과해야 한다.)
3. **요구 2 (세션 탭 + `init` 재작성)** — 가장 크다. 삭제 → `init` 재작성 → 상호 참조 3곳 →
   「로그 UI 규격」 정리 → 템플릿 CSS/body/script 순서로.
   → T22-6·11·15·16·17·88·89·90 삭제, T22-8·14·18·70·80 수정, T22-91·93 신설.
4. **요구 1 (브라우저 포커스)** — 자동 발행 4번 문안 2곳. → T22-94 신설.
5. **`README.md`** — `wc -l commands/dashboard.md` 로 줄 수 갱신, 설명 문단 2곳.
6. 전체 `bash tests/run.sh` 통과 + [수동 검증 시나리오](#수동-검증-시나리오) 1~15.

---

## 사용자 승인 — 확정 (2026-08-12)

1. **요구 2의 방향 → A 채택.** `init` 은 항상 완전히 새로 시작한다(이전 작업 로그 소실).
   승인 전 `feature/hub-dashboard`(`hub/bin/hub_parse.py`)를 조사해 허브가 `#dz-log` 를
   전혀 읽지 않고(파서 docstring: "로그 항목은 여러 줄에 걸쳐 있어 파싱하지 않는다"), 세션
   이력은 완전히 별도인 `~/.claude/hub/events/*.jsonl` 훅 이벤트 로그로 관리한다는 사실을
   확인했다 — **이번 변경은 허브 쪽 데이터 손실을 일으키지 않는다.** 허브가 읽는
   `Tier1Snapshot`(제목·부제·진행률·단계·구현 진행률·갱신 시각)은 이미 지금도 `init`·`step`
   호출마다 매번 새로 그려지는 "현재 상태" 스냅샷이라 이번 변경으로 새로 생기는 손실이 없다.
   `subtitle` 필드는 파싱만 되고 `hub_template.html`/`hub.py`/`hub_collect.py` 어디에서도
   렌더링되지 않는 죽은 필드였다(그룹 4 제거로 인한 허브 영향 없음). 이 결론은
   `feature/hub-dashboard` 의 **현재** 구현 기준이며, 그 브랜치가 향후 `#dz-log` 를 읽도록
   바뀌면 재검토가 필요하다 — 이번 작업 범위에서 그 브랜치는 건드리지 않는다.
2. **「지금」 카드에서 단계 번호·이름을 완전히 없애는 것 → 승인.** 카드는
   `경과 / 마지막 기록 / 다음` 3행이 된다.
3. **`docs/images/dashboard-sample.png` 재촬영 → 이번 범위에서 제외.** 별도로 처리한다.

### 추가 확정 (2026-08-12, 검증용 스크린샷 검토 후)

위 2번의 3행 재설계를 실제로 구현해 스크린샷으로 확인한 결과, 사용자가 "경과/마지막 기록/다음
정보 자체가 쓸모없다"고 판단해 **「지금」 카드를 완전히 제거**하기로 최종 결정했다. 현재 진행
중인 작업이 무엇인지는 `ol#dz-steps` 의 활성 `<li>` 에 이미 표시되는 `.step-detail` 한 줄로
충분하다는 판단이다. `#dz-now-card` 와 그 전용 코드(마크업·CSS·`renderNowCard()` 등 렌더 함수
4종·`step` 절차의 `data-started-at` 각인 규칙)를 전부 제거했고, 불변식 7("「지금」 카드는 파생
뷰다")도 함께 삭제했다(불변식 8은 번호를 유지). 위 2번 절은 **이 최종 결정으로 대체된다.**
