# 대시보드 단계별 상세 요약 + PiP 압축 뷰 (PRP)

## 요구사항 요약

`/dashboard` 에 사용자 피드백 두 가지를 반영한다. **(1) PiP 압축 뷰** — 플로팅(Document PiP) 창에서는
「작업 추적」 로그 카드를 감추고 세션 제목·전체 단계 목록·진행률만 보여주며, **현재 진행중인 단계가
한눈에 띄게** 강조한다. **(2) 단계별 상세 요약** — `ol.steps` 의 각 `<li>` 에 "그 단계에서 지금 무엇을
하고 있는지" 한 줄 요약을 붙일 수 있게 `step` 에 **선택적 상세 인자**를 추가한다.

두 요청은 독립적이지 않다. PiP 압축 뷰가 보여줘야 할 "현재 진행중인 단계"의 실질 정보량은 단계명
하나(예: "3 구현")로는 거의 0이고, **상세 텍스트가 붙는 순간 비로소 곁눈질할 가치가 생긴다**
(예: "3 구현 [진행중] — commands/dashboard.md 폴링 스크립트 작성 중"). 그래서 이 PRP 는 두 요청을
한 라운드로 묶고, PiP 압축 뷰에서 **활성 단계의 상세만 노출**하는 규칙으로 둘을 결합한다.

### 사용자 요청 원문

> "pip 모드에서는 간략히 보여주면 좋겠어. 작업 추적 항목은 제거해도 될거 같고, 세션 제목, 전체 진행 단계,
> 현재 진행중인 단계를 보여주면 좋겠어."
> "대시보드 진행 단계 항목에서 각 단계(ol.steps li)에 어떤 업무를 진행 중인지 상세 내용을 간략하게
> 요약해서 보여주면 좋을거 같아."

### 이 PRP 의 위치 — 선행 라운드와의 관계

| 문서 | 상태 | 이 PRP 와의 관계 |
|------|------|-----------------|
| [`session-dashboard.md`](./session-dashboard.md) | 커밋됨 | 원형. "파일의 DOM 자체가 상태" 대전제 |
| [`dashboard-group-matrix.md`](./dashboard-group-matrix.md) | 커밋됨 (67e86b4) | 그룹×단계 모델. **확정 방향 6 이 서술형 텍스트를 스코프 아웃했다 → 이번에 부분 번복한다(설계 결정 3)** |
| [`dashboard-pip-floating.md`](./dashboard-pip-floating.md) | **구현·검수 PASS, 미커밋(워킹 디렉토리)** | 직전 라운드. PiP `.wrap` 이동 방식·폴링 동기화 계약이 이번 설계의 전제 |

> **중요**: 이 PRP 의 기준선은 **커밋된 HEAD 가 아니라 현재 워킹 디렉토리**다.
> `commands/dashboard.md`(924줄) · `tests/run.sh`(1518줄, T22-1~38) · `README.md` 에는 PiP 기능이
> 이미 반영돼 있다. 구현 착수 전에 `git status` 로 그 미커밋 변경이 그대로 있는지 확인한다.

### 확정 사항 (재논의 대상 아님)

1. PiP 에서 로그 카드는 **감춘다**(사용자 요청). 무엇을 어떻게 감출지는 아래 설계 결정 1.
2. 폴링 동기화 계약(`dashboard-pip-floating.md`)을 깨지 않는다. `syncLog` 를 포함한 동기화 7종은
   **한 줄도 퇴화하지 않는다**.
3. `step` 의 기존 두 호출 형태(`<n>`, `<g>.<p>`)는 상세 인자 없이도 **오늘과 완전히 동일하게** 동작한다.
4. `init`/`log`/`serve`/`on`/`off` 절차는 건드리지 않는다.
5. 불변식 1(선형·매트릭스 비공존)과 불변식 2(`<li>`/`<td>` 는 한 줄에 하나)를 유지한다.

### 사용자 스토리

> 전체 경로 작업을 돌리는 개발자로서, 에디터를 보는 동안 화면 구석의 작은 플로팅 창만 흘깃 보고
> **"지금 어느 단계에서 무슨 파일을 만지고 있는지"**를 알고 싶다. 로그 20건을 좁은 창에 밀어 넣는 대신,
> 단계 목록 + 활성 단계의 한 줄 요약만 남기면 곁눈질 비용이 0에 가까워진다.

### 복잡도

**Medium** — 3파일. 신규 코드는 CSS 6줄 + 기존 CSS 1줄 수정 + 마크업 2곳 + `step` 절차 1단계 개정 +
테스트 8개. **새 모듈·새 레이어·새 의존성 없음. 스크립트(JS)는 한 줄도 바뀌지 않는다.**

---

## UX 전환

### Before — PiP 창 (직전 라운드 결과)

```
┌─ 플로팅 420×620 ────────────┐
│ 세션 제목                    │
│ ▇▇▇▇▇░░░  3/6 · 50%         │
│ 1 설계          [완료]       │   ← 단계명뿐. "지금 뭘 하는지" 정보량 0
│ 2 승인          [완료]       │
│ 3 구현          [진행중]     │
│ 4 검수          [대기]       │
│ ───────────────────────     │
│ 작업 추적                    │   ← 좁은 창에서 .lead 가 말줄임으로 잘려
│ [전체][구현][검수]           │      읽히지도 않는데 세로 공간을 다 먹는다
│ 17:07 R2 검수FAIL applyGa…  │
│ 16:52 R2 구현    matrix …   │
│ …                            │
│ 갱신: 2026-08-06 17:07       │
└──────────────────────────────┘
```

### After — PiP 창 (이번 라운드)

```
┌─ 플로팅 420×620 ────────────┐
│ 세션 제목                    │
│ ▇▇▇▇▇░░░  3/6 · 50%         │
│ 1 설계          [완료]       │
│ 2 승인          [완료]       │
│ ┏━━━━━━━━━━━━━━━━━━━━━━┓   │   ← active 단계만 배경 강조
│ ┃ 3 구현        [진행중] ┃   │
│ ┃   commands/dashboard… ┃   │   ← 활성 단계의 상세만 노출
│ ┗━━━━━━━━━━━━━━━━━━━━━━┛   │
│ 4 검수          [대기]       │
│                              │
│ 갱신: 2026-08-06 17:07       │   ← 정체 감지 신호. PiP 에서도 유지한다
└──────────────────────────────┘
     (로그 카드는 display:none — DOM 에는 그대로 살아 있다)
```

### After — opener(전체 화면). 상세는 완료 단계에도 남아 이력이 된다

```
┌── http://localhost:8791/dashboard.html ──────────────────┐
│ 1 설계                                        [완료]     │
│     PRP 4종 검토 후 dashboard-step-detail 작성           │
│ 2 승인                                        [완료]     │
│ 3 구현                                      [진행중]     │
│     commands/dashboard.md 폴링 스크립트 작성 중          │
│ 4 검수                                        [대기]     │
│ ── 작업 추적 (전체 화면에서는 그대로 보인다) ──          │
└──────────────────────────────────────────────────────────┘
```

### 상호작용 변화

| 접점 | Before | After | 비고 |
|------|--------|-------|------|
| `step <n> <state>` | 상태만 갱신 | **동일** | 하위 호환. 상세 인자는 선택 |
| `step <n> <state> "상세"` | (없음) | 상태 + 한 줄 요약 갱신 | 신규 |
| `step <n> <state> ""` | (없음) | 상태 갱신 + 상세 **지움** | 명시적 비우기 |
| `step <g>.<p> <state> "상세"` | (없음) | 상태 갱신 + **상세 무시 + 한 줄 보고** | 설계 결정 4 |
| PiP 창 진입 | 제목·진행률·단계·**로그** 전부 | 로그 카드만 숨김 | CSS 1줄 |
| PiP 창의 active 단계 | 굵은 글씨 + 파란 칩 | + 배경 강조 + **상세 노출** | 나머지 단계의 상세는 숨김 |
| PiP 창 닫고 복귀 | 로그 그대로 복귀 | **동일** (DOM 을 건드리지 않으므로) | 설계 결정 1 |
| 유형 필터·세션 탭 | PiP 에서도 조작 가능 | PiP 에서는 안 보임(카드 안이므로) | opener 에서는 그대로 |
| `file://` 로 열기 | 로그 포함 전체 표시 | **동일** (`body.dz-pip` 미매칭) | 회귀 없음 |

---

## 영향 범위

### 수정 파일 (3개)

| 파일 | 수정 내용 | 이유 |
|------|----------|------|
| `commands/dashboard.md` | ① frontmatter `argument-hint` ② 「호출 규약」의 `step` 2줄 ③ 「데이터 모델」 동적 셀렉터 표의 `#dz-step-{n}` 행 + 정적 요소 표에 `#dz-log-card` 행 ④ 「진행 시각화 규격」에 `.step-detail` 규격 절 신설 ⑤ `init` 6단계의 `<li>` 마크업 ⑥ `step` 절차 1단계 개정 ⑦ 「CLAUDE.md 트리거」 표 아래 각주 1줄 ⑧ 템플릿: 헤더 주석 맵 2줄 · `<style>` 6줄 추가 + 1줄 수정 · `<body>` 1줄 수정 | 이 커맨드가 유일한 실행 주체다. 템플릿·절차·규약이 전부 이 한 파일에 있다 |
| `tests/run.sh` | T22 에 하위 검증 **T22-39 ~ T22-46** 추가, 선행 주석·`test_desc` 의 `T22-1~T22-38` → `T22-1~T22-46` | 이 저장소의 회귀 방지 수단은 grep 기반 문서 정합성 테스트뿐이다 |
| `README.md` | L120 커맨드 표의 줄 수(**현재 913 은 이미 stale — 실제 924**), L139~149 설명 문단에 1~2문장 | 줄 수는 실제 값이어야 하고, 사용자가 상세 인자·PiP 압축 뷰의 존재를 알 경로가 README 뿐이다 |

> **README L120 의 stale 줄 수는 직전 라운드에서 누락된 것이다.** 이번 변경과 무관하게 이미 틀려 있으므로
> 같은 줄을 어차피 고칠 때 함께 바로잡는다(승인 항목 5). 무관한 개선이 아니라 **같은 줄의 정확성 복구**다.

### 미영향 (전수 확인)

| 대상 | 확인 근거 | 결과 |
|------|----------|------|
| 템플릿 하단 `<script>` (약 130줄) | 압축은 **CSS 전용**이고 상세는 `outerHTML` 동기화가 이미 흡수한다 | **한 줄도 바뀌지 않는다** |
| 폴링 동기화 계약 7행 | `#dz-steps` 는 이미 `outerHTML` 대입 → 새 `<span>` 이 자동으로 실려 온다. `#dz-log` 는 `innerHTML` 대입 → 부모 div 에 id 가 붙어도 무관 | 계약 불변 |
| `sessionTabsChanged(fresh)` | 라디오가 **DOM 에 그대로 남는다**(`display:none` 은 개수에 영향 없음) | 정상 동작 (→ 리스크 1) |
| 「동적(치환 대상) — 7 셀렉터」 개수 | `.step-detail` 은 **class** 다. 새 `id` 를 만들지 않는다. `#dz-log-card` 는 정적 요소 표로 간다 | **7 유지** (T22-2 무영향) |
| `init`/`log`/`serve`/`on`/`off` 절차 | 확정 사항 4 | 한 글자도 건드리지 않는다 |
| `install.sh` 의 `COMMANDS_FILE_COUNT=8` | 새 커맨드 파일을 만들지 않는다 | 변경 불필요 |
| 프로젝트 `CLAUDE.md` | `/dashboard` 언급은 "`step`·`log` 를 호출한다" 수준. 상세 인자는 선택이라 트리거 규약이 바뀌지 않는다 | **수정하지 않는다** |
| `log` 절차의 grep 앵커 `grep -n 'id="dz-log"'` | 패턴이 **닫는 따옴표까지 포함**하므로 `id="dz-log-card"` 는 매칭되지 않는다 | 앵커 유일성 유지 (→ 설계 결정 2, T22-41) |
| T22-37 (pip 버튼이 `.wrap` 바깥) | `id="dz-updated"` 는 문서 전체에서 **정확히 1회** 등장(확인 완료). 이번에 추가하지 않는다 | 계속 통과 |
| T22-38 (`[id="dz-` 금지) | 새 셀렉터는 CSS id 셀렉터(`#dz-log-card`) 형태다 | 계속 통과 |
| T23-1~11 | `on`/`off` 절·README 설치 절차 미변경. `argument-hint` 의 `| on | off` 연속 문자열 유지 | 계속 통과 |

### 손대지 않는 것 (발견했지만 이번 요청과 무관)

- `commands/dashboard.md` L628 의 `~/Desktop/dashboard.html` 원본 경로 언급. 이전 두 라운드에서도
  "언급만 하고 지우지 않는다"로 처리됐다. 이번에도 동일.
- `PIP_WIDTH`/`PIP_HEIGHT`(420×620). 로그가 사라져 세로 여유가 생기지만 창 크기는 사용자가 조절하고
  브라우저가 기억한다. 상수를 건드리지 않는다(YAGNI).

---

## 필독 파일 (구현 착수 전)

| 우선순위 | 파일 | 범위 | 이유 |
|---------|------|------|------|
| P0 | `commands/dashboard.md` | L700~790 (템플릿 `<style>` + `<body>`) | 유일한 수정 본체. 특히 L711~721 `ol.steps` 계열, L764~767 `body.dz-pip`, L771~790 마크업 |
| P0 | `commands/dashboard.md` | L398~436 (`step` 절차) | 상세 갱신을 얹을 자리. grep 앵커 + 전이(±1) 산술 구조를 그대로 유지해야 한다 |
| P0 | `commands/dashboard.md` | L49~172 (데이터 모델 + 폴링 동기화 계약) | 셀렉터 계약·불변식 2·grep 유일성 불변식. **무엇을 건드리면 안 되는지의 근거** |
| P0 | `tests/run.sh` | L1229~1338 (T22-29~38 + 함수 말미) | 새 하위 검증을 붙일 자리와 assert 스타일(`record_failure` 후 `return 1`) |
| P1 | `commands/dashboard.md` | L791~921 (`<script>`) | **읽되 고치지 않기 위해** 읽는다. `syncVisualization`·`sessionTabsChanged` 가 이번 설계의 전제 |
| P1 | `docs/prps/dashboard-pip-floating.md` | L436~458 (설계 결정 2·3), L661~677 (리스크 5) | 이동 방식·라디오 미치환 계약의 근거 |
| P1 | `docs/prps/dashboard-group-matrix.md` | L13~22 (확정 방향), L409~419 (스코프 아웃), L516~520 (리스크 5) | 확정 방향 6 을 부분 번복하는 근거와, 매트릭스 칸 폭 제약 |
| P2 | `README.md` | L111~152 | 커맨드 표와 설명 문단의 문체 |

### 외부 조사

**필요 없음 — 전부 기존 내부 패턴이다.** 새로 쓰는 CSS 기능은 두 가지뿐이고 둘 다 광범위 지원 상태다.

| 주제 | 근거 | 핵심 |
|------|------|------|
| `flex-basis:100%` 로 flex 줄바꿈 | CSS Flexbox Level 1 (CR, 전 브라우저) | `flex-wrap:wrap` 인 컨테이너에서 `flex-basis:100%` 인 아이템은 **항상 새 줄을 차지**한다. 래퍼 `div` 없이 "두 번째 줄"을 만드는 유일한 방법이라 `~` 결합자 제약과 충돌하지 않는다 |
| `:empty` 의사 클래스 | CSS Selectors Level 3 (REC, 전 브라우저) | **자식 노드와 텍스트가 하나도 없을 때만** 매칭된다. 공백 문자 하나만 있어도 매칭이 풀린다 → 상세 span 은 `<span class="step-detail"></span>` 처럼 **내부에 공백조차 넣지 않는다**(GOTCHA) |

**새 npm/pip 의존성 없음. 새 색 토큰 없음.**

---

## 데이터 모델

### 단계 상세(step detail) — 개념

```
Step                      # 선형 모드의 <li id="dz-step-{n}">
  index   : int           # 1부터
  name    : string        # init 이 넣고 이후 치환하지 않는다
  state   : "done" | "active" | "wait"
  detail  : string        # 신규. 빈 문자열이면 "없음". 줄바꿈 불가. 기본값 ""
```

- `detail` 은 **단계에 종속된 값**이다. 별도 `#dz-current` 요소를 만들지 않는 이유가 여기 있다 —
  분리하면 "어느 단계의 상세인가"를 잃고, 완료된 단계의 이력도 남지 않는다(설계 결정 3).
- `detail` 은 **`step` 호출자가 문자열로 준다.** 대시보드가 추론하거나 `#dz-log` 에서 끌어오지 않는다.
  이 커맨드의 대전제("기계적 치환만, 판단 개입 금지")를 지키는 유일한 방법이다.
- 매트릭스 모드의 `Cell` 에는 `detail` 이 **없다**(설계 결정 4).

### 동적(치환 대상) 셀렉터 표 — 1행 갱신, **개수는 7 그대로**

| 셀렉터 | 치환 대상 | 값 |
|--------|----------|-----|
| `#dz-step-{n}` **(그룹 1개)** | `class` 속성 + 자식 `.chip` 텍스트 + **자식 `.step-detail` 텍스트** | `done`\|`active`\|`wait` / `완료`\|`진행중`\|`대기` / **한 줄 상세(빈 문자열 허용)** |

> `.step-detail` 은 **class** 이고 `<li>` 와 같은 줄 안에 있으므로, 셀렉터 표의 행 수도 `step` 의 Edit 횟수도
> 늘지 않는다. `<li>` 줄 전문 치환 **1회에 상태와 상세가 함께 반영**된다.

### 정적 요소 표 — 1행 추가

| 요소 | 역할 |
|------|------|
| `#dz-log-card` | 「작업 추적」 카드 `<div>`. `init`/`step`/`log` 도, 폴링도 이 요소 자체를 치환하지 않는다. **PiP 압축 뷰가 CSS 로 숨기기 위한 유일한 용도**이며, DOM 에서 제거하지 않는다 |

### 불변식 추가 (기존 2개에 이어)

3. **`.step-detail` 의 내용에 줄바꿈이 들어가지 않는다.** `<li>` 가 여러 줄로 쪼개지면 불변식 2가
   깨지고, `step` 0단계의 grep 앵커와 감사용 `grep -c 'dz-step-.*class="done"'` 가 동시에 어긋난다.
4. **`#dz-log-card` 는 CSS 로만 감춘다.** 스크립트가 이 id 를 `getElementById`/`querySelector` 로
   참조하지 않는다(→ 설계 결정 1, T22-45 가 역방향 assertion 으로 강제).

---

## 인터페이스

### 호출 규약 (`step` 2줄 개정)

```
/dashboard init "<제목>" "<단계1|단계2|...>"
/dashboard init "<제목>" "<그룹A:단계1,단계2|그룹B:단계1,단계2>"
/dashboard step <n> <done|active|wait> ["상세"]    # 그룹 1개 — 단계 번호. 상세는 선택
/dashboard step <g>.<p> <done|active|wait>         # 그룹 2개 이상 — 행.열 (상세 미지원)
/dashboard log <impl|pass|fail|commit> "<한 줄 요약>" ["상세"] [--round N]
/dashboard serve [포트] | serve stop
/dashboard on | off
```

### frontmatter

```yaml
argument-hint: "init \"<제목>\" \"<단계1|...> 또는 <그룹A:단계1,단계2|그룹B:...>\" | step <n>|<g>.<p> <done|active|wait> [\"상세\"] | log <impl|pass|fail|commit> \"<요약>\" [...] | serve [포트] | on | off"
```

> **GOTCHA**: `| on | off` 를 **연속 문자열로 맨 뒤에 유지**한다(T23-6 이 `head -4` 에서 이 부분일치를
> 확인). `serve` 도 `head -4` 안에 남아야 한다(T22-36). `[\"상세\"]` 는 `step` 절 안에만 넣는다.

### CLAUDE.md 트리거 표 — 표는 그대로, 아래 각주 1줄 추가

```markdown
> `step` 의 네 번째 인자로 한 줄 상세를 붙이면(예: `step 3 active "폴링 스크립트 작성 중"`) 그 단계가
> 지금 무엇을 하고 있는지가 대시보드에 바로 보인다. 생략해도 되며, 생략하면 기존 상세가 유지된다.
```

### `step` 절차 — 1단계만 개정 (Bash 1회 + Edit 3회, 비용 불변)

0단계(grep)와 2·3단계(진행률·갱신 시각)는 **한 글자도 바뀌지 않는다.**

```markdown
1. **상태 치환** — 0에서 얻은 줄 전문을 `old_string` 으로 Edit 1회.
   - 선형: `class="{state}"` 와 자식 `.chip` 텍스트, 그리고 아래 표에 따른 `.step-detail` 텍스트
   - 그룹: `data-state="{state}"` 와 칸 텍스트       (같은 어휘를 쓴다)

   **`.step-detail` 갱신 규칙 (선형 모드 전용)**

   | 네 번째 인자 | 동작 |
   |-------------|------|
   | 주어지지 않음 | `.step-detail` 을 **건드리지 않는다**(기존 값 유지). 상세 없이 부르던 기존 호출과 결과가 완전히 같다 |
   | `"내용"` | 이스케이프한 내용으로 `<span class="step-detail">내용</span>` 를 치환한다 |
   | `""`(빈 문자열) | 내용을 비운다. `.step-detail:empty` 규칙이 그 줄을 통째로 숨긴다 |

   - **이스케이프**: `log` 와 동일하게 `&`→`&amp;`, `<`→`&lt;`, `>`→`&gt;` 순서로 치환한다.
     코드 조각(`Foo<T>`, `a && b`)이 그대로 들어가면 태그가 깨진다.
   - **줄바꿈을 넣지 않는다.** `<li>` 가 여러 줄로 쪼개지면 불변식 2가 깨져 이후 `step` 의 grep 앵커와
     감사용 `grep -c` 가 전부 어긋난다. 길이는 40자 내외를 권장하며, 넘치면 CSS 가 말줄임 처리한다.
   - 대상 `<li>` 에 `.step-detail` span 이 **없으면**(이 기능 도입 이전에 만들어진 대시보드)
     `</li>` 바로 앞에 새로 삽입한다. 같은 Edit 1회 안에서 처리되므로 비용이 늘지 않는다.
   - **매트릭스 모드(`<g>.<p>`)에서 상세 인자가 주어지면**, 상태 갱신은 정상 수행하고 **상세는
     무시했음을 한 줄 보고한다.** 중단하지 않는다 — 상태 갱신까지 막으면 부가 정보 손실이
     진행률 정지라는 더 큰 손실로 번진다(설계 결정 4).
```

### `init` 6단계 — 선형 `<li>` 마크업 (span 1개 추가)

```html
<li id="dz-step-{n}" class="{active|wait}"><span class="num">{n}</span>{단계명}<span class="chip">{진행중|대기}</span><span class="step-detail"></span></li>
```

> **GOTCHA**: `<span class="step-detail"></span>` 안에 **공백조차 넣지 않는다.** `:empty` 는 공백 문자
> 하나에도 매칭이 풀려 빈 회색 줄이 남는다. 그리고 `<li>` 는 **여전히 한 줄에 하나**다(불변식 2).

### 템플릿 `<style>` — 기존 1줄 수정 + 6줄 추가

**수정** (현행 L712. `flex-wrap`·`row-gap` 만 덧붙인다):

```css
  ol.steps li{display:flex;align-items:center;gap:12px;padding:11px 4px;border-bottom:1px solid var(--soft);font-size:15px;flex-wrap:wrap;row-gap:3px}
```

**추가 A** — `li.active .chip` 규칙(현행 L721) **바로 뒤**, `table.matrix` 앞:

```css
  .step-detail{flex-basis:100%;margin-left:38px;font-size:12.5px;font-weight:400;color:var(--muted);overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
  .step-detail:empty{display:none}
```

- `margin-left:38px` = `.num` 폭 26px + `ol.steps li` 의 `gap` 12px. 단계명 시작선과 정렬된다.
- `font-weight:400` 은 `li.active{font-weight:700}` 상속을 되돌린다(활성 단계에서 상세까지 굵으면
  단계명과 위계가 무너진다).
- `overflow/text-overflow/white-space` 3종은 `.entry .lead`(현행 L754)가 이미 쓰는 말줄임 패턴 그대로다.

**추가 B** — `<style>` 맨 끝(`body.dz-pip .sub` 현행 L767) **바로 뒤**:

```css
  body.dz-pip #dz-log-card{display:none}
  body.dz-pip ol.steps li{padding:9px 4px}
  body.dz-pip ol.steps li.active{background:#EAF2FB;border-radius:8px;padding-left:8px;padding-right:8px}
  body.dz-pip ol.steps li:not(.active) .step-detail{display:none}
```

- `#EAF2FB` 는 **이미 템플릿에 리터럴로 존재**하는 값이다(`li.active .chip`, `.matrix td.cell[data-state="active"]`,
  `.badge.impl`). 새 색 토큰을 만들지 않으므로 T22-27(`:root` 완전 일치)이 계속 통과한다.
- 이 4줄은 opener 문서(`body` 에 `dz-pip` 클래스 없음)와 `file://` 경로에서 **매칭 대상이 없어 아무 일도
  하지 않는다.** 매트릭스 CSS 가 선형 세션에서 노는 것과 동일한 구조다.

### 템플릿 `<body>` — 1줄 수정

```html
  <div class="card" id="dz-log-card">
```

(현행 L780 `<div class="card">` 중 **두 번째** 카드. 첫 번째 카드(제목/진행률)는 그대로 둔다.)

### 템플릿 헤더 주석 맵 — 2줄 갱신

```
  #dz-step-{n}          : [그룹 1개] 각 단계 li — class(done|active|wait) + .chip 텍스트
                          + .step-detail 텍스트(그 단계의 한 줄 상세. 비어 있으면 CSS 가 숨긴다)
```

정적 목록에 1줄 추가:

```
  #dz-log-card               : 작업 추적 카드 div. PiP 압축 뷰가 CSS 로만 숨긴다
                               (DOM 에서 제거하지 않는다 — 복귀 시 로그가 사라진다)
```

---

## 설계 결정과 근거

### 1. 로그 카드는 **CSS 로 숨긴다**. DOM 에서 제거하지 않는다

사용자 요청은 "제거해도 될 거 같다"였지만, **DOM 제거는 세 곳을 동시에 깨뜨린다.**

| 깨지는 것 | 구체적 결과 |
|----------|------------|
| `sessionTabsChanged(fresh)` | 라이브 `wrap.querySelectorAll('input[name="dzs"]').length` 가 0 이 되고 `fresh` 는 2 이상 → **매 폴링(5초)마다 "새 세션"으로 오판** → `pipWindow.close()` + `location.reload()` 무한 루프. 플로팅이 사실상 사용 불가가 된다 |
| PiP 복귀 (`insertBefore(wrap, pipButton)`) | `.wrap` 이 통째로 돌아오지만 제거된 카드는 돌아오지 않는다 → **로그가 사라진 채 복귀**. 사용자가 지적한 바로 그 무결성 문제 |
| `syncLog(fresh)` | `wrap.querySelector('#dz-log')` 가 `null` → `if(live && next)` 가드에 걸려 조용히 no-op. 이후 로그 갱신이 **영구히 멈춘다**(실패가 눈에 보이지도 않는다) |

`display:none` 은 이 셋 중 어느 것도 건드리지 않는다. 요소는 DOM 에 그대로 있고, 폴링은 보이든 안 보이든
같은 노드를 갱신하며, 복귀는 서브트리 이동 하나로 끝난다. **비용은 CSS 1줄이고 얻는 것은 세 계약의 보존이다.**

부수 효과: 압축 뷰에서 렌더 트리가 줄어 좁은 창의 레이아웃 계산이 오히려 가벼워진다.

### 2. 로그 카드에 `id` 를 붙인다 — 구조 셀렉터(`:nth-of-type`)를 쓰지 않는다

로그 카드에는 현재 식별자가 없다(`<div class="card">` 두 개 중 두 번째). 후보는 셋이었다.

| 후보 | 판정 |
|------|------|
| `body.dz-pip .card:nth-of-type(2)` | **기각.** `nth-of-type` 은 class 가 아니라 **태그(div) 순서**로 센다 — `.wrap` 안의 `div` 는 카드 2개 + `.foot` 3개다. 지금은 맞지만 카드를 추가·재배치하는 순간 **조용히 엉뚱한 카드를 숨긴다.** 실패가 눈에 보이지 않는 종류의 결합이다 |
| `body.dz-pip .card:last-of-type` | **기각.** `.foot` 이 마지막 div 라 애초에 매칭되지 않는다. 실제로는 `.card + .card` 형태를 써야 하는데 역시 순서 의존이다 |
| `<div class="card" id="dz-log-card">` | **채택.** 의도가 마크업에 적혀 있고, CSS 규칙이 1줄로 끝나며, 순서 변경에 영향받지 않는다 |

**`id` 추가가 grep 앵커를 깨지 않는지 직접 검증했다.** `log` 절차 0-a 의 앵커는
`grep -n 'id="dz-log"'` 로 **닫는 따옴표까지 포함**한다. `id="dz-log-card"` 에서는 `dz-log` 다음이
`-` 이므로 이 패턴에 매칭되지 않는다. 즉 앵커의 줄 단위 유일성이 보존된다.
같은 이유로 `step` 0단계의 `id="dz-step-{n}"` 계열도 무관하다. **T22-41 이 이 성질을 직접
회귀 테스트로 고정한다** — "따옴표를 뺀 느슨한 패턴으로 되돌리거나 id 이름을 바꾸면 실패한다".

### 3. 상세는 **단계 안에** 넣는다 — `#dz-current` 서술형 요소를 신설하지 않는다

`dashboard-group-matrix.md` 확정 방향 6 은 `#dz-current`/`#dz-next` 서술형 텍스트를 스코프 아웃했고,
그 근거는 두 가지였다: (a) 서술형 문장은 기계적 치환이 아니라 매번 다시 쓰는 판단이다, (b) 같은 정보가
이미 `#dz-log` 에 남는다.

**이번 설계는 (a)를 우회하고 (b)의 전제를 바꾼다 — 부분 번복이며, 그 차이를 명시한다.**

- **(a) 반박**: 상세 텍스트를 **`step` 호출자가 인자로 준다.** 대시보드는 받은 문자열을 이스케이프해
  치환할 뿐이고, "무엇을 쓸지"는 이미 `log impl "…"` 에서 하던 것과 같은 종류의 입력이다.
  판단이 커맨드 안으로 들어오지 않는다 → 결정성 유지.
- **(b) 전제 변화**: PiP 압축 뷰가 **로그 카드를 감추기로 한 순간**, "같은 정보가 `#dz-log` 에 있다"가
  압축 뷰에서는 성립하지 않는다. 요청 1 이 요청 2 의 필요를 만들어낸 것이다.
- **별도 요소가 아니라 `<li>` 안에 두는 이유**: 상세가 단계와 붙어 있어야 (i) 어느 단계의 이야기인지
  자명하고, (ii) 완료 단계의 상세가 남아 **단계별 이력**이 되며, (iii) `step` 의 Edit 횟수가 늘지 않고,
  (iv) 폴링의 `#dz-steps` `outerHTML` 동기화가 상세까지 **공짜로** 실어 나른다. 별도 요소였다면
  셀렉터 표가 8행이 되고 폴링 계약에 행이 하나 더 붙는다.

### 4. 이번 스코프는 **선형 모드 한정**이다 (매트릭스 제외)

사용자 요청 원문이 `ol.steps li` 를 명시했지만, 그것만이 근거는 아니다.

1. **물리적 공간이 없다.** 카드 폭 860px, 그룹명 열 34%(≈290px)를 빼면 6열 기준 칸당 ≈95px 다.
   `dashboard-group-matrix.md` 리스크 5 는 이미 "7열 이상이면 **낱말 하나도** 줄바꿈된다"고
   기록했다. 그 칸에 한 줄 요약을 넣는 것은 설계가 아니라 파괴다.
2. **표 밖에 두면 새 컴포넌트다.** "활성 칸 목록" 같은 영역을 만들면 셀렉터·마크업·`step` 절차가
   전부 늘고, 이번 요청에 없던 UI 를 발명하게 된다(YAGNI).
3. **매트릭스 세션에서 손실이 없다.** 오늘과 동일하게 동작하고, 상세는 `#dz-log` 가 계속 담는다.
   PiP 압축 뷰에서도 표는 그대로 보이고 로그 카드만 숨는다 — 요청 1 의 이득은 그대로 받는다.
4. **무시는 조용히 하지 않는다.** `step <g>.<p> … "상세"` 를 받으면 상태는 갱신하고 **무시했음을
   한 줄 보고**한다. 중단하지 않는 이유는 상태 갱신 자체는 정상 수행 가능하기 때문이다 —
   부가 정보 손실 때문에 진행률을 멈추면 손해가 더 크다.

> 다중 트랙 세션에서 상세가 실제로 아쉬워지면 그때 별도 라운드에서 다룬다. 지금 만들지 않는다.

### 5. 상세 인자 생략 = **기존 값 유지**, `""` = 지움

세 가지 후보 중에서 골랐다.

| 규칙 | 판정 |
|------|------|
| 생략하면 상세를 **비운다** | **기각.** 하위 호환이 깨진다. 기존 `step 3 done` 호출이 방금 넣은 상세를 지워버려, 상세를 유지하려면 매번 다시 넘겨야 한다 |
| 생략하면 **유지**, 지우려면 `""` | **채택.** 기존 호출의 결과가 오늘과 완전히 동일(확정 사항 3). 지우는 의도는 빈 문자열로 명시된다 |
| 별도 `--clear` 플래그 | **기각.** `""` 로 충분한데 문법이 하나 늘어난다 |

"유지"가 절차상으로도 가장 싸다 — `<li>` 줄 전문 치환에서 **그 부분을 그냥 안 건드리면** 된다.
분기 하나도 추가되지 않는다.

대가: `done` 으로 넘어간 단계에 진행형 문장("…작성 중")이 남아 어색할 수 있다. 완화는 둘이다 —
호출자가 완료 시점의 문장을 넘기거나 `""` 로 지운다(규약에 명시), 그리고 **PiP 압축 뷰에서는
active 단계의 상세만 보이므로** 어색한 문장이 곁눈질 화면에 뜨지 않는다.

### 6. PiP 에서 "현재 단계 강조"는 **CSS 배경**으로 한다 — `scrollIntoView` 를 쓰지 않는다

| 후보 | 판정 |
|------|------|
| active 단계 배경 강조 + 활성 상세만 노출 | **채택.** CSS 2줄. JS 0줄. 폴링과 무관 |
| `scrollIntoView` 로 활성 단계에 스크롤 | **기각.** JS 가 필요하고 **폴링마다 재실행**해야 하며, 사용자가 직접 스크롤한 위치를 5초마다 뺏는다(`<details>` 펼침을 되접는 것과 같은 종류의 실수 — `dashboard-pip-floating.md` 설계 결정 3 이 이미 배운 교훈). 게다가 단계는 보통 4~6개라 420×620 창에서 **스크롤 자체가 거의 발생하지 않는다** |
| 비활성 단계를 아예 숨김 | **기각.** 사용자가 "전체 진행 단계"를 보고 싶다고 명시했다 |

**활성 단계의 상세만 노출**하는 규칙(`li:not(.active) .step-detail{display:none}`)이 요청 1("간략히")과
요청 2(상세)를 잇는 지점이다. 전체 화면에서는 모든 상세가 보여 이력이 되고, 좁은 창에서는
지금 필요한 한 줄만 남는다. **같은 DOM, 다른 스코프.**

### 7. `#dz-updated`(갱신 시각)는 PiP 에서도 **계속 보여준다**

로그 카드와 함께 숨기고 싶은 유혹이 있지만 숨기지 않는다. `dashboard-pip-floating.md` 리스크 1
(숨은 탭의 타이머 스로틀링으로 폴링이 최대 1분까지 늘어질 수 있음)의 **완화책 4개 중 하나가
"`#dz-updated` 가 항상 보여 정체가 눈에 보인다"**였다. 이걸 숨기면 "조용히 틀린 화면"이 된다.
한 줄짜리 `.foot` 이라 압축 뷰의 공간을 거의 먹지도 않는다.

### 8. 스코프 아웃과 사유

| 제외 항목 | 사유 |
|-----------|------|
| 매트릭스 칸의 상세 | 설계 결정 4. 칸 폭이 물리적으로 부족하다 |
| `#dz-log` 최신 항목을 활성 단계 상세로 **자동** 반영 | 자동 연결은 "어느 로그가 어느 단계에 속하는가"라는 판단을 커맨드 안에 들인다. `log` 와 `step` 은 호출 시점이 다르다. 명시 인자가 결정적이다 |
| 상세를 여러 줄로 | 불변식 2(한 줄에 하나)와 정면 충돌. 말줄임 한 줄로 충분하다 |
| 상세를 `title` 속성 툴팁으로 | 좁은 PiP 창에서 hover 툴팁은 발견성이 사실상 0이다 |
| PiP 전용 창 크기 재조정(`PIP_HEIGHT` 축소) | 브라우저가 크기를 기억하고 사용자가 조절한다. 상수를 건드릴 근거가 없다 |
| PiP 에서 로그를 "최근 1건만" 남기기 | 사용자는 "작업 추적 항목은 제거해도 될 것 같다"고 했다. 부분 노출은 요청보다 복잡하고 CSS 규칙이 늘어난다 |
| PiP 안에 "로그는 전체 화면에서 보세요" 안내 문구 | 마크업이 늘고, `#dz-pip-hint` 는 opener 에 있어 PiP 창에서 안 보인다. README 안내로 충분하다 |

---

## 테스트 계획

### 무엇을 검증할 수 있고 무엇은 못 하는가

이 커맨드에는 실행 코드가 없다(LLM 지시문 + HTML 템플릿). **브라우저 런타임과 CSS 렌더 결과는 자동
검증 대상이 아니고**, 템플릿·절차의 문자열 정합성과 자산 비퇴화만 grep 으로 검증한다 — T22 가 이미
채택한 방식이다. 런타임은 아래 「수동 확인」으로 커버한다.

### `tests/run.sh` — T22 에 하위 검증 추가 (T22-39 ~ T22-46)

새 테스트 함수를 만들지 않는다. `total_tests` 는 그대로 두고 `test_dashboard_template_integrity` 안,
T22-38 **바로 뒤**(`log_ok` 앞)에 이어 붙인다. 선행 주석(L999)과 `test_desc`(L1002)의
`T22-1~T22-38` 표기를 `T22-1~T22-46` 으로 고친다.

| ID | 검증 | 방법 | 막으려는 회귀 |
|----|------|------|--------------|
| T22-39 | 로그 카드 식별자 존재 | `grep -qF 'id="dz-log-card"'` | 식별자 없이 `:nth-of-type` 같은 구조 셀렉터로 되돌아감(설계 결정 2) |
| T22-40 | **PiP 압축 규칙 존재** | `grep -qF 'body.dz-pip #dz-log-card{display:none}'` | 압축 뷰 자체의 유실 |
| T22-41 | **로그 앵커 비충돌** | `grep -n 'id="dz-log"' \| grep -q 'dz-log-card'` 가 매칭되면 **실패** | 카드 id 를 `id="dz-log"` 로 바꾸거나 앵커를 느슨하게 만들어 `log` 절차의 줄 단위 유일성이 깨짐 |
| T22-42 | **CSS 로만 숨김** | `grep -qE '(getElementById\|querySelector)\([^)]*dz-log-card'` 가 매칭되면 **실패** | 스크립트가 카드를 DOM 에서 떼어내는 방식으로 퇴행(설계 결정 1의 3중 파손) |
| T22-43 | 로그 동기화 비퇴화 | `grep -qF 'function syncLog'` **및** `grep -qF "live.innerHTML = next.innerHTML"` | 압축을 넣으며 로그 동기화를 함께 지움 |
| T22-44 | 단계 상세 마크업·CSS | `grep -qF 'class="step-detail"'` **및** `grep -qF '.step-detail:empty{display:none}'` | 상세 span 또는 빈 값 숨김 규칙 유실(빈 회색 줄이 남는 퇴행) |
| T22-45 | **PiP 는 활성 상세만** | `grep -qF 'body.dz-pip ol.steps li:not(.active) .step-detail'` | 요청 1·2 를 잇는 규칙의 유실 → 좁은 창이 상세로 도배됨 |
| T22-46 | 상세 규약 문서화 | `head -4 \| grep -q '상세'` **및** `grep -qF 'step <n> <done\|active\|wait> ["상세"]'` **및** `grep -q '줄바꿈을 넣지 않는다'` | 절차만 있고 규약에 없어 아무도 못 씀 / 불변식 3 문구 유실 |

T22-41 · T22-42 는 **역방향 assertion**(매칭되면 실패)이다 — T22-7·T22-28·T22-35·T22-38 이 이미 쓰는 패턴이다.

T22-41 참고 구현(기존 스타일 준수):

```bash
  # T22-41: 로그 앵커 비충돌 — log 절차 0-a 의 grep 앵커 `id="dz-log"` 는 닫는 따옴표까지 포함하므로
  # id="dz-log-card" 에 걸리지 않아야 한다. 걸리면 앵커가 2줄을 매칭해 log 절차 전체가 깨진다.
  if grep -n 'id="dz-log"' "$dashboard_command_file" | grep -q 'dz-log-card'; then
    record_failure "$test_name" "T22-41: 로그 카드 id 가 log 절차의 grep 앵커와 충돌함"
    return 1
  fi
```

### 기존 테스트에 대한 영향 (전수 확인)

| 테스트 | 영향 | 근거 |
|--------|------|------|
| T22-2 (셀렉터 7종) | 계속 통과 | 셀렉터를 지우지 않고 새 id 를 동적 표에 넣지도 않는다 |
| T22-4 (필터 라디오 3종) | 계속 통과 | 라디오 마크업 미변경(카드 안에 그대로, `display:none` 은 마크업이 아니다) |
| T22-7 (`class="legend"` 부재) | 계속 통과 | 범례를 만들지 않는다 |
| T22-10 (`log-title` 블록) | 계속 통과 | 카드 `<div>` 에 id 만 붙는다 |
| T22-11 (`data-current-session="1"`) | 계속 통과 | `#dz-log` 여는 태그 미변경 |
| T22-12 (log grep 패턴) | 계속 통과 | 부분일치 패턴 유지. **T22-41 이 새 id 와의 충돌 가능성을 추가로 막는다** |
| T22-22~26 (매트릭스·선형 자산) | 계속 통과 | `<ol id="dz-steps">`·`dz-step-{n}`·`step <g>.<p>` 전부 유지, span 만 추가 |
| T22-27 (`:root` 완전 일치) | 계속 통과 | 새 CSS 는 기존 토큰과 **이미 템플릿에 있는 리터럴**(`#EAF2FB`)만 쓴다 |
| T22-29~34 (PiP 스크립트) | 계속 통과 | `<script>` 를 한 줄도 바꾸지 않는다 |
| T22-35 (`--bind 127.0.0.1`) | 계속 통과 | `serve` 절 미변경 |
| T22-36 (`argument-hint` 의 `serve`) | 계속 통과 | `serve` 를 `head -4` 안에 유지 |
| T22-37 (pip 버튼이 `.wrap` 바깥) | 계속 통과 | 이 테스트는 `commands/dashboard.md` **한 파일만** 검사한다. 그 파일에 `id="dz-updated"` 를 새로 추가하지 않으므로(현행 1회 유지) 줄 번호 비교가 그대로 성립한다. 이 PRP 문서는 검사 대상이 아니다 |
| T22-38 (`[id="dz-` 금지) | 계속 통과 | 새 셀렉터는 `#dz-log-card` 형태다 |
| T23-6 (`\| on \| off`) | 계속 통과 | `["상세"]` 를 `step` 절 안에만 넣어 맨 뒤 연속 문자열을 보존 |
| T23-11 (README `settings.local.json` ≤3) | 계속 통과 | README 추가 문장에 이 문자열을 넣지 않는다 |

### 검증 명령

```bash
cd /Users/pascal/works/personal/coding-env
bash tests/run.sh                                      # 기대: 총 N / 통과 N / 실패 0 (착수 전 기준선과 동일)
wc -l commands/dashboard.md                            # README L120 의 줄 수를 이 값으로 갱신
head -4 commands/dashboard.md | grep -c '| on | off'   # 1 (T23-6 보호)
head -4 commands/dashboard.md | grep -c 'serve'        # 1 이상 (T22-36 보호)
grep -c 'id="dz-updated"' commands/dashboard.md        # 1 (T22-37 보호)
grep -n 'id="dz-log"' commands/dashboard.md | grep -c 'dz-log-card'   # 0 (T22-41 보호)
```

> **착수 전에 기준선을 먼저 재고 시작한다** — 직전 라운드가 미커밋 상태이므로 `bash tests/run.sh` 를
> 한 번 돌려 "현재 몇 개 통과"인지 기록한 뒤 작업한다.

### 수동 확인 (자동화 불가 — Chrome 실기 필요)

| # | 절차 | 합격 기준 |
|---|------|----------|
| 1 | 새 프로젝트에서 `/dashboard init "테스트" "설계\|구현\|검수"` | 단계 목록 모양이 **오늘과 동일**(빈 상세 줄이 보이지 않음 — `:empty` 확인) |
| 2 | `step 2 active "commands/dashboard.md 폴링 스크립트 작성 중"` | 2번 단계 아래 회색 한 줄이 단계명 시작선에 정렬돼 나타남 |
| 3 | 이어서 `step 2 done` (상세 인자 없음) | 상태만 완료로 바뀌고 **상세 문구가 그대로 남음**(하위 호환) |
| 4 | 이어서 `step 2 done ""` | 상세 줄이 사라지고 레이아웃이 1번 상태로 복귀 |
| 5 | `step 3 active "<T> 제네릭 & 이스케이프 확인"` | `<`·`&` 가 텍스트로 그대로 보이고 마크업이 깨지지 않음 |
| 6 | 80자짜리 상세를 넣는다 | 한 줄로 말줄임(…) 처리되고 `<li>` 가 두 줄로 쪼개지지 않음 |
| 7 | `grep -c 'dz-step-.*class="done"'` 로 감사 재계수 | 진행률과 일치(불변식 2 비파손 확인) |
| 8 | `/dashboard serve` → `http://localhost:8791/…` → 「플로팅」 | **로그 카드가 안 보이고** 제목·진행률·단계 목록·갱신 시각만 표시 |
| 9 | 8번 상태에서 화면 확인 | active 단계에 배경 강조가 들어가고, **active 단계의 상세만** 보임(완료 단계 상세는 숨김) |
| 10 | 8번 상태에서 `step 3 active "새 상세"` | 5초 내 플로팅 창의 상태·상세가 함께 갱신됨(`outerHTML` 동기화 확인) |
| 11 | 플로팅 창을 닫는다 | opener 로 복귀하며 **로그 카드가 내용 그대로 다시 보임**(데이터 무결성 — 이번 라운드의 핵심 제약) |
| 12 | 11번 직후 `log impl "복귀 후 로그"` | 5초 내 로그 항목이 정상 추가됨(`syncLog` 비퇴화) |
| 13 | 「구현」 유형 필터를 고른 뒤 플로팅 진입 → 종료 | 필터 선택이 유지됨(라디오가 DOM 에 살아 있음) |
| 14 | 플로팅 중 다른 세션에서 `/dashboard init` | 창이 스스로 닫히고 opener 가 리로드되어 새 세션 탭 표시 — **무한 리로드가 발생하지 않음**(설계 결정 1의 핵심 검증) |
| 15 | 매트릭스 세션(`init "…" "A:설계,구현\|B:설계,구현"`)에서 `step 2.1 done "상세"` | 칸 상태는 갱신되고 **"상세는 무시했다"는 한 줄 보고**가 나옴 |
| 16 | 15번 상태에서 플로팅 진입 | 표가 그대로 렌더되고 로그 카드만 숨김(매트릭스 비회귀) |
| 17 | 기존 대시보드(상세 span 없는 `<li>`)에 `step 1 done "상세"` | span 이 새로 삽입되고 정상 표시(마이그레이션 경로) |
| 18 | `file://` 로 연다 | 로그 카드가 **정상적으로 보임**(`body.dz-pip` 미매칭 확인). 포커스 시 리로드 동작 유지 |

---

## 구현 순서

각 단계 뒤에 `bash tests/run.sh` 로 비회귀를 확인한다.

### Task 1 — 데이터 모델·규격 문서 갱신
- **ACTION**: `commands/dashboard.md` 의 「데이터 모델」 동적 셀렉터 표 `#dz-step-{n}` 행 갱신,
  정적 요소 표에 `#dz-log-card` 행 추가, 불변식 3·4 추가.
- **MIRROR**: 기존 표 형식과 「불변식」 서술 스타일(현행 L79~88).
- **GOTCHA**: 셀렉터 **개수는 7 그대로**다. `.step-detail` 은 class 이고 `#dz-log-card` 는 정적 요소다.
- **VALIDATE**: 표 행 수가 7 인지 눈으로 확인. `bash tests/run.sh`.

### Task 2 — 템플릿 `<style>` 수정
- **ACTION**: `ol.steps li` 규칙에 `flex-wrap:wrap;row-gap:3px` 덧붙이기, `.step-detail` 2줄 추가
  (`li.active .chip` 뒤), `body.dz-pip` 4줄 추가(`<style>` 맨 끝).
- **GOTCHA**: 새 색 토큰 금지. `#EAF2FB` 는 이미 파일에 있는 리터럴이다. `:root` 줄을 건드리지 않는다.
- **VALIDATE**: `git diff` 로 `:root` 줄 무변경 확인(T22-27). `bash tests/run.sh`.

### Task 3 — 템플릿 마크업 + 헤더 주석 맵
- **ACTION**: 두 번째 `<div class="card">` 에 `id="dz-log-card"` 추가. 헤더 주석 맵 2곳 갱신.
- **GOTCHA**: **첫 번째** 카드(제목/진행률)가 아니라 **두 번째** 카드다. `id="dz-updated"` 를 새로 만들지 않는다.
- **VALIDATE**: `grep -c 'id="dz-updated"'` = 1, `grep -n 'id="dz-log"' | grep -c 'dz-log-card'` = 0.

### Task 4 — `init` 6단계 `<li>` 마크업에 빈 span 추가
- **ACTION**: 선형 `<li>` 템플릿 끝(`</li>` 앞)에 `<span class="step-detail"></span>` 추가.
- **GOTCHA**: span 안에 **공백 한 칸도 넣지 않는다**(`:empty` 매칭이 풀린다). `<li>` 는 여전히 한 줄.
- **VALIDATE**: 마크업 블록이 한 줄인지 확인. `bash tests/run.sh`.

### Task 5 — `step` 절차 1단계 개정 + 호출 규약 + frontmatter + 트리거 각주
- **ACTION**: 위 「인터페이스」의 `step` 절차 1단계를 그대로 반영. 호출 규약 2줄, `argument-hint`,
  트리거 표 각주 1줄.
- **GOTCHA**: 0·2·3단계는 건드리지 않는다. `argument-hint` 는 `| on | off` 를 맨 뒤 연속 문자열로 유지.
- **VALIDATE**: `head -4 … | grep -c '| on | off'` = 1, `head -4 … | grep -c 'serve'` ≥ 1.

### Task 6 — `tests/run.sh` 에 T22-39~46 추가
- **ACTION**: T22-38 뒤(`log_ok` 앞)에 8개 하위 검증 추가. 주석·`test_desc` 를 `T22-1~T22-46` 으로.
- **MIRROR**: 기존 `record_failure "$test_name" "T22-NN: …"` 후 `return 1` 스타일.
- **VALIDATE**: `bash tests/run.sh` → 착수 전 기준선과 동일한 통과 수.

### Task 7 — `README.md`
- **ACTION**: L120 줄 수를 `wc -l commands/dashboard.md` 실제 값으로. 설명 문단에 1~2문장
  (상세 인자 + PiP 압축 뷰).
- **GOTCHA**: 새 문장에 `settings.local.json` 을 쓰지 않는다(T23-11).
- **VALIDATE**: `bash tests/run.sh`.

### Task 8 — 수동 확인 1~18 수행

---

## 리스크와 대안

### 1. DOM 제거 방식으로 잘못 구현하면 폴링이 무한 루프에 빠진다

**가능성 중 / 영향 상.** 사용자 요청 원문이 "제거해도 될 거 같다"여서 구현자가 문자 그대로
`card.remove()` 를 넣을 유인이 실제로 있다. 그 순간 `sessionTabsChanged` 가 매 폴링마다 참이 되어
플로팅 창이 5초마다 닫히고 페이지가 리로드된다(설계 결정 1).
**완화**: PRP 에 3중 파손을 명시했고, **T22-42 가 역방향 assertion 으로 강제**하며,
수동 확인 14 가 무한 리로드 부재를 직접 확인한다.

### 2. 상세 텍스트에 줄바꿈이 들어가면 불변식 2가 깨진다

**가능성 중 / 영향 상.** `<li>` 가 두 줄로 쪼개지면 `step` 0단계의 grep 앵커가 어긋나고 감사용
`grep -c` 가 조용히 틀린 값을 낸다. 실패가 즉시 보이지 않는 종류다.
**완화**: 절차에 "줄바꿈을 넣지 않는다"를 명문화(불변식 3), T22-46 이 그 문구를 고정, 수동 확인 6·7 로 확인.
**기각한 대안**: 절차에서 개행을 `<br>` 로 치환 → 여러 줄 상세라는 새 요구를 발명하는 것이고,
CSS 말줄임과도 충돌한다.

### 3. `flex-wrap:wrap` 추가가 기존 선형 UI 레이아웃을 바꿀 수 있다

**가능성 하 / 영향 중.** `ol.steps li` 는 오래된 규칙이고 `.chip{margin-left:auto}` 와 상호작용한다.
다만 상세가 비면 `.step-detail:empty{display:none}` 으로 아이템 자체가 사라져 **줄바꿈이 일어날 이유가
없고**, 나머지 아이템 폭 합이 컨테이너를 넘지 않으므로 현행과 동일하게 렌더된다.
**완화**: 수동 확인 1 이 "오늘과 동일"을 직접 확인한다. `row-gap:3px` 로 wrap 시 간격도 못박았다.

### 4. 완료된 단계에 진행형 상세가 남아 어색하다

**가능성 상 / 영향 하.** 설계 결정 5 에서 의도적으로 수용했다.
**완화**: `""` 로 지우는 규약, 그리고 PiP 압축 뷰에서는 active 상세만 보이므로 곁눈질 화면에 뜨지 않는다.
전체 화면에서는 오히려 "이 단계에서 무엇을 했는지" 이력으로 읽힌다.

### 5. 매트릭스 세션은 상세 기능을 못 쓴다

**가능성 중 / 영향 하.** 설계 결정 4 에서 의도적으로 수용했다. 손실은 "없던 기능이 계속 없는 것"뿐이고,
같은 정보는 `#dz-log` 에 남는다. **완화**: 무시 사실을 한 줄 보고해 사용자가 조용히 속지 않게 한다.

### 6. PiP 에서 로그가 안 보여 혼란스러울 수 있다

**가능성 하 / 영향 하.** 사용자가 직접 요청한 동작이다. 창을 닫으면 즉시 전체 화면에서 보이고,
README 에 한 줄 적는다. **기각한 대안**: PiP 안에 "로그는 전체 화면에서" 안내 문구 추가 →
마크업이 늘고, 압축하자는 요청과 정면으로 모순된다.

### 7. 직전 라운드가 미커밋 상태라 기준선이 흔들릴 수 있다

**가능성 중 / 영향 중.** 누군가 `git checkout` 이나 stash 로 워킹 디렉토리를 되돌리면 이 PRP 가
전제하는 `body.dz-pip`·`#dz-pip-btn`·T22-29~38 이 통째로 사라진다.
**완화**: 구현 착수 전 `git status --short` 로 4개 파일의 미커밋 변경을 확인하고
`bash tests/run.sh` 기준선을 기록한다(구현 순서 서두).

### 8. 검토했으나 채택하지 않은 전체 대안

| 대안 | 기각 사유 |
|------|----------|
| **PiP 전용 콘텐츠를 별도로 복제해 넣기** | 동기화 코드가 두 벌이 되고 라디오 상태가 두 벌이 된다. `dashboard-pip-floating.md` 설계 결정 2 가 이미 기각한 방향 |
| **`.card:nth-of-type(2)` 구조 셀렉터로 숨기기** | 태그 순서 의존이라 카드를 추가하면 조용히 엉뚱한 카드를 숨긴다(설계 결정 2) |
| **상세를 `#dz-current` 별도 요소로** | 어느 단계의 상세인지 잃고, 완료 단계 이력이 없고, 셀렉터 표가 8행이 되고, 폴링 계약에 행이 하나 는다(설계 결정 3) |
| **`log` 의 최신 항목을 활성 단계 상세로 자동 반영** | 판단이 커맨드 안으로 들어온다. 호출 시점도 다르다 |
| **상세 인자를 `--detail "…"` 플래그로** | `log` 의 `["상세"]` 위치 인자 관례와 어긋난다. 같은 커맨드 안에 두 가지 문법이 생긴다 |
| **PiP 에서 `scrollIntoView` 로 활성 단계에 스크롤** | JS 추가 + 폴링마다 재실행 + 사용자 스크롤 탈취. 단계 4~6개에서는 스크롤 자체가 거의 없다(설계 결정 6) |
| **매트릭스 칸에도 상세를 넣기** | 칸당 ≈95px. 낱말 하나도 줄바꿈되는 폭이다(설계 결정 4) |

---

## 워크플로우 경로 판정

**전체 경로(설계 → 구현 → 검수)가 맞다.** 근거는 각각 단독으로 충족한다:

- **공개 인터페이스 변경**: `step` 에 네 번째 인자 추가, `argument-hint` 변경.
- **데이터 모델 변경**: 동적 셀렉터 표의 `#dz-step-{n}` 행 확장, 정적 요소 `#dz-log-card` 신설,
  불변식 3·4 추가.
- **파일 3개 이상**: `commands/dashboard.md`, `tests/run.sh`, `README.md`.
- **새 외부 의존성**: 없음.
- **민감 영역**: 아님(네트워크·권한·삭제 없음). **검수는 sonnet 기본으로 충분하다.**
  단 검수자는 "폴링 계약 3종(`syncLog`·`sessionTabsChanged`·복귀 경로) 비퇴화"를 중점 항목으로 본다.

**신뢰도(단일 패스 구현 가능성): 8/10.** 변경 지점이 문자열 수준까지 특정돼 있고 JS 를 건드리지 않는다.
감점 2는 CSS 렌더 결과(플렉스 줄바꿈·정렬)가 자동 검증 불가라 수동 확인에 의존하기 때문이다.

---

## 사용자 승인이 필요한 핵심 결정

1. **PiP 에서 로그 카드는 DOM 제거가 아니라 CSS `display:none` 으로 숨긴다.** 로그 카드에 `id="dz-log-card"`
   1개를 추가한다. 제거 방식은 폴링 무한 루프·복귀 시 로그 소실·`syncLog` 영구 정지를 동시에 일으킨다
   (설계 결정 1). 사용자가 말한 "제거"를 **시각적 제거**로 해석했다 — 맞는가?

2. **단계 상세는 선형(`#dz-step-{n}`) 모드 전용이다.** 매트릭스 모드는 칸 폭(≈95px)이 물리적으로
   부족해 제외하고, `step <g>.<p> … "상세"` 호출은 **상태만 갱신하고 상세를 무시했음을 한 줄 보고**한다
   (설계 결정 4). 매트릭스에서도 상세가 필요하면 별도 라운드로 미룬다.

3. **상세 인자를 생략하면 기존 상세가 유지되고, 지우려면 `""` 를 명시한다.** 기존 `step 3 done` 호출의
   결과가 오늘과 완전히 같아지는 대신, 완료 단계에 진행형 문장이 남을 수 있다(설계 결정 5).

4. **PiP 압축 뷰에서는 활성 단계의 상세만 보이고, 전체 화면에서는 모든 단계의 상세가 보인다.**
   두 요청을 잇는 규칙이다. 전체 화면의 완료 단계 상세를 "이력"으로 남기는 것에 동의하는가?
   (전부 숨기는 쪽을 원하면 CSS 1줄로 바꿀 수 있다.)

5. **`README.md` L120 의 stale 한 줄 수(913 → 실제 924+)를 이번에 함께 바로잡는다.** 직전 라운드에서
   누락된 값이며 어차피 같은 줄을 고친다. 이번 변경 범위 밖의 수정이므로 명시적으로 확인한다.
