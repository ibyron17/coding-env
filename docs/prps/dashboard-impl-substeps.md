# 대시보드 「구현」 단계 전용 서브태스크 패널 (PRP)

## 요구사항 요약

`/dashboard` 에 **「구현」 매크로 단계 하나에만 붙는 고정 서브태스크 패널**을 추가한다. 전체 경로
워크플로우에서 「구현」은 유일하게 여러 서브에이전트가 **병렬로** 뛰는 단계인데, 현재 대시보드는 그
구간을 `#dz-step-3` 이라는 칸 하나(`진행중`)로만 표현한다. 실제로는 그 안에서 10~15개의 세부 작업이
돌고 그중 둘 이상이 동시에 활성인데, 곁눈질 화면에는 몇 시간 동안 같은 화면이 떠 있다. 이 패널은
그 구간을 **번호가 붙은 세부 작업 목록 + 항목별 상태(done/active/wait)** 로 펼쳐 보여준다.

계기는 사용자가 보여준 외부 샘플(`~/Desktop/m7-progress-dashboard.html`)의 두 번째 패널
(`#impl-stages`, 14개 항목 중 2개가 동시에 `active`)이다. **그 파일은 우리 `/dashboard` 가 만든 것이
아니고 셀렉터 명명 체계도 완전히 다르다** — 형태만 참고하고 마크업·CSS·절차는 전부 이 저장소의
기존 관례로 다시 짓는다.

### 사용자와 합의된 확정 사항 (재논의 대상 아님)

이 PRP 는 아래 5가지를 **전제**로 그 위의 구체 설계만 담는다.

1. **스코프를 「구현」 단계 하나로 고정한다.** 임의의 단계에 하위 목록을 붙이는 범용 메커니즘을
   만들지 않는다. 설계(design-architect 1회)·사용자 승인(1회)·검수(code-reviewer 1회)는 원자적
   단일 행위라 쪼갤 것이 없다. 병렬 지점은 「구현」뿐이다(YAGNI).
2. **데이터 출처의 한계를 설계·문서에 명시한다.** 갱신 주체는 여전히 메인 세션뿐이며
   (`session-dashboard.md` 「4. 메인 세션만 갱신한다」), 서브태스크 상태는 오케스트레이터가 **직접
   아는 두 시점**에만 바뀐다: 디스패치(→`active`), 결과 보고 수령(→`done`).
   "서브에이전트 내부의 실시간 진행률"로 오인되지 않게 문서와 화면 양쪽에 적는다.
3. **비어 있으면 CSS 만으로 카드 전체가 자동으로 숨는다.** `init` 이 조건부로 마크업을 빼는 방식이
   아니라 `.step-detail:empty{display:none}` 과 같은 계열의 "내용이 비면 CSS 가 알아서 숨긴다"
   패턴을 재사용한다. 이 프로젝트는 Document PiP 때문에 이미 Chromium 만 지원 대상이므로
   `:has()` 를 안전하게 쓸 수 있다.
4. **갱신 메커니즘을 새로 만들지 않는다.** `step` 이 쓰는 grep-앵커 + Edit 방식(파일 전체를 읽지 않고
   대상 줄만 grep 해 그 전문을 `old_string` 으로 삼는다)을 그대로 적용한다. 토큰·복잡도 예산이 실제
   관심사다.
5. **기존 불변식·셀렉터 계약을 깨지 않는다.** 불변식 1(선형/매트릭스 비공존)과 충돌하지 않아야 하고,
   기존 「동적(치환 대상) — 7 셀렉터」 표는 **한 글자도 바꾸지 않는다**(새 셀렉터만 추가).
   회귀 테스트는 `tests/run.sh` 의 `test_dashboard_template_integrity`(T22) 에 T22-48 부터 이어 붙이고
   `total_tests=22` 는 유지한다.

### 이 PRP 의 위치 — 선행 라운드와의 관계

| 문서 | 상태 | 이 PRP 와의 관계 |
|------|------|-----------------|
| [`session-dashboard.md`](./session-dashboard.md) | 커밋됨 | 원형. "DOM 자체가 상태" · **「4. 메인 세션만 갱신한다」가 이번 설계의 가장 강한 제약** |
| [`dashboard-group-matrix.md`](./dashboard-group-matrix.md) | 커밋됨 | grep-앵커 방식·불변식 1·"새 메커니즘을 안 만든다"의 선례. 확정 방향 6(서술형 요소 스코프 아웃)의 경계를 이번에도 지킨다 |
| [`dashboard-pip-floating.md`](./dashboard-pip-floating.md) | 커밋됨 | 폴링 동기화 계약. **이번 라운드는 이 계약에 행을 하나 추가한다(설계 결정 5)** |
| [`dashboard-step-detail-pip-compact.md`](./dashboard-step-detail-pip-compact.md) | 커밋됨 | `.step-detail` 중첩 span · `:empty` CSS 숨김 · PiP 압축 스코핑의 정확한 선례. **이번 설계는 이 세 자산을 그대로 재사용한다** |

> **기준선은 커밋된 HEAD(`1f3e3c2`)다.** 워킹 디렉토리는 clean 이고
> `commands/dashboard.md` 는 974줄, `tests/run.sh` 는 1605줄(T22-1~T22-47)이다.
> 오늘 반영된 PiP 창 리사이즈 수정(`resizePipToFit()`, T22-47)이 포함된 상태를 전제한다.

### 사용자 스토리

> 전체 경로 작업을 돌리는 개발자로서, 「구현」 단계에 들어가 서브에이전트 두셋을 병렬로 붙여 놓은 뒤
> 화면 구석의 플로팅 창만 흘깃 보고 **"지금 어떤 세부 작업 둘이 돌고 있고, 남은 게 몇 개인지"**를
> 알고 싶다. 지금은 `3 구현 [진행중]` 한 줄이 두 시간 동안 그대로 떠 있어 곁눈질의 정보량이 0이다.

### 복잡도

**Medium** — 3파일. 신규 자산은 **CSS 1줄 추가 + 기존 CSS 1줄에 셀렉터 1개 추가 + 마크업 5줄 +
JS 함수 1개(5줄) + 절차 1개 절 신설 + 테스트 11개.** 새 모듈·새 레이어·새 의존성·새 색 토큰 없음.
레이아웃 CSS 는 **한 줄도 새로 쓰지 않는다**(설계 결정 2).

---

## 스코프와 비목표

### 스코프 안

- 「구현」 단계 전용 고정 슬롯 1개. 카드 1장, 세부 작업 목록 1개, 세부 진행 카운터 1개.
- 항목 상태 3종(`done`|`active`|`wait`)과 **동시 다중 `active`**.
- 항목별 한 줄 상세(기존 `.step-detail` 재사용). 담당 서브에이전트 표기는 이 상세 문자열 안에 넣는다.
- 목록이 비면 카드 자동 숨김(CSS `:has()`).
- 폴링 동기화 대상에 카드 추가(안 하면 브라우저에서 영구히 stale).

### 비목표 (명시적으로 만들지 않는 것)

| 제외 항목 | 사유 |
|-----------|------|
| **임의 단계에 하위 목록을 붙이는 범용 메커니즘** | 확정 사항 1. `step <n>` ↔ 슬롯 매핑, 슬롯 개수 관리, 슬롯별 셀렉터 네임스페이스가 전부 따라온다. 병렬 지점은 「구현」뿐이다 |
| **서브에이전트가 대시보드를 직접 갱신** | 확정 사항 2. `session-dashboard.md` 설계 결정 4를 정면으로 위반한다(별도 컨텍스트·파일 경합) |
| **서브에이전트 내부 실시간 진행률(%)** | 오케스트레이터가 알 수 없는 값이다. 알 수 없는 값을 그려내면 대시보드 전체의 신뢰가 깨진다 |
| **세부 작업 간 의존 관계·DAG 시각화** | 목록은 평면이다. 의존 관계는 항목 이름·상세 문자열로 충분하다(샘플도 "2·3과 병렬 가능"을 텍스트로 적었다) |
| **세부 작업이 매크로 진행률에 반영되는 것** | 불변식 6. 분모가 두 체계로 갈라지면 `step` 2단계의 전이(±1) 산술이 무너진다 |
| **세부 작업별 소요 시간·경과 타이머** | 새 스크립트가 필요하고, 이 커맨드는 "기계적 치환만" 이 대전제다 |
| **담당 에이전트를 위한 별도 필드(`data-agent`)** | 소비자(CSS·JS·절차)가 하나도 없다. 상세 문자열에 "에이전트 C" 를 적으면 끝난다(YAGNI) |
| **`impl clear` / 목록 재정의** | 아래 리스크 3. `init` 가드와 같은 철학으로 "파일을 지우고 다시 시작" 안내로 처리한다 |
| **매트릭스 모드에서 그룹별 서브태스크** | 목록은 세션당 하나다. 그룹 귀속은 항목 이름에 적는다(설계 결정 7) |

---

## UX 전환

### Before

```
┌── 세션 진행 상황 ──────────────────────────────┐
│ ▇▇▇▇░░░░  2/4 · 50%                            │
│ 1 설계                              [완료]     │
│ 2 승인                              [완료]     │
│ 3 구현                            [진행중]     │   ← 두 시간 동안 이 한 줄
│     서브에이전트 2개 병렬 진행 중              │      (상세 한 줄이 전부)
│ 4 검수                              [대기]     │
└────────────────────────────────────────────────┘
```

### After

```
┌── 세션 진행 상황 ──────────────────────────────┐
│ ▇▇▇▇░░░░  2/4 · 50%                            │
│ 1 설계                              [완료]     │
│ 2 승인                              [완료]     │
│ 3 구현                            [진행중]     │
│ 4 검수                              [대기]     │
└────────────────────────────────────────────────┘
┌── 구현 세부 작업 · 디스패치와 결과 보고 시점에만 갱신 ──┐
│ 3/8 · 38%                                      │
│ 1 path-guard 순수 로직            [완료]       │
│     14케이스 + 22테스트 통과                   │
│ 2 파일 I/O + 트리                 [진행중]     │   ← 둘이 동시에 진행중
│     에이전트 C — config 상수 소유              │
│ 3 읽기 라우트                     [대기]       │
│ 4 변경 배지 파이프라인            [진행중]     │   ← 병렬. 이 패널의 존재 이유
│     에이전트 D — P2 실측 완료                  │
│ …                                              │
└────────────────────────────────────────────────┘
┌── 작업 추적 ──────────────────────────────────┐ (그대로)
```

목록이 비어 있으면(=`impl set` 을 부른 적이 없는 모든 세션) **두 번째 카드가 통째로 렌더되지
않는다.** 오늘과 화면이 완전히 동일하다.

### 상호작용 변화

| 접점 | Before | After | 비고 |
|------|--------|-------|------|
| `init` (선형/그룹 모두) | 카드 2장 | **동일** (impl 카드는 비어 있어 CSS 가 숨김) | 절차 무변경 |
| `step <n>` / `step <g>.<p>` | 매크로 단계 갱신 | **동일** | 한 글자도 안 바뀐다 |
| `impl set "<작업1\|…>"` | (없음) | 세부 작업 목록 정의, 전부 `wait` | 신규 |
| `impl <k> active ["상세"]` | (없음) | 그 항목만 진행중 + 세부 카운터 갱신 | 신규. 동시 다중 허용 |
| `impl <k> done ["상세"]` | (없음) | 그 항목만 완료 + 세부 카운터 갱신 | 신규 |
| 매크로 진행률 | `step` 만 바꾼다 | **동일** (`impl` 은 절대 안 건드린다) | 불변식 6 |
| `log` / 세션 탭 / 유형 필터 | — | **동일** | 한 글자도 안 바뀐다 |
| PiP 압축 뷰 | 로그 카드만 숨김 | **동일 규칙 + impl 카드는 보인다** | 설계 결정 6 |
| `file://` 로 열기 | 포커스 시 리로드 | **동일** | 회귀 없음 |

---

## 영향 범위

### 수정 파일 (3개)

| 파일 | 수정 내용 | 이유 |
|------|----------|------|
| `commands/dashboard.md` | ① frontmatter `argument-hint` ② 「호출 규약」 2줄 ③ 「CLAUDE.md 트리거」 표 3행 + 각주 ④ 「데이터 모델」에 **새 하위 절** 신설(기존 7 셀렉터 표는 무수정) + 정적 요소 표 1행 + 불변식 5·6 ⑤ 「폴링 동기화 계약」 표 1행 ⑥ **`impl` 절차 절 신설** ⑦ 템플릿: 헤더 주석 맵 4줄 · `<style>` 1줄 추가 + 1줄 수정 · `<body>` 5줄 추가 · `<script>` 함수 1개 + 호출 1줄 | 이 커맨드가 유일한 실행 주체다. 템플릿·절차·규약이 전부 이 한 파일에 있다 |
| `tests/run.sh` | T22 에 하위 검증 **T22-48 ~ T22-58** 추가, 선행 주석(L998)과 `test_desc`(L1001)의 `T22-1~T22-47` → `T22-1~T22-58` | 이 저장소의 회귀 방지 수단은 grep 기반 문서 정합성 테스트뿐이다 |
| `README.md` | L120 커맨드 표의 줄 수(**현재 959 는 이미 stale — 실제 974**), L139~152 설명 문단에 2~3문장 | 줄 수는 실제 값이어야 하고, 사용자가 `impl` 하위 명령의 존재를 알 경로가 README 뿐이다 |

> **README L120 의 stale 줄 수는 직전 라운드에서 또 누락됐다**(913→924 로 고쳤어야 할 자리가 959 로
> 갱신됐으나 현재 실제는 974). 어차피 같은 줄을 고치므로 함께 바로잡는다(승인 항목 6).

### 미영향 (전수 확인)

| 대상 | 확인 근거 | 결과 |
|------|----------|------|
| **불변식 1** (`#dz-step-*` ↔ `#dz-cell-*` 비공존) | 새 셀렉터는 `dz-impl-` **독립 네임스페이스**다. `#dz-impl-tasks` 는 `#dz-steps`/`#dz-matrix` 중 어느 쪽과도 형제가 아니라 **다른 카드 안**에 있고, `syncVisualization` 의 `querySelector('#dz-steps,#dz-matrix')` 에 매칭되지 않는다 | **충돌 없음.** 선형·매트릭스 어느 세션에서도 그대로 동작 |
| 기존 「동적 — 7 셀렉터」 표 | 새 셀렉터는 **별도 하위 절**에 넣는다. 기존 7행의 이름·의미·개수를 건드리지 않는다 | **7 유지** (T22-2 무영향) |
| `step` 절차 0~3단계 | `impl` 은 자기 grep 앵커(`id="dz-impl-…"`)만 본다. 두 절차가 같은 줄을 다투지 않는다 | 한 글자도 안 바뀐다 |
| `log` 절차의 앵커 `grep -n 'id="dz-log"'` | 새 id 중 `dz-log` 를 **닫는 따옴표까지 포함해** 매칭하는 것은 없다(`dz-impl-card`·`dz-impl-tasks`·`dz-impl-count`·`dz-impl-{k}`) | 앵커 유일성 유지 (T22-50) |
| `#dz-subtitle` 과의 접두어 충돌 | 후보였던 `dz-sub-{k}` 는 `id="dz-sub` 로 느슨하게 grep 하면 `id="dz-subtitle"` 에 걸린다. **그래서 `dz-impl-` 로 정했다**(설계 결정 1) | 충돌 없음 |
| `#dzf-impl` (유형 필터 라디오) | 문자열 `dz-impl` 은 `dzf-impl` 에 포함되지 않는다(`dzf` vs `dz-`) | 충돌 없음 |
| `sessionTabsChanged(fresh)` | 새 카드 안에 `input[name="dzs"]` 이 하나도 없다 | 정상 동작 |
| `resizePipToFit()` | `apply()` 끝에서 `.wrap` 실제 높이를 다시 잰다. impl 카드가 나타나/사라져도 자동 반영 | 정상 동작 (→ 리스크 4) |
| 감사용 `grep -c 'dz-step-.*class="done"'` | impl 항목 줄에는 `dz-step-` 이 없다 | 매크로 계수 불변 |
| T22-37 (pip 버튼이 `.wrap` 바깥) | `id="dz-updated"` 를 **새로 추가하지 않는다**(현행 1회 유지). 새 카드는 `#dz-updated` **앞**에 들어간다 | 계속 통과 |
| T22-38 (`[id="dz-` 금지) | 새 CSS 는 `#dz-impl-card:not(:has(#dz-impl-tasks li))` 형태다 | 계속 통과 |
| T22-10 (`class="log-title"`) | 마크업의 `<div class="log-title">작업 추적</div>` 은 그대로다. CSS 규칙에 셀렉터 하나가 추가될 뿐 | 계속 통과 |
| T22-24 (`<ol class="steps" id="dz-steps">`) | 그 줄은 그대로다. impl 목록은 **다른 줄**에 `<ol class="steps" id="dz-impl-tasks">` 로 존재 | 계속 통과 |
| T22-27 (`:root` 완전 일치) | 새 색 토큰을 만들지 않는다. `:root` 줄을 건드리지 않는다 | 계속 통과 |
| T23-6 (`\| on \| off`) | `impl` 항목을 `serve` 앞에 넣어 맨 뒤 연속 문자열을 보존한다 | 계속 통과 |
| T23-11 (README `settings.local.json` ≤3) | 추가 문장에 이 문자열을 넣지 않는다 | 계속 통과 |
| `install.sh` 의 `COMMANDS_FILE_COUNT=8` | 새 커맨드 파일을 만들지 않는다 | 변경 불필요 |
| 프로젝트 `CLAUDE.md` | `/dashboard` 언급은 "착수 시 `init`, 단계 전환마다 `step`·`log`" 수준이고 `impl` 은 **선택**이라 트리거 규약이 바뀌지 않는다 | **수정하지 않는다** |

### 손대지 않는 것 (발견했지만 이번 요청과 무관)

- `commands/dashboard.md` L657 의 `~/Desktop/dashboard.html` 원본 경로 언급. 세 라운드 연속으로
  "언급만 하고 지우지 않는다"로 처리됐다. 이번에도 동일.
- `init` 가드의 알려진 한계("완전히 새 작업"과 "기존 작업 재개"를 구분하지 못한다). 이번 기능이
  같은 한계를 물려받지만(리스크 3), 가드 자체를 손보는 것은 별도 요청이다.

---

## 필독 파일 (구현 착수 전)

| 우선순위 | 파일 | 범위 | 이유 |
|---------|------|------|------|
| P0 | `commands/dashboard.md` | L732~806 (`<style>`) · L808~828 (`<body>`) | 유일한 수정 본체. 특히 L754~755 `.step-detail`, L792 `.log-title`, L798~805 `body.dz-pip`, L818 `#dz-log-card` |
| P0 | `commands/dashboard.md` | L409~465 (`step` 절차) | `impl` 절차가 **문장 구조까지 그대로 베낄** 원본. grep 앵커 + 전이(±1) 산술 |
| P0 | `commands/dashboard.md` | L52~183 (데이터 모델 + 폴링 동기화 계약) | 셀렉터 계약·불변식 1~4·grep 유일성 불변식. **무엇을 건드리면 안 되는지의 근거** |
| P0 | `commands/dashboard.md` | L862~909 (`<script>` 의 sync 함수 4종 + `apply()`) | 5번째 sync 함수를 붙일 자리와 그 스타일 |
| P0 | `tests/run.sh` | L1336~1425 (T22-39~47 + 함수 말미) | 새 하위 검증을 붙일 자리와 assert 스타일(`record_failure` 후 `return 1`) |
| P1 | `docs/prps/session-dashboard.md` | L417~424 (설계 결정 4) | "메인 세션만 갱신한다"의 원문 근거. 확정 사항 2의 출처 |
| P1 | `docs/prps/dashboard-step-detail-pip-compact.md` | L291~333 (마크업·CSS), L500~512 (역방향 테스트 예시) | `.step-detail`·`:empty`·PiP 스코핑의 정확한 선례와 테스트 문체 |
| P2 | `~/Desktop/m7-progress-dashboard.html` | L103~118 | **형태만** 참고(항목 = 번호 + 이름 + 상태 + 노트). 셀렉터·CSS 는 베끼지 않는다 |
| P2 | `README.md` | L111~152 | 커맨드 표와 설명 문단의 문체 |

### 외부 조사

**필요 없음 — 새로 쓰는 CSS 기능은 `:has()` 하나뿐이다.**

| 주제 | 근거 | 핵심 |
|------|------|------|
| `:has()` 관계 의사 클래스 | CSS Selectors Level 4. Chrome/Edge 105+ 지원 | 이 프로젝트는 `documentPictureInPicture in window` 로 이미 Chromium 만 지원 대상으로 못 박았고(플로팅 미지원 시 버튼 비활성 + 사유 표시), **폴백 경로에서도 `:has()` 미지원의 결과는 "빈 카드가 보인다" 뿐**이라 치명적이지 않다 |
| `:empty` 를 쓰지 않는 이유 | CSS Selectors Level 3 | `:empty` 는 **공백·줄바꿈 하나에도 매칭이 풀린다**. 템플릿의 빈 목록은 `<ol …>` 과 `</ol>` 이 두 줄로 나뉘어 있어(기존 `#dz-steps` 와 동일한 형태) 내부에 개행 텍스트 노드가 있다 → `:empty` 는 **절대 매칭되지 않는다**. `:has(li)` 는 개행에 영향받지 않는다(설계 결정 3) |

**새 npm/pip 의존성 없음. 새 색 토큰 없음. 새 CSS 레이아웃 규칙 없음.**

---

## 데이터 모델

### 개념 모델

```
ImplTaskList                     # 세션당 0개 또는 1개. 「구현」 매크로 단계에 종속된 고정 슬롯
  tasks : ImplTask[]             # 순서 있음. len == 0 이면 카드가 CSS 로 숨는다
ImplTask
  index  : int                   # 1부터. #dz-impl-{k} 의 k 이자 화면에 보이는 번호
  name   : string                # impl set 이 넣고 이후 치환하지 않는다
  state  : "done" | "active" | "wait"     # active 가 동시에 여러 개일 수 있다
  detail : string                # 기존 .step-detail 재사용. 빈 문자열이면 "없음". 줄바꿈 불가
```

- **`active` 가 동시에 여러 개일 수 있다. 이것이 이 패널의 존재 이유다.** 매크로 `step` 과 달리
  단일 활성 가정을 두지 않으며, 개수를 검증하거나 강제하지 않는다.
- `state` 어휘는 매크로 단계와 **글자 하나까지 같다**(`done`/`active`/`wait`, `완료`/`진행중`/`대기`).
  `na` 는 없다 — 자리 채움이 필요한 직사각형 표가 아니다.
- `detail` 에 담당 서브에이전트를 적는다(예: `"에이전트 C — config 상수 소유"`). 별도 필드를 만들지 않는다.

### 이 패널이 보여주는 것과 **보여주지 않는 것** (확정 사항 2)

| 갱신 시점 | 누가 아는가 | 반영 |
|----------|-----------|------|
| 서브에이전트를 **디스패치하는 순간** | 오케스트레이터(자기가 부른다) | `impl <k> active` |
| 서브에이전트가 **결과를 보고해 온 순간** | 오케스트레이터(자기가 받는다) | `impl <k> done` |
| 그 사이 서브에이전트 내부의 진행 상황 | **아무도 모른다** | **반영하지 않는다** |

> 이 패널은 **오케스트레이터가 직접 관측한 두 시점만** 기록한다. `active` 로 표시된 항목이 "얼마나
> 진행됐는지"는 **서브에이전트 내부의 실시간 진행률이 아니다** — 디스패치 이후 아직 결과 보고가
> 오지 않았다는 뜻일 뿐이다. 서브에이전트가 이 파일에 접근하지 않는다는 원칙
> (`session-dashboard.md` 설계 결정 4)은 이 기능에서도 예외 없이 유지된다.

이 문단은 `commands/dashboard.md` 의 `impl` 절 서두에 **그대로** 들어가고, T22-55 가 문구를 고정한다.

### 셀렉터 — 기존 표는 무수정, 새 하위 절만 추가

**「동적(치환 대상) — 7 셀렉터」 표는 한 글자도 바꾸지 않는다.** 아래를 그 표 다음에 새 하위 절로 넣는다.

#### 구현 세부 작업 슬롯 — 동적 2 + 구조 1 (기본 상태: 비어 있음)

| 셀렉터 | 치환 대상 | 값 | 치환 주체 |
|--------|----------|-----|----------|
| `#dz-impl-{k}` | `class` 속성 + 자식 `.chip` 텍스트 + 자식 `.step-detail` 텍스트 | `done`\|`active`\|`wait` / `완료`\|`진행중`\|`대기` / 한 줄 상세(빈 문자열 허용) | `impl <k>` |
| `#dz-impl-count` | 텍스트 | `3/8 · 38%` (매크로 진행률과 **같은 형식**) | `impl set` · `impl <k>` |
| `#dz-impl-tasks` | 자식 `<li>` 목록 | `impl set` 이 **한 번만** 채운다. 이후 항목 단위로만 갱신한다 | `impl set` |

**정적 요소 표에 1행 추가** (`init`/`step`/`log` 가 절대 치환하지 않는 요소):

| 요소 | 역할 |
|------|------|
| `#dz-impl-card` | 「구현 세부 작업」 카드 `<div>`. 템플릿에 **항상** 존재하며, 목록이 비면 CSS `:has()` 가 통째로 숨긴다. DOM 에서 제거하지 않는다 |

> `#dz-impl-card` 는 정적 요소지만 **폴링 동기화 대상**이다(설계 결정 5). `#dz-log-card` 와 다른 점이
> 여기다 — 로그 카드는 안쪽 `#dz-log` 만 동기화되지만, impl 카드는 안쪽에 별도 동기화 대상이 둘
> (`#dz-impl-tasks`·`#dz-impl-count`)이라 카드째 교체가 더 싸다.

### 불변식 추가 (기존 1~4에 이어)

5. **`<li id="dz-impl-…">` 는 반드시 한 줄에 하나씩 생성한다.** 불변식 2와 같은 이유다 — `impl`
   절차의 grep 앵커와 감사용 `grep -c 'dz-impl-.*class="done"'` 가 줄 단위 계수에 의존한다.
   상세에 줄바꿈을 넣지 않는 규칙(불변식 3)도 그대로 적용된다.
6. **구현 세부 작업의 상태 변화는 매크로 진행률을 바꾸지 않는다.** `impl` 절차는
   `#dz-progress-bar` 와 `#dz-progress-pct` 를 **읽지도 쓰지도 않는다.** 세부 진행은
   `#dz-impl-count` 만 쓴다. 두 분모를 섞으면 `step` 2단계의 전이(±1) 산술이 무너진다.

### 폴링 동기화 계약 — 1행 추가

| 대상 | 동기화 연산 | 근거 |
|------|------------|------|
| `#dz-impl-card` | `outerHTML` 대입 | 카드 안에 라디오·`<details>` 같은 **사용자 상태가 하나도 없어** 통째 교체가 안전하다. 항목·카운터를 따로 동기화하면 함수가 둘로 늘고, 목록 길이가 바뀌는 `impl set` 순간을 별도로 처리해야 한다 |

---

## 인터페이스

### 호출 규약 (2줄 추가)

```
/dashboard init "<제목>" "<단계1|단계2|...>"
/dashboard init "<제목>" "<그룹A:단계1,단계2|그룹B:단계1,단계2>"
/dashboard step <n> <done|active|wait> ["상세"]    # 그룹 1개 — 단계 번호. 상세는 선택
/dashboard step <g>.<p> <done|active|wait>         # 그룹 2개 이상 — 행.열 (상세 미지원)
/dashboard impl set "<작업1|작업2|...>"            # 「구현」 단계의 세부 작업 목록을 한 번 정의
/dashboard impl <k> <done|active|wait> ["상세"]    # 세부 작업 1개 갱신. active 동시 다중 허용
/dashboard log <impl|pass|fail|commit> "<한 줄 요약>" ["상세"] [--round N]
/dashboard serve [포트] | serve stop
/dashboard on | off
```

`impl` 의 첫 인자가 `set` 이면 목록 정의, 정수면 항목 갱신이다. `step` 이 `.` 유무로 두 형태를
가르는 것과 같은 **단일 토큰 판정**이다.

> **`log impl` 과 혼동하지 않는다.** `impl` 은 **첫 번째** 토큰일 때만 이 기능이고,
> `log` 다음에 오는 `impl` 은 로그 항목의 종류다. 위치가 다르므로 판정에 모호함이 없다.

### frontmatter

```yaml
argument-hint: "init \"<제목>\" \"<단계1|...> 또는 <그룹A:단계1,단계2|그룹B:...>\" | step <n>|<g>.<p> <done|active|wait> [\"상세\"] | impl set \"<작업1|...>\"|<k> <done|active|wait> | log <impl|pass|fail|commit> \"<요약>\" [...] | serve [포트] | on | off"
```

> **GOTCHA**: `| on | off` 를 **맨 뒤 연속 문자열로 유지**한다(T23-6 이 `head -4` 부분일치로 확인).
> `serve` 도 `head -4` 안에 남아야 한다(T22-36). `impl` 항목은 `log` **앞**에 넣는다.

### CLAUDE.md 트리거 표 — 3행 추가 + 각주 1줄

| 시점 | 호출 |
|------|------|
| 구현 착수 — 세부 작업 목록이 확정됐을 때(선택) | `impl set "<작업1\|…>"` |
| 서브에이전트를 디스패치하기 직전 | `impl <k> active ["담당·범위"]` |
| 서브에이전트 결과 보고를 받은 직후 | `impl <k> done ["결과 요약"]` |

```markdown
> `impl` 은 **선택**이다. 세부 작업이 서넛 이하이거나 병렬이 없으면 부르지 않아도 되며, 부르지
> 않으면 카드 자체가 화면에 나타나지 않는다. 갱신 시점은 오케스트레이터가 직접 아는 두 순간
> (디스패치 · 결과 보고)뿐이다 — 서브에이전트는 이 파일에 접근하지 않는다.
```

### `impl set` — 절차 (Bash 1회 + Edit 3회, 결정적)

```markdown
0. **대상 줄 3종을 한 번에 확보한다.** Bash 1회:
   ```bash
   grep -n -e 'id="dz-impl-tasks"' -e 'id="dz-impl-count"' -e 'id="dz-impl-1"' .claude/dashboard.html
   ```
   - **결과 3줄** → 이미 목록이 있다. **이미 목록이 있으면 덮어쓰지 않는다** — 진행 중인 상태를
     날린다. 보고 후 중단하고, 목록을 새로 만들려면 `.claude/dashboard.html` 을 지우고
     `/dashboard init` 부터 다시 하라고 안내한다.
   - **결과 2줄** → 정상. 1번으로 진행한다.
   - **결과 0~1줄** → 이 기능 도입 이전에 만들어진 대시보드다. 마이그레이션하지 않는다.
     보고 후 중단하고 위와 같은 안내를 한다(`.claude/dashboard.html` 은 임시 산출물이다).

1. **인자 파싱** — 두 번째 인자를 `|` 로 나눈다. 결과가 작업 목록이고 개수를 `K` 라 한다.
   그룹 문법(`:`)은 **없다** — 세부 작업 목록은 평면이다. 작업명에 `|` 를 쓰지 않는다.

2. **목록 생성** — 0에서 얻은 `id="dz-impl-tasks"` 줄 전문을 `old_string` 으로 Edit 1회.
   그 줄 **뒤에** 아래 `<li>` 를 K개 삽입한다. **전부 `wait` 이고 상세는 빈 span 이다.**
   ```html
      <li id="dz-impl-{k}" class="wait"><span class="num">{k}</span>{작업명}<span class="chip">대기</span><span class="step-detail"></span></li>
   ```
   - **한 줄에 하나씩** 쓴다(불변식 5).
   - `<span class="step-detail"></span>` 안에 **공백조차 넣지 않는다**(`:empty` 매칭이 풀린다).
   - 작업명은 `&`→`&amp;`, `<`→`&lt;`, `>`→`&gt;` 순서로 이스케이프한다.
   - **첫 항목을 `active` 로 만들지 않는다.** `init` 이 1번 단계를 active 로 두는 것과 다르다 —
     목록 정의와 디스패치는 다른 시점이고, 부르지도 않은 서브에이전트를 진행중으로 그리면
     이 패널의 유일한 신뢰 근거(관측된 두 시점만 기록)가 깨진다.

3. `#dz-impl-count` 텍스트를 `0/K · 0%` 로 Edit 1회.
4. `#dz-updated` 텍스트를 현재 시각으로 Edit 1회.
```

### `impl <k>` — 절차 (Bash 1회 + Edit 3회, `step` 과 동일 구조)

```markdown
0. **대상 줄과 세부 진행률 줄을 한 번에 확보한다.** Bash 1회:
   ```bash
   grep -n -e 'id="dz-impl-{k}"' -e 'id="dz-impl-count"' .claude/dashboard.html
   ```
   각 요소는 한 줄에 하나씩 생성되므로 결과는 항상 2줄이다.
   - 대상 줄이 안 나온다 → 인덱스가 틀렸거나 `impl set` 을 부른 적이 없다. **다른 항목을 추측해서
     고치지 말고 보고 후 중단한다.** 인덱스를 잊었으면 `grep -n 'dz-impl-[0-9]'` 로 목록 전체
     지도를 한 번에 얻는다.

1. **상태 치환** — 0에서 얻은 줄 전문을 `old_string` 으로 Edit 1회.
   `class="{state}"` 와 자식 `.chip` 텍스트를 바꾼다. `.step-detail` 갱신 규칙은
   `step` 절차 1단계의 표(생략=유지 / `"내용"`=치환 / `""`=지움)와 **완전히 동일**하다.
   이스케이프·줄바꿈 금지 규칙도 그대로 적용된다.
   - **동시에 여러 항목이 active 일 수 있다.** 다른 항목의 상태를 확인하거나 되돌리지 않는다.
     이 호출은 지정된 한 항목만 건드린다.

2. **세부 진행률 재계산** — 0에서 읽은 `#dz-impl-count` 텍스트가 `"M/K · P%"` 이므로 M·K 를 그대로
   얻는다. 완료 항목 수는 다시 세지 않고 **전이로 계산한다**(`step` 2단계와 같은 산술):
   - 이전 상태 ≠ done 이고 새 상태 = done  → M+1
   - 이전 상태 = done 이고 새 상태 ≠ done  → M-1
   - 그 외                                  → M 그대로

   P = round(M/K × 100). M 이 바뀌지 않았으면 이 단계를 건너뛴다.
   **`#dz-progress-bar`·`#dz-progress-pct` 는 건드리지 않는다**(불변식 6).

3. `#dz-updated` 텍스트를 현재 시각으로 치환한다. (Edit 1회)

**감사(audit)용 재계수** — 세부 진행률이 어긋난 것 같으면 아래로 다시 센다.
```bash
grep -c 'dz-impl-[0-9].*class="done"' .claude/dashboard.html
```
```

### 템플릿 `<style>` — 기존 1줄 수정 + 1줄 추가

**수정** (현행 L792. 셀렉터 하나만 덧붙인다 — 새 규칙을 복제하지 않기 위해서다):

```css
  .log-title,.impl-title{font-size:13px;font-weight:700;color:var(--muted);margin:0 0 10px}
```

**추가** — `.step-detail:empty{display:none}`(현행 L755) **바로 뒤**, `table.matrix` 앞:

```css
  #dz-impl-card:not(:has(#dz-impl-tasks li)){display:none}
```

- 배치 근거: "내용이 비면 CSS 가 숨긴다"는 같은 계열의 규칙끼리 붙여 둔다.
- `:empty` 가 아니라 `:has(li)` 인 이유는 설계 결정 3.
- **레이아웃 규칙을 새로 쓰지 않는다.** 목록에 `class="steps"` 를 주면 기존
  `ol.steps`·`ol.steps li`·`.num`·`.chip`·`.step-detail`·`li.done`·`li.active` 와
  `body.dz-pip ol.steps …` 4줄이 **전부 그대로 적용된다**(설계 결정 2).

### 템플릿 `<body>` — 5줄 추가

첫 번째 카드(`</div>` 로 닫히는 제목/진행률 카드)와 `#dz-log-card` **사이**에 넣는다.

```html
  <div class="card" id="dz-impl-card">
    <div class="impl-title">구현 세부 작업 · 디스패치와 결과 보고 시점에만 갱신</div>
    <div class="pct" id="dz-impl-count">0/0 · 0%</div>
    <ol class="steps" id="dz-impl-tasks">
    </ol>
  </div>
```

- 제목 문구에 갱신 시점을 적어 두는 것이 **화면상의 유일한 정직성 장치**다(확정 사항 2).
- `#dz-impl-count` 는 `class="pct"` 를 재사용한다(새 CSS 0줄). 순서는 매크로 카드와 동일하게
  「요약 텍스트 → 목록」이다.
- `<ol>` 과 `</ol>` 을 **두 줄로** 둔다(`#dz-steps` 와 동일). `impl set` 이 그 사이에 항목을 넣는다.

### 템플릿 `<script>` — sync 함수 1개 + 호출 1줄

`syncLog` 정의 **바로 뒤**에 추가한다(기존 4개 sync 함수와 같은 문체):

```js
  function syncImplCard(fresh){
    // 이 카드는 .wrap 안이지만 #dz-steps 동기화 대상이 아니다 — 별도 대입이 없으면 영구히 stale 된다.
    var live = wrap.querySelector('#dz-impl-card'), next = fresh.getElementById('dz-impl-card');
    if(live && next) live.outerHTML = next.outerHTML;
  }
```

`apply()` 안의 호출 순서(현행 L907)를 한 줄 바꾼다:

```js
    syncProgressBar(fresh); syncVisualization(fresh); syncImplCard(fresh); syncLog(fresh);
```

`resizePipToFit()` 은 그 뒤에 그대로 남는다 — 카드가 나타나거나 항목이 늘면 PiP 창 높이가 자동으로 다시 맞는다.

### 템플릿 헤더 주석 맵 — 4줄 추가

```
  #dz-impl-{k}          : [선택] 구현 세부 작업 li — class(done|active|wait) + .chip 텍스트
                          + .step-detail 텍스트. active 가 동시에 여러 개일 수 있다
  #dz-impl-count        : [선택] 구현 세부 진행 텍스트 (예: 3/8 · 38%). 매크로 진행률과 별개다
  #dz-impl-tasks        : [선택] 세부 작업 ol — impl set 이 한 번 채운다
```

정적 목록에 1줄 추가:

```
  #dz-impl-card              : 구현 세부 작업 카드 div. 목록이 비면 CSS :has() 가 통째로 숨긴다
                               (DOM 에서 제거하지 않는다 — 폴링이 outerHTML 로 동기화한다)
```

---

## 설계 결정과 근거

### 1. 셀렉터 접두어를 `dz-impl-` 로 한다 — `dz-sub-` 는 기존 셀렉터와 충돌한다

| 후보 | 판정 |
|------|------|
| `dz-sub-{k}` | **기각.** `#dz-subtitle` 이 이미 있다. `grep 'id="dz-sub'` 같은 느슨한 패턴이 제목 줄에 걸려 "결과는 항상 N줄" 전제가 조용히 깨진다. 이 저장소의 grep 앵커 방식과 최악의 궁합이다 |
| `dz-task-{k}` | **기각.** 이름이 스코프를 말하지 않는다. 확정 사항 1(「구현」 전용 고정 슬롯)이 이름에서 사라져, 다음 라운드에서 범용화 유혹이 생긴다 |
| `dz-impl-{k}` | **채택.** 기존 어떤 셀렉터와도 접두어가 겹치지 않고(`dzf-impl` 은 `dz-impl` 을 포함하지 않는다), 하위 명령 이름(`impl`)·카드 제목과 이름이 일치한다 |

이름이 스코프를 고정한다는 점이 중요하다. `#dz-impl-*` 라는 이름은 "이건 구현 단계의 것"이라고
마크업 자체가 말하므로, 별도의 `data-owner-step="3"` 같은 **소비자 없는 속성을 만들 필요가 없다**.

### 2. 세부 작업 목록에 `class="steps"` 를 그대로 준다 — 레이아웃 CSS 를 새로 쓰지 않는다

이번 설계에서 가장 큰 절약이다. `<ol class="steps" id="dz-impl-tasks">` 하나로 아래가 **전부 공짜**가 된다.

| 재사용 자산 | 얻는 것 |
|------------|--------|
| `ol.steps li` (flex + `flex-wrap` + `row-gap`) | 항목 한 줄 레이아웃과 상세 줄바꿈 |
| `.num` | 번호 배지. **인덱스가 화면에 보이는 것은 기능 요구다** — 호출자가 `impl <k>` 로 항목을 지정하려면 k 를 눈으로 알아야 한다 |
| `.chip`, `li.done .chip`, `li.active .chip`, `li.done`, `li.active` | 상태 색·굵기. 이 규칙들이 `ol.steps` 로 스코프되지 않은 **기존 사실** 덕분에 그대로 적용된다 |
| `.step-detail`, `.step-detail:empty` | 항목별 한 줄 상세와 빈 값 숨김 |
| `body.dz-pip ol.steps li…` 4줄 | PiP 압축 레이아웃 + active 배경 강조 + 비활성 상세 숨김 |

**중복 정의(`ol.impl-tasks{…}`)를 만들지 않는다.** 두 벌이 되면 한쪽만 고쳐 화면이 갈라진다.
T22-50 이 역방향 assertion 으로 `ol.impl-tasks` 규칙의 신설을 막는다.

**부작용 점검**: `class="steps"` 를 두 번 쓰면 `syncVisualization` 의
`querySelector('#dz-steps,#dz-matrix')` 가 헷갈릴까? **아니다 — 그 셀렉터는 id 기반이다.**
`#dz-impl-tasks` 는 매칭되지 않는다. 불변식 1(선형/매트릭스 비공존)도 id `dz-step-`/`dz-cell-`
에 관한 것이라 무관하다.

### 3. 자동 숨김은 `:has(li)` 로 한다 — `:empty` 는 이 자리에서 **작동하지 않는다**

확정 사항 3은 "`.step-detail:empty` 같은 관례를 재사용하라"였다. 관례(=내용이 비면 CSS 가 숨긴다)는
그대로 따르되, **의사 클래스는 `:empty` 를 쓸 수 없다.**

| 후보 | 판정 |
|------|------|
| `#dz-impl-tasks:empty{display:none}` | **기각.** 두 가지가 동시에 틀린다. (a) 템플릿의 빈 목록은 `<ol …>` 과 `</ol>` 이 두 줄이라 **내부에 개행 텍스트 노드가 있고**, `:empty` 는 공백 하나에도 매칭이 풀린다. (b) 목록만 숨어도 **카드 제목과 카운터는 남는다** — "카드 전체가 안 보여야 한다"는 요구를 못 맞춘다 |
| `init` 이 조건부로 카드 마크업을 뺀다 | **기각.** 확정 사항 3이 명시적으로 배제. `init` 에 분기가 생기고, `impl set` 시점에 카드를 통째로 삽입하는 별도 Edit 경로가 필요해진다 |
| 스크립트가 항목 수를 세어 `style.display` 를 대입 | **기각.** JS 상태가 늘고 폴링마다 재실행해야 한다. 이 커맨드는 "CSS 로 되는 건 CSS 로"가 원칙이다(`session-dashboard.md` 설계 결정 5) |
| `#dz-impl-card:not(:has(#dz-impl-tasks li)){display:none}` | **채택.** 규칙 1줄. 개행에 영향받지 않고, 숨기는 대상이 **카드 전체**다 |

`:has()` 를 쓸 수 있는 근거는 이 프로젝트가 이미 Chromium 만 지원 대상으로 못 박았다는 점이다
(`documentPictureInPicture in window` 로 전체 게이팅). **그리고 실패 모드가 가볍다** —
`:has()` 미지원 브라우저에서도 깨지는 게 아니라 **빈 카드가 하나 보일 뿐**이다.

### 4. 하위 명령을 새로 만든다 — `step` 을 확장하지 않는다

`step 3.1` 로 "3번 단계의 1번 세부 작업"을 표현하고 싶은 유혹이 있지만 **`<g>.<p>` 는 이미
매트릭스 칸 좌표다.** 같은 문법에 두 의미를 얹으면 `step` 0단계가 "지금 이 파일이 매트릭스인가"를
먼저 판단해야 하고, 판정 실패가 곧 엉뚱한 칸 갱신이 된다. 확정 방향 4("결정성 유지")를 정면으로 깬다.

| 후보 | 판정 |
|------|------|
| `step 3.1 active` | **기각.** 매트릭스 좌표와 문법 충돌. 판정 분기가 생긴다 |
| `substeps "…"` + `substep <k> …` | **기각.** 한 글자 차이 이름 두 개는 LLM 지시문에서 최악이다 |
| `impl set "…"` + `impl <k> …` | **채택.** 첫 토큰이 `set` 이냐 정수냐로 갈리는 단일 토큰 판정. 셀렉터 접두어·카드 제목과 이름이 일치한다 |

`init` 에 세 번째 인자로 목록을 받는 안도 **불가능하다** — 세부 작업 목록은 **PRP 가 승인된 뒤에야**
확정된다. `init` 은 그보다 앞선 착수 보고 시점에 불린다. 별도 호출 시점이 필연이다.

### 5. 폴링 동기화에 **행을 하나 추가한다** (이번 라운드에서 유일하게 스크립트를 건드린다)

직전 라운드는 "스크립트는 한 줄도 바뀌지 않는다"가 자랑거리였다. 이번엔 바뀐다. **안 바꾸면
기능이 조용히 반쯤 죽기 때문이다** — `apply()` 는 6개 대상만 동기화하므로, impl 카드는
`http://` 로 열린 브라우저와 PiP 창에서 **영원히 초기 상태로 남는다**(파일은 정상, 화면만 거짓).
`file://` 경로는 전체 리로드라 우연히 동작해서, **가장 알아채기 어려운 형태의 버그**가 된다.

| 후보 | 판정 |
|------|------|
| `syncVisualization` 의 셀렉터에 `#dz-impl-tasks` 추가 | **기각.** `querySelector` 는 **첫 매칭 하나**만 준다. 둘을 다루려면 함수 구조를 바꿔야 하고, 이미 T22-29~34 로 고정된 검증된 코드를 건드리게 된다 |
| `#dz-impl-tasks` 와 `#dz-impl-count` 를 각각 동기화 | **기각.** 함수가 둘로 늘고 계약 표에 행이 둘 는다. 얻는 게 없다 |
| `#dz-impl-card` 를 `outerHTML` 로 통째 교체 | **채택.** 함수 1개(5줄), 계약 1행. 카드 안에 **사용자 상태가 하나도 없다**(라디오 없음, `<details>` 없음) — `syncVisualization` 이 `#dz-steps` 를 통째 교체하는 것과 정확히 같은 근거다 |

기존 sync 함수 4개와 **문체가 같은 5번째 함수**를 추가하는 것이므로 새 메커니즘이 아니다
(확정 사항 4의 정신 유지). T22-51 이 함수와 호출의 존재를, T22-53 이 기존 `syncVisualization`
셀렉터의 비퇴화를 각각 고정한다.

### 6. PiP 압축 뷰에서 impl 카드를 **숨기지 않는다**

로그 카드는 숨기는데 이건 왜 안 숨기나 — **곁눈질 화면이 원하는 정보가 정확히 이것**이기 때문이다.
직전 라운드는 "PiP 에 남길 것 = 세션 제목 · 전체 진행 단계 · 현재 진행중인 단계"로 정리했고,
「구현」 구간에서 "현재 진행중인 것"의 실체가 바로 이 세부 작업 둘이다. 로그는 이력이고
이건 현재 상태다.

그리고 `class="steps"` 재사용 덕분에 **PiP 전용 CSS 를 한 줄도 안 써도** 원하는 동작이 나온다:
active 항목은 배경 강조가 들어가고, 비활성 항목의 상세는 숨는다
(`body.dz-pip ol.steps li:not(.active) .step-detail{display:none}`).

| 후보 | 판정 |
|------|------|
| `body.dz-pip #dz-impl-card{display:none}` | **기각.** 압축 뷰에서 가장 값진 정보를 버린다 |
| PiP 에서 `done` 항목만 숨겨 목록을 줄인다 | **기각(이번엔).** 규칙 1줄이면 되지만, 항목이 사라졌다 나타나며 목록이 위아래로 움직여 곁눈질에 오히려 방해다. 세로 공간 문제는 `resizePipToFit` 의 클램프와 스크롤로 이미 처리된다(리스크 4). 실사용에서 아쉬우면 그때 1줄 추가한다 |
| 그대로 보여준다 | **채택.** 새 CSS 0줄 |

T22-58 이 역방향 assertion 으로 "숨김 규칙이 몰래 들어오는 것"을 막는다.

### 7. 목록은 **평면**이다 — 매트릭스 모드에서도 하나뿐이다

매트릭스 세션(그룹 2개 이상)에는 「구현」 열이 그룹 수만큼 있다. 그룹별 서브태스크 목록을
만들면 슬롯이 N개가 되고 확정 사항 1(고정 슬롯 하나)이 즉시 깨진다.

**세션당 목록 하나로 두고, 그룹 귀속은 항목 이름에 적는다**(예: `"worktree — 파일 I/O"`).
절차에 분기가 생기지 않고(선형·매트릭스 어느 쪽에서도 같은 코드 경로), 참고 샘플도 정확히
이 방식이었다(항목 이름에 `0b ·`, `2·3과 병렬 가능` 같은 맥락을 텍스트로 넣었다).

### 8. `impl set` 은 항목을 **전부 `wait` 으로** 만든다

`init` 은 1번 단계를 `active` 로 만든다. `impl set` 은 그러지 않는다. 목록 정의와 디스패치는
**다른 시점**이고, 아직 부르지도 않은 서브에이전트를 진행중으로 그리는 순간 이 패널의 유일한
신뢰 근거(확정 사항 2 — 관측된 두 시점만 기록)가 무너진다. 디스패치할 때 `impl <k> active` 를
부르면 된다. 호출 1회를 아끼려고 정직성을 파는 거래는 하지 않는다.

### 9. 세부 진행률 텍스트를 매크로와 **같은 형식**(`M/K · P%`)으로 둔다

바가 없으니 `%` 를 뺄 수도 있지만, `impl <k>` 2단계는 `step` 2단계의 파싱·전이 산술을
**문장째 재사용**한다. 형식이 갈리면 절차가 두 벌이 된다. 형식을 맞추는 대가는 P 를 한 번
더 계산하는 것뿐이다. (`dashboard-group-matrix.md` 설계 결정 7이 이미 같은 판단을 했다.)

### 10. 스코프 아웃과 사유

| 제외 항목 | 사유 |
|-----------|------|
| 임의 단계에 붙는 범용 하위 목록 | 확정 사항 1 |
| 세부 작업이 매크로 진행률에 기여 | 불변식 6. 분모가 갈라지면 `step` 산술이 무너진다 |
| `impl` 다중 인덱스(`impl 2,4 active`) | `dashboard-group-matrix.md` 가 다중 셀 `step` 을 같은 이유로 미룬 선례가 있다. 실사용 부담이 확인되면 그때(YAGNI) |
| 담당 에이전트 전용 필드·배지 | 소비자가 없다. 상세 문자열로 충분하다 |
| 세부 작업 목록을 `#dz-log` 에서 자동 유도 | 판단이 커맨드 안으로 들어온다. `log` 와 `impl` 은 호출 시점이 다르다 |
| 항목 추가·삭제·재정렬 | `init` 가드와 같은 철학(리스크 3). 형태를 바꾸려면 파일을 지우고 다시 시작한다 |
| 항목별 경과 시간 | 새 스크립트가 필요하다. `#dz-updated` 가 정체 감지를 이미 담당한다 |

---

## 테스트 계획

### 무엇을 검증할 수 있고 무엇은 못 하는가

이 커맨드에는 실행 코드가 없다(LLM 지시문 + HTML 템플릿). **브라우저 런타임과 CSS 렌더 결과는
자동 검증 대상이 아니고**, 템플릿·절차의 문자열 정합성과 자산 비퇴화만 grep 으로 검증한다 —
T22 가 이미 채택한 방식이다. 특히 **`:has()` 가 실제로 카드를 숨기는지는 grep 으로 알 수 없다** →
수동 확인 1·2 가 전담한다.

### `tests/run.sh` — T22 에 하위 검증 추가 (T22-48 ~ T22-58)

새 테스트 함수를 만들지 않는다. `total_tests=22` 는 그대로 두고
`test_dashboard_template_integrity` 안, **T22-47 바로 뒤**(`log_ok` 앞)에 이어 붙인다.
선행 주석(L998)과 `test_desc`(L1001)의 `T22-1~T22-47` 표기를 `T22-1~T22-58` 로 고친다.

| ID | 검증 | 방법 | 막으려는 회귀 |
|----|------|------|--------------|
| T22-48 | 슬롯 셀렉터 4종 존재 | `grep -qF` × 4: `id="dz-impl-card"` · `id="dz-impl-tasks"` · `id="dz-impl-count"` · `id="dz-impl-{k}"` | 슬롯 자체의 유실 |
| T22-49 | **자동 숨김 규칙 존재** | `grep -qF '#dz-impl-card:not(:has(#dz-impl-tasks li)){display:none}'` | 빈 카드가 모든 세션에 노출(확정 사항 3 위반) |
| T22-50 | **레이아웃 CSS 재사용** | `grep -qF '<ol class="steps" id="dz-impl-tasks">'` **및** `ol\.impl-tasks` 가 매칭되면 **실패** | 레이아웃 규칙 중복 정의 → 매크로 목록과 세부 목록이 갈라짐(설계 결정 2) |
| T22-51 | **폴링 동기화 추가** | `grep -qF 'function syncImplCard(fresh){'` **및** `grep -qF 'syncImplCard(fresh);'` | 카드가 동기화에서 빠져 `http://`·PiP 에서 영구히 stale(설계 결정 5의 조용한 실패) |
| T22-52 | **CSS 로만 숨김** | `grep -n 'dz-impl-card' \| grep -qE 'remove\(\|style\.display'` 가 매칭되면 **실패** | 스크립트가 카드를 DOM 에서 떼어내거나 인라인 스타일로 숨기는 퇴행 |
| T22-53 | 기존 시각화 동기화 비퇴화 | `grep -qF "querySelector('#dz-steps,#dz-matrix')"` | 새 동기화를 넣으며 기존 함수를 개조·파손 |
| T22-54 | **매크로 진행률 비침해 문구** | `grep -q '구현 세부 작업의 상태 변화는 매크로 진행률을 바꾸지 않는다'` | 세부 완료가 매크로 분모에 섞여 `step` 전이 산술이 무너짐(불변식 6) |
| T22-55 | **데이터 출처 한계 문구** | `grep -q '서브에이전트 내부의 실시간 진행률이 아니다'` **및** `grep -q '디스패치'` | 한계 서술이 사라져 실시간 진행률로 오인(확정 사항 2) |
| T22-56 | **병렬 active 허용 문구** | `grep -q '동시에 여러 항목이 active 일 수 있다'` | 단일 활성 가정이 슬며시 들어와 이 패널의 존재 이유가 사라짐 |
| T22-57 | 규약 노출 | `head -4 \| grep -q 'impl set'` **및** `grep -qF '/dashboard impl <k> <done\|active\|wait> ["상세"]'` | 절차만 있고 규약에 없어 아무도 못 씀 |
| T22-58 | **가드 문구 + PiP 미숨김** | `grep -q '이미 목록이 있으면 덮어쓰지 않는다'` **및** `grep -qF 'body.dz-pip #dz-impl-card{display:none}'` 이 매칭되면 **실패** | 재호출이 진행 상태를 날림 / 곁눈질 화면에서 가장 값진 카드를 숨기는 규칙이 몰래 추가됨(설계 결정 6) |

T22-50 · T22-52 · T22-58 은 **역방향 assertion**(매칭되면 실패)이다 —
T22-7·T22-28·T22-35·T22-38·T22-41·T22-42 가 이미 쓰는 패턴이며, 이 저장소 관례대로
**왜 그 방향인지 한 줄 이유를 주석으로 남긴다.**

참고 구현 (T22-52, 기존 스타일 준수):

```bash
  # T22-52: CSS 로만 숨김 — 스크립트가 impl 카드를 DOM 에서 떼어내거나 인라인 스타일로 숨기면
  # 폴링의 outerHTML 대입 대상이 사라져 카드가 영구히 되살아나지 않는다(역방향 assertion).
  if grep -n 'dz-impl-card' "$dashboard_command_file" | grep -qE 'remove\(|style\.display'; then
    record_failure "$test_name" "T22-52: 스크립트가 dz-impl-card 를 DOM/인라인 스타일로 조작함"
    return 1
  fi
```

참고 구현 (T22-58 후반, 역방향):

```bash
  # T22-58: PiP 압축 뷰에서 impl 카드를 숨기지 않는다 — 「구현」 구간의 곁눈질 화면에서
  # 가장 값진 정보가 이 카드다(설계 결정 6). 로그 카드와 달리 숨김 규칙이 있으면 실패다.
  if grep -qF 'body.dz-pip #dz-impl-card{display:none}' "$dashboard_command_file"; then
    record_failure "$test_name" "T22-58: PiP 에서 impl 카드를 숨기는 규칙이 추가됨"
    return 1
  fi
```

### 기존 테스트에 대한 영향 (전수 확인)

| 테스트 | 영향 | 근거 |
|--------|------|------|
| T22-2 (셀렉터 7종) | 계속 통과 | 기존 7행을 건드리지 않고 새 하위 절만 추가. 루프는 존재 여부만 본다 |
| T22-5 (`COMMANDS_FILE_COUNT`) | 계속 통과 | 새 커맨드 파일 없음 |
| T22-7 (`class="legend"` 부재) | 계속 통과 | 범례를 만들지 않는다(상태 어휘가 낱말이라 필요 없다) |
| T22-10 (`class="log-title"`) | 계속 통과 | 마크업의 `log-title` 은 그대로. CSS 셀렉터만 `.log-title,.impl-title` 로 확장 |
| T22-12 (log grep 패턴) | 계속 통과 | `log` 절을 건드리지 않는다. 새 id 중 `id="dz-log"` 에 매칭되는 것이 없다 |
| T22-22~26 (매트릭스·선형 자산) | 계속 통과 | `<ol class="steps" id="dz-steps">` 줄 그대로. `dz-step-{n}`·`step <g>.<p>` 그대로 |
| T22-27 (`:root` 완전 일치) | 계속 통과 | 새 색 토큰·리터럴 색을 쓰지 않는다 |
| T22-29~34 (PiP 스크립트) | 계속 통과 | 기존 함수를 고치지 않고 **함수 하나와 호출 한 줄만 추가**한다. `requestWindow` 호출 수 불변 |
| T22-37 (pip 버튼이 `.wrap` 바깥) | 계속 통과 | `id="dz-updated"` 를 새로 추가하지 않는다(현행 1회). 새 카드는 `.foot` **앞**에 들어간다 |
| T22-38 (`[id="dz-` 금지) | 계속 통과 | 새 CSS 는 `#dz-impl-card:not(:has(…))` 형태 |
| T22-39~46 (로그 카드·단계 상세) | 계속 통과 | `#dz-log-card`·`.step-detail` 자산을 **재사용만** 하고 지우지 않는다 |
| T22-47 (`resizePipToFit`) | 계속 통과 | `apply()` 말미의 호출 위치를 유지한다(새 sync 는 그 **앞**에 넣는다) |
| T23-6 (`\| on \| off`) | 계속 통과 | `impl` 항목을 `log`·`serve` 앞에 넣어 맨 뒤 연속 문자열 보존 |
| T23-11 (README ≤3) | 계속 통과 | 추가 문장에 `settings.local.json` 을 쓰지 않는다 |

### 검증 명령

```bash
cd /Users/pascal/works/personal/coding-env
bash tests/run.sh                                        # 기대: 총 22 / 통과 22 / 실패 0
wc -l commands/dashboard.md                              # README L120 의 줄 수를 이 값으로 갱신
head -4 commands/dashboard.md | grep -c '| on | off'     # 1 (T23-6 보호)
head -4 commands/dashboard.md | grep -c 'serve'          # 1 이상 (T22-36 보호)
head -4 commands/dashboard.md | grep -c 'impl set'       # 1 (T22-57 보호)
grep -c 'id="dz-updated"' commands/dashboard.md          # 1 (T22-37 보호)
grep -n 'id="dz-log"' commands/dashboard.md | grep -c 'dz-impl'   # 0 (앵커 비충돌)
grep -c 'ol\.impl-tasks' commands/dashboard.md           # 0 (T22-50 보호)
```

### 수동 확인 (자동화 불가 — Chrome 실기 필요)

| # | 절차 | 합격 기준 |
|---|------|----------|
| 1 | 새 프로젝트에서 `/dashboard init "테스트" "설계\|승인\|구현\|검수"` 후 `file://` 로 연다 | 화면이 **오늘과 완전히 동일**. 구현 세부 작업 카드가 **보이지 않는다**(`:has()` 확인) |
| 2 | 브라우저 개발자 도구로 `#dz-impl-card` 확인 | DOM 에는 **존재하고** 계산된 스타일이 `display:none` 이다(제거된 게 아님) |
| 3 | `/dashboard impl set "path-guard\|파일 I/O\|읽기 라우트\|쓰기\|diff\|배지\|웹 셸\|문서"` | 카드가 나타나고 8항목이 전부 `대기`, 카운터가 `0/8 · 0%` |
| 4 | `impl 1 active "에이전트 A — 순수 로직"` | 1번만 진행중, 상세 한 줄 표시, 카운터는 `0/8 · 0%` **그대로** |
| 5 | 이어서 `impl 4 active "에이전트 D — 병렬"` | **1번과 4번이 동시에 진행중**(핵심 요구), 매크로 진행률 **불변** |
| 6 | 이어서 `impl 1 done "22테스트 통과"` | 1번 완료, 카운터 `1/8 · 13%`, 4번은 진행중 유지 |
| 7 | `grep -c 'dz-impl-[0-9].*class="done"'` 로 감사 재계수 | 카운터의 M 과 일치(불변식 5 비파손) |
| 8 | 매크로 `step 3 done` 호출 | 매크로 진행률만 바뀌고 **세부 카운터는 안 바뀐다**(불변식 6) |
| 9 | `impl 3 active "<T> 제네릭 & 이스케이프"` | `<`·`&` 가 텍스트로 보이고 마크업이 안 깨진다 |
| 10 | `impl set "다시"` 재호출 | **가드가 걸려 중단**되고 안내가 나온다. 기존 목록이 그대로 남아 있다 |
| 11 | `impl 99 done` | 보고 후 중단. 다른 항목을 건드리지 않는다 |
| 12 | `/dashboard serve` → `http://localhost:8791/…` 로 열고 `impl 5 active` | **5초 내** 브라우저에 반영된다(설계 결정 5 — 이 확인을 빼면 조용히 실패한다) |
| 13 | 12번 상태에서 「플로팅」 진입 | impl 카드가 **보이고**, active 항목 둘에 배경 강조, 비활성 항목의 상세는 숨김 |
| 14 | 13번 상태에서 `impl 6 done` | 창 높이가 콘텐츠에 맞춰 다시 조정된다(`resizePipToFit`) |
| 15 | 플로팅 창을 닫는다 | opener 로 복귀하며 impl 카드·로그 카드가 내용 그대로 보인다 |
| 16 | 매트릭스 세션(`init "…" "A:설계,구현\|B:설계,구현"`)에서 `impl set "A—파서\|B—렌더러"` | 표와 impl 카드가 **함께** 정상 렌더(불변식 1 비충돌 확인) |
| 17 | 세부 작업 14개로 3번을 반복한 뒤 플로팅 | 창이 최대 높이(720px)에서 클램프되고 **스크롤로 나머지를 볼 수 있다**(리스크 4) |
| 18 | 「구현」 유형 필터·세션 탭을 조작한 뒤 폴링 5초 대기 | 선택이 유지된다(impl 카드 교체가 라디오를 건드리지 않음) |

---

## 구현 순서

각 단계 뒤에 `bash tests/run.sh` 로 비회귀를 확인한다. **착수 전에 기준선(22/22)을 먼저 기록한다.**

### Task 1 — 데이터 모델·불변식·동기화 계약 문서 갱신
- **ACTION**: `commands/dashboard.md` 의 「데이터 모델」에 '구현 세부 작업 슬롯' 하위 절 신설,
  정적 요소 표에 `#dz-impl-card` 행, 불변식 5·6, 폴링 동기화 계약 표에 1행.
- **MIRROR**: 기존 표 형식과 불변식 서술 스타일(현행 L82~95).
- **GOTCHA**: **기존 「동적 — 7 셀렉터」 표를 한 글자도 건드리지 않는다.**
- **VALIDATE**: 7행 유지를 눈으로 확인. `bash tests/run.sh`.

### Task 2 — 템플릿 `<style>` 수정
- **ACTION**: `.log-title` 규칙에 `,.impl-title` 추가, `.step-detail:empty` 뒤에 `:has()` 규칙 1줄.
- **GOTCHA**: 새 색 토큰·새 레이아웃 규칙 금지. `:root` 줄을 건드리지 않는다.
- **VALIDATE**: `git diff` 로 `:root` 무변경 확인(T22-27), `grep -c 'ol\.impl-tasks'` = 0.

### Task 3 — 템플릿 마크업 + 헤더 주석 맵
- **ACTION**: 첫 카드와 `#dz-log-card` 사이에 impl 카드 5줄. 헤더 주석 맵에 4줄.
- **GOTCHA**: `id="dz-updated"` 를 새로 만들지 않는다(T22-37). `<ol>`/`</ol>` 은 **두 줄**로 둔다.
- **VALIDATE**: `grep -c 'id="dz-updated"'` = 1.

### Task 4 — 템플릿 `<script>` 에 `syncImplCard` 추가
- **ACTION**: `syncLog` 뒤에 함수 1개, `apply()` 호출 줄에 `syncImplCard(fresh);` 삽입.
- **GOTCHA**: `resizePipToFit()` 은 **그대로 맨 뒤**에 남긴다(T22-47). 기존 4개 sync 함수를
  한 글자도 고치지 않는다(T22-29~34, T22-53).
- **VALIDATE**: `bash tests/run.sh`.

### Task 5 — `impl` 절차 절 신설 + 규약 + frontmatter + 트리거 표
- **ACTION**: 「인터페이스」의 `impl set`·`impl <k>` 절차를 그대로 반영. 호출 규약 2줄,
  `argument-hint`, 트리거 표 3행 + 각주. **데이터 출처 한계 문단을 절 서두에 넣는다.**
- **GOTCHA**: `argument-hint` 는 `| on | off` 를 맨 뒤 연속 문자열로 유지(T23-6),
  `serve` 를 `head -4` 안에 유지(T22-36). T22-54~56 이 고정하는 세 문장을 **문자 그대로** 쓴다.
- **VALIDATE**: 「검증 명령」의 `head -4` 3종.

### Task 6 — `tests/run.sh` 에 T22-48~58 추가
- **ACTION**: T22-47 뒤(`log_ok` 앞)에 11개 하위 검증. 주석·`test_desc` 를 `T22-1~T22-58` 로.
- **MIRROR**: `record_failure "$test_name" "T22-NN: …"` 후 `return 1`. 역방향 3종에는 이유 주석.
- **VALIDATE**: `bash tests/run.sh` → 22/22.

### Task 7 — `README.md`
- **ACTION**: L120 줄 수를 `wc -l` 실제 값으로. 설명 문단에 2~3문장(`impl` 하위 명령 +
  자동 숨김 + **갱신 시점 한계**).
- **GOTCHA**: 새 문장에 `settings.local.json` 을 쓰지 않는다(T23-11).
- **VALIDATE**: `bash tests/run.sh`.

### Task 8 — 수동 확인 1~18 수행

---

## 리스크와 대안

### 1. "실시간 진행률"로 오인될 수 있다 — 이번 라운드의 최대 리스크

**가능성 상 / 영향 중.** 항목이 `진행중` 으로 20분째 떠 있으면 보는 사람은 "지금 그 작업이
돌고 있다"고 읽는다. 실제로는 "디스패치했고 아직 보고가 안 왔다"뿐이며, 서브에이전트가
실패해 멈춰 있어도 화면은 동일하다.
**완화**: (a) 카드 제목에 `· 디스패치와 결과 보고 시점에만 갱신` 을 **상시 노출**,
(b) `impl` 절 서두의 한계 문단을 T22-55 가 문구째 고정, (c) `#dz-updated` 가 계속 보여
"언제 마지막으로 갱신됐는지"가 함께 읽힌다.
**기각한 대안**: 항목에 경과 시간 타이머를 붙인다 → 새 스크립트가 필요하고, "오래 걸림"과
"멈춤"을 구분해 주지도 못한다.

### 2. `impl` 호출을 빠뜨려 목록이 화석이 된다

**가능성 상 / 영향 하.** 서브에이전트를 셋 붙여 놓고 `impl <k> active` 를 안 부르면 목록이
전부 `대기` 인 채 남는다. `step` 도 같은 성격의 리스크를 이미 안고 있다(지시문 방식의 근본 한계).
**완화**: 트리거 표에 **디스패치 직전 / 결과 수령 직후**라는 구체적 시점을 못 박았다.
자동화(hook)는 `session-dashboard.md` 설계 결정 4에서 이미 기각된 방향이라 다시 꺼내지 않는다.

### 3. 세션 2에서 이전 세션의 목록이 남고, 재정의가 막힌다

**가능성 중 / 영향 중.** `init` 가드가 기존 대시보드를 재렌더하지 않으므로, 같은 프로젝트에서
새 전체 경로 작업을 시작하면 지난 세션의 세부 작업 목록이 그대로 보인다. 그 상태에서
`impl set` 을 부르면 가드에 걸려 중단된다.
**완화**: 가드 메시지가 "`.claude/dashboard.html` 을 지우고 `init` 부터 다시 하라"를 안내한다.
이는 `init` 가드의 알려진 한계(현행 문서 L307~311)와 **정확히 같은 성질·같은 해법**이다.
**기각한 대안**: `impl clear` 하위 명령 추가 → 문법이 하나 늘고, 오호출이 진행 상태를 날리는
경로를 새로 만든다. 파일 삭제 한 번이 더 싸다(YAGNI).

### 4. 항목이 많으면 PiP 창이 스크롤된다

**가능성 중 / 영향 하.** 14항목이면 `.wrap` 실제 높이가 `PIP_MAX_HEIGHT`(720px)를 넘어
클램프되고 아래쪽이 스크롤된다.
**완화**: 클램프·스크롤은 이미 구현돼 있고(`resizePipToFit`), active 항목은 배경 강조가 있어
찾기 쉽다. 수동 확인 17이 이 상태를 직접 본다.
**기각한 대안**: PiP 에서 `done` 항목 숨기기 → 목록이 위아래로 움직여 곁눈질에 오히려 방해다
(설계 결정 6). 실사용에서 아쉬우면 CSS 1줄로 나중에 켠다.

### 5. `:has()` 미지원 환경

**가능성 하 / 영향 하.** 이 프로젝트는 이미 Chromium 만 지원 대상이지만, `file://` + 구형
브라우저 조합이면 `:has()` 가 무시된다.
**완화**: 실패 모드가 **"빈 카드가 하나 보인다"** 뿐이다. 데이터도 절차도 깨지지 않는다.
**기각한 대안**: `:has()` 없이 JS 로 개수를 세어 숨기기 → 폴링마다 재실행해야 하고 CSS 원칙을 깬다.

### 6. 스크립트를 건드리는 첫 라운드라 폴링 계약이 퇴화할 수 있다

**가능성 중 / 영향 상.** `apply()` 를 편집하면서 `resizePipToFit()` 호출 순서를 바꾸거나
`syncLog` 를 밀어내면 직전 두 라운드의 성과가 조용히 죽는다.
**완화**: T22-43(`syncLog`)·T22-47(`resizePipToFit`)·T22-53(`syncVisualization`)이 세 방향에서
고정한다. 구현 순서 Task 4의 GOTCHA 에도 명시했다. **검수자는 이 지점을 중점 항목으로 본다.**

### 7. 세부 카운터와 실제 상태가 어긋난다

**가능성 중 / 영향 하.** 전이(±1) 산술은 "이전 상태를 정확히 읽었다"에 의존한다. `step` 이
이미 같은 구조를 쓰고 있고, 이전 상태는 0단계 grep 결과 줄에 그대로 적혀 있어 기억에
의존하지 않는다.
**완화**: 감사용 재계수 명령(`grep -c 'dz-impl-[0-9].*class="done"'`)을 절차에 명시.
수동 확인 7이 이를 직접 돌린다.

### 8. 검토했으나 채택하지 않은 전체 대안

| 대안 | 기각 사유 |
|------|----------|
| **임의 단계에 하위 목록을 붙이는 범용 슬롯** | 확정 사항 1. 슬롯↔단계 매핑·네임스페이스·개수 관리가 전부 따라온다. 병렬 지점은 「구현」뿐이다 |
| **`step 3.1` 로 세부 작업 표현** | `<g>.<p>` 가 이미 매트릭스 좌표다. 문법 충돌 → 판정 분기 → 결정성 상실(설계 결정 4) |
| **서브에이전트가 자기 항목을 직접 갱신** | `session-dashboard.md` 설계 결정 4 정면 위반. 별도 컨텍스트 + 병렬 파일 경합 |
| **세부 작업을 매트릭스의 한 행으로 표현** | 「구현」 열 하나에 14칸이 필요한데 표는 직사각형이다. 나머지 그룹 전부에 `na` 12칸이 생긴다 |
| **`#dz-impl-tasks:empty` 로 숨기기** | 개행 텍스트 노드 때문에 **절대 매칭되지 않고**, 매칭돼도 카드 제목이 남는다(설계 결정 3) |
| **`init` 이 조건부로 카드 마크업을 넣기** | 확정 사항 3이 명시적으로 배제. `init` 분기 + 카드 삽입 Edit 경로가 늘어난다 |
| **`syncVisualization` 셀렉터에 impl 목록 추가** | `querySelector` 는 첫 매칭만 준다. 검증된 함수를 개조하게 된다(설계 결정 5) |
| **세부 작업이 매크로 진행률에 기여** | 분모가 두 체계로 갈라져 `step` 2단계 전이 산술이 무너진다(불변식 6) |
| **담당 에이전트를 `data-agent` 속성으로** | 소비자가 하나도 없다. 상세 문자열에 적으면 끝난다 |

---

## 워크플로우 경로 판정

**전체 경로(설계 → 구현 → 검수)가 맞다.** 근거는 각각 단독으로 충족한다:

- **공개 인터페이스 변경**: `impl` 하위 명령 신설, `argument-hint` 변경.
- **데이터 모델 변경**: 새 셀렉터 4종, 불변식 5·6 추가, 폴링 동기화 계약 1행 추가.
- **파일 3개 이상**: `commands/dashboard.md`, `tests/run.sh`, `README.md`.
- **새 외부 의존성**: 없음.
- **민감 영역**: 아님(네트워크·권한·삭제 없음). **검수는 sonnet 기본으로 충분하다.**
  단 검수자는 **① 폴링 계약 비퇴화(`syncLog`·`syncVisualization`·`resizePipToFit` 호출 위치)
  ② 기존 7 셀렉터 표 무수정 ③ 매크로 진행률 비침해**를 중점 항목으로 본다.

**신뢰도(단일 패스 구현 가능성): 8/10.** 변경 지점이 문자열 수준까지 특정돼 있고, 신규 자산의
대부분이 기존 CSS 재사용이다. 감점 2는 (a) 이번 라운드가 `<script>` 를 건드리는 첫 라운드라
폴링 계약 퇴화 위험이 있고, (b) `:has()` 렌더 결과가 자동 검증 불가라 수동 확인 1·2 에
의존하기 때문이다.

---

## 사용자 승인이 필요한 핵심 결정

1. **하위 명령 이름을 `impl` 로 하고, `impl set "<작업1|…>"` + `impl <k> <done|active|wait> ["상세"]`
   두 형태로 고정한다.** 셀렉터 접두어도 `dz-impl-` 이다(`dz-sub-` 는 `#dz-subtitle` 과 접두어가
   겹쳐 grep 앵커를 위협하므로 기각). `log impl` 과는 토큰 위치로 구분된다 — 이 이름으로 확정하는가?

2. **세부 작업 상태는 오케스트레이터가 직접 아는 두 시점(디스패치 → `active`, 결과 보고 → `done`)에만
   갱신된다.** 서브에이전트는 이 파일에 여전히 접근하지 않으며, 카드 제목에
   `· 디스패치와 결과 보고 시점에만 갱신` 이라는 문구가 **상시 노출**된다. 이 문구를 화면에 두는 데
   동의하는가(빼면 화면상의 정직성 장치가 사라진다)?

3. **세부 진행 카운터 `#dz-impl-count`(`3/8 · 38%`)를 만든다.** 매크로 진행률과 형식은 같지만
   **완전히 독립**이며 `#dz-progress-bar`/`#dz-progress-pct` 를 절대 건드리지 않는다(불변식 6).
   최소 구성을 원하면 이 셀렉터를 빼고 `impl <k>` 를 Edit 2회로 줄일 수 있다 — 넣는 쪽으로 가는가?

4. **PiP 압축 뷰에서 impl 카드를 숨기지 않는다.** 로그 카드는 계속 숨기되, 「구현」 구간에서
   곁눈질에 가장 값진 정보가 이 카드이기 때문이다. 항목이 14개면 창이 최대 높이에서 클램프되고
   스크롤된다(리스크 4) — 이 트레이드오프를 수용하는가?

5. **`impl set` 재호출은 가드로 막고, 목록을 새로 만들려면 `.claude/dashboard.html` 을 지우고
   `init` 부터 다시 한다.** `impl clear` 같은 하위 명령을 만들지 않는다(`init` 가드와 같은 철학).
   세션 2에서 이전 목록이 남는 한계를 함께 수용하는가?

6. **`README.md` L120 의 stale 한 줄 수(959 → 실제 974+)를 이번에 함께 바로잡는다.** 직전 라운드에서
   또 누락된 값이며 어차피 같은 줄을 고친다. 이번 변경 범위 밖의 수정이므로 명시적으로 확인한다.
