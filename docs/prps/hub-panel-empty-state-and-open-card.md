# 허브 — 열린 카드 선택 표시 + 대시보드 없는 프로젝트의 **패널 빈 상태** (PRP)

> 요구 2건: **R1** 지금 상세 패널에 열려 있는 프로젝트의 카드를 **화면에서 알아볼 수 있게** 한다 ·
> **R2** 대시보드가 없는 프로젝트(티어 2·3)의 카드도 **클릭 대상**으로 만들고, 패널은 대시보드
> 대신 **왜 없는지**를 띄운다

| 항목 | 값 |
|------|-----|
| 대상 | `hub/bin/hub_template.html` **1개 파일이 기능의 전부** + `tests/run.sh` + `hub/README.md` + PRP 이관 표기 2개 |
| 브랜치 | `main` (HEAD `ba74548`) |
| 상위 설계 정본 | [`hub-dashboard.md`](./hub-dashboard.md) → [`hub-card-interactions-and-usage.md`](./hub-card-interactions-and-usage.md)(결정 N1~N8) → [`hub-detail-side-panel.md`](./hub-detail-side-panel.md)(SP1~SP19 · 불변식 H1⁗) → [`hub-session-revival-and-stale-tier1.md`](./hub-session-revival-and-stale-tier1.md)(GN4~GN7) → [`hub-panel-polish-and-icons.md`](./hub-panel-polish-and-icons.md)(PV1~PV13) → **이 문서** |
| 워크플로우 경로 | **전체 경로** — 패널의 **신원 키**(공개 상태 모델)와 **불변식 H1⁗ 조항 ③** 이 함께 바뀐다 |
| 규모 | **Medium** — 신규 1개(이 문서) / 수정 5개 파일. 템플릿 순증 약 **+78 / −14줄**. 새 함수 4개, 새 상수 6개, 새 CSS 규칙 4개, 새 정적 노드 1개 |
| **Python 변경** | **없음.** `dashboard_key` 가 티어 1 만 값을 갖는 규칙(`hub_project.py:198`)을 **그대로 둔다** — 이 버그는 그 규칙 때문이 아니라 그 규칙을 **클릭 가능 여부로 겸용한** 표현 계층 때문이다. 스냅샷 계약(`ProjectView`)에 필드를 하나도 더하지 않는다(근거: 결정 OC4) |
| 새 외부 의존성 | **없음** (바닐라 CSS/JS) |
| 새 색 토큰 | **0개.** `--accent-soft`·`--muted` 만 재사용한다(결정 OC9) |
| 결정 코드 | **OC**(Open Card). 단일 문자 A~Z 는 소진됐고 두 글자 관례(SP·MD·GN·PV·DG·ZG·RV·MS)가 확립돼 있다. `OC` 가 저장소 전체에서 미사용임을 확인했다(`grep -rn "결정 OC" docs/prps` → 0건) |
| 리스크 코드 | **P-n**. 최근 5개 PRP(SP·PV·GN·ZG·MS)가 전부 `P-n` 을 쓴다. `R-n` 은 `hub-card-interactions-and-usage.md` 의 **지역 계열**이며, 이 문서가 인용하는 `R-6`·`R-11` 은 그 문서 것이다 |
| 승인 상태 | **승인됨(2026-08-19) · 구현·검수 완료** — 「승인 요청 항목」 1~5 전부 권고안대로 채택(항목 4 = 안 A, 모든 카드 제목을 `<button>` 으로). 검수 PASS(Critical 0 · Major 0 · Minor 3) |

### 개정 이력

| 개정 | 내용 |
|------|------|
| 초판 (2026-08-19) | 사용자 신고 1건에서 출발. 결정 OC1~OC12 신설. 불변식 **H1⁗ 조항 ③ 개정**(개명 없이 — 선례 PV5), **SP8 개정**, **SP15 재진술**, **N2·N6·N7 부분 대체**. 검사 T25-80 개정 · T25-105~T25-109 신설 |


> **줄 번호 기준선 — 반드시 먼저 읽어라.** 이 문서의 모든 `hub_template.html` 줄 번호는
> **HEAD `ba74548` 의 커밋 상태(1383줄)** 기준이며, 그 상태를 직접 읽어 확인한 값이다.
> **작업 트리에는 이 PRP 와 무관한 미커밋 변경이 있다** — 세션 발췌 잘라내기 + 툴팁 폭 확대
> (`hub/bin/hub_template.html` **+37줄** · `hub/bin/hub_hook.py`). 그 결과 인용 줄 번호가
> 아래와 같이 밀린다(diff 로 실측):
>
> | 원래 줄 | 시프트 | 이유 |
> |---------|--------|------|
> | ~263 | **0** | 변경 지점 앞 |
> | 264~624 | **+9** | `.tooltip` 규칙에 주석 9줄 |
> | 625~818 | **+18** | 발췌 상수 3개 + 주석 9줄 |
> | 819~ (끝까지) | **+37** | `excerptDisplayText`·`renderSessionExcerpt` 신설 21줄 − 2줄 |
>
> 즉 **패널 CSS는 +9, `TIER_DESCRIPTION` 은 +18, `renderProject`·`render()`·패널 IIFE 는
> 전부 +37** 이다(검증: `app.insertAdjacentHTML('beforeend', extra);` 1142행 → 1179행 · 파일
> 1383줄 → 1420줄, 둘 다 +37 로 일치). **구현 직전에 `git status` 를 확인하고, 그 변경이
> 병합된 뒤라면 위 표 대신 다시 실측하라.**
>
> **설계 내용에는 영향이 없음을 확인했다**(diff 전문 대조): 그 작업은 `.tooltip` CSS ·
> 발췌 상수 · `renderSession`/`renderSessionExcerpt` 만 건드리고, 이 PRP 가 만지는
> 카드 속성(`renderProject`) · `render()` 말미 · 패널 IIFE · 패널 CSS 와 **겹치는 줄이 0줄**이다.

---

## 요구사항 요약

허브의 상세 패널은 카드를 눌러 그 프로젝트의 대시보드를 우측에 띄운다(결정 SP1). 그런데
**대시보드가 있는 프로젝트(티어 1)만 클릭 대상**이고, 티어 2·3 카드 클릭은 위임 핸들러의 조기
반환에 걸려 **아무 일도 일어나지 않는다**. 패널이 이미 열려 있으면 그 "아무 일도"가 치명적으로
바뀐다 — A(티어 1)의 대시보드가 열린 채 B(티어 2)를 누르면 **패널에는 계속 A 가 있는데 사용자는
B 를 눌렀다.** 틀린 내용이 정답 자리에 서 있고, 화면 어디에도 "지금 열려 있는 것은 A 다"라는
표시가 없다. 이 문서는 두 가지를 함께 고친다 — **어느 카드가 열려 있는지 카드 격자에서 보이게**
하고(R1), **모든 카드가 클릭에 답하게** 한다(R2. 답은 대시보드이거나, 대시보드가 없는 사유다).

### 사용자가 신고한 재현 절차 (직접 코드로 확인한 인과)

| # | 사실 | 근거(직접 읽음) |
|---|------|------------------|
| 1 | `dashboard_key` 는 **티어 1 프로젝트만** 값을 갖는다 | `hub_project.py:198` — `dashboard_key=project_dashboard_key(path) if tier == 1 else None` |
| 2 | 카드는 그 키가 있고 **서버 모드**일 때만 `data-dashboard-key` 를 받는다 | `hub_template.html:897~899` — `isClickable = Boolean(project.dashboard_key) && isServed` |
| 3 | 클릭 위임 핸들러가 `closest('[data-dashboard-key]')` 로 대상을 찾고 없으면 **조기 반환**한다 | `hub_template.html:1373~1374`. B 카드 클릭은 `openDetailPanel` 에 **도달조차 못 한다** → iframe·제목이 A 그대로 남는다 |
| 4 | 열림 상태의 유일한 진실 `openDashboardKey`(1291행)는 **`body.dzh-panel-open` 에만** 투영된다. 카드 격자에는 `card-open`·`aria-current` 류가 **0건**이다 | `grep -n "card-open\|aria-current" hub/bin/hub_template.html` → 0건 |
| 5 | 밀어내기 모드(≥1024px)에서는 배경이 `inert` 가 **아니고**(1319~1321행: 오버레이일 때만 켠다) 카드가 전부 보인다 → 지금 열린 패널이 **어느 카드의 것인지 알 단서가 패널 제목 텍스트뿐**이다 | 결정 SP3·SP1 |

### 왜 이 버그가 생겼나 — 정본 이관의 사각지대

이 버그는 실수가 아니라 **결정의 전제가 사라진 것을 아무도 다시 보지 않아서** 생겼다.
같은 일이 또 생기지 않게 인과를 기록한다.

1. 티어 2·3 조기 반환은 **결정 N6 에 명시**돼 있었다 —
   `hub-card-interactions-and-usage.md:701`: *"클릭 핸들러가 조기 반환. `cursor:pointer` 를 주지
   않고 `.project-name` 도 버튼이 아닌 `<span>` 으로 렌더한다(없는 기능을 있는 척하지 않는다)"*.
2. **그 시점의 상세 화면은 모달이었다**(결정 N5, `<dialog>`+`showModal()`+`::backdrop`).
   모달이 열리면 배경이 `inert` 이므로 **A 가 열린 채 B 를 클릭하는 것 자체가 불가능**했다.
   즉 N6 의 조기 반환은 그 시절 **관측 가능한 피해가 0** 이었다.
3. 2026-08-14 모달 → 사이드 패널 전환에서 N6 은 「**유지**」로 이관됐다. 그런데 그 근거는
   **드래그 핸들·텍스트 선택 가드뿐**이다 — `hub-detail-side-panel.md:291`:
   *"→ **유지** — 드래그 핸들·텍스트 선택 가드는 패널에서도 그대로다"*. N6 표의 **티어 2·3 행은
   새로 생긴 비모달 푸시 모드에 대해 재검토되지 않았다.**
4. 같은 문서의 결정 **N7** 은 `file://` 모드에 안내 툴팁을 주면서
   *"아무 반응 없는 클릭(조용한 실패)을 만들지 않는다"* 고 못박았다
   (`hub-card-interactions-and-usage.md:722`). **지금 티어 2·3 클릭은 정확히 그 조용한 실패**이며,
   게다가 **틀린 콘텐츠가 답 자리에 서 있으므로 `file://` 케이스보다 나쁘다.**

> **결론: 이것은 취향 문제가 아니라 프로젝트 자체 원칙과의 내적 모순이다.** N7 이 금지한 것을
> N6 의 유효기간 지난 조항이 만들어 내고 있다. 그래서 이 PRP 는 새 기능을 얹는 것이 아니라
> **이미 선언된 원칙(N7)을 코드에 관철**하는 일이다.
>
> 그리고 R1 은 `hub-detail-side-panel.md` **YAGNI 보류 2** 의 재방문 트리거가 발동한 것이다 —
> 예고된 트리거는 *"카드가 20개를 넘어 '어느 걸 열었더라'가 실제 불편으로 보고될 때"* 였는데,
> 실제로 온 신고는 그보다 **한 단계 나쁜 형태**("열려 있는 것을 다른 프로젝트로 오해했다")였다.
> 보류 2 가 기각 근거로 든 전제("강조를 유지하려면 **렌더 IIFE 가 패널 상태를 알아야 한다**")는
> **틀렸다** — 결정 OC8 이 그 전제 없이 해결한다.

### 사용자 스토리

| # | 스토리 |
|---|--------|
| S1 | 패널이 열려 있는 동안 **어느 카드의 것인지** 카드 격자만 봐도 안다 |
| S2 | 다른 카드를 누르면 표시가 **그 카드로 옮겨간다**. 폴링·틱이 카드를 다시 그려도 표시는 남는다 |
| S3 | 대시보드가 **없는** 프로젝트 카드를 눌러도 패널이 응답한다 — 제목이 그 프로젝트로 바뀌고, 본문에 **왜 대시보드가 없는지**가 적힌다 |
| S4 | 그 순간 **이전 프로젝트의 대시보드는 화면에서 사라진다.** 남아 있어서 오해할 여지가 없다 |
| S5 | 빈 상태에서 또 다른 프로젝트를 눌러도 **제목이 따라 바뀐다**(두 프로젝트가 모두 대시보드가 없어도) |
| S6 | 스크린리더 사용자는 열려 있는 프로젝트 카드에서 "현재 항목"이라는 안내를 듣는다 |
| S7 | 같은 카드를 다시 눌러도 읽고 있던 대시보드의 **스크롤 위치가 유지된다**(결정 SP8 의 목적은 그대로 달성된다) |
| S8 | 열어 둔 프로젝트의 티어가 바뀌면(예: `/dashboard init` 실행) **카드를 한 번 더 누르는 것만으로** 패널이 새 사실을 반영한다 |

### 성공 기준 (검증 가능한 형태)

| # | 기준 | 검증 |
|---|------|------|
| G1 | 클릭 대상 판정이 **두 속성의 OR** 이다 — `CLICKABLE_CARD_SELECTOR` 가 `data-dashboard-key` 와 `data-panel-empty-reason` 을 함께 본다 | T25-105 |
| G2 | 빈 상태에서 iframe 이 `display:none` 이고 사유 문구가 보인다 | T25-105 · 수동 M2 |
| G3 | 사유 문구의 단일 출처가 `TIER_DESCRIPTION` 이다 — **패널 IIFE 범위에 티어 문구 리터럴이 0건** | T25-105 |
| G4 | 패널의 신원 키가 `data-project-path` 다 — `hub_template.html` 에 `openDashboardKey` 가 **0건** | T25-106 |
| G5 | 열린 카드에 `.card-open` 이 붙고 그 `.project-name` 에 `aria-current="true"` 가 붙는다 | T25-108 · 수동 M4 |
| G6 | 폴링·틱이 `#dzh-app` 을 통째로 교체한 뒤에도 표시가 유지된다 | T25-107 · 수동 M5 |
| G7 | 재적용에 **새 `MutationObserver` 를 쓰지 않는다** — 파일 전체 `new MutationObserver` 등장 횟수가 **여전히 2**(툴팁용 2개) | T25-107 |
| G8 | `dzh:rendered` 리터럴이 파일에 **정확히 2회**(발행·수신) 있고, 발행 지점이 `insertAdjacentHTML` 뒤 · 패널 IIFE 앞이다 | T25-107 |
| G9 | "대기 타이머 ⇒ 닫힘"(SP15)이 여전히 성립한다 — `clearTimeout(panelBlankTimerId)` 이 `openDetailPanel`·`closeDetailPanel` **사이 한 곳뿐** | T25-83(무수정) · 수동 M8 |
| G10 | 문서(`hub/README.md`·머리 주석)가 화면과 일치한다 — 「티어 2·3 카드는 클릭 대상이 아니다」·「카드에는 패널 열림 상태를 반영하지 않는다」가 **0건** | T25-109 |

---

## 확정된 전제 (재론하지 않는다)

1. **허브 파이썬은 한 줄도 바꾸지 않는다**(SP 초판 전제 1 승계). `dashboard_key` 는 계속 티어 1
   만 값을 갖고, `ProjectView` 에 새 필드가 생기지 않는다. `tier` 는 이미 스냅샷에 있으나
   **패널이 그것을 읽지도 않는다**(결정 OC4).
2. **`/dashboard` 생성물(`commands/dashboard.md`)도 바꾸지 않는다**(SP 초판 전제 2 승계).
   이 PRP 는 그 문서를 iframe 으로 **띄우거나 안 띄우거나** 둘 중 하나만 한다.
3. **새 색 토큰을 만들지 않는다**(SP 초판 전제 4 승계). T25-29·T25-88 이 색 리터럴 회귀를
   기계적으로 막는다.
4. **`role="dialog"`·`aria-labelledby`·`aria-label="닫기"`·`data-tooltip="닫기 (Esc)"` 는 건드리지
   않는다**(SP R2 전제 2 승계). T25-76·T25-85 가 이미 지킨다.
5. **패널 폭 550px(SP19)·브레이크포인트 1024px(SP3)·닫기 애니메이션 180ms(SP12~SP17)는
   무변경**이다.
6. **문구를 새로 쓰지 않는다.** 빈 상태 사유는 이미 화면에 있는 `TIER_DESCRIPTION`
   (`hub_template.html:633~637`)과 `FILE_MODE_DASHBOARD_HINT`(672행)를 **재사용**한다.
   행동 안내 문장 추가는 YAGNI 보류 4 로 미룬다.

### 비목표 (이번 범위 밖 — 명시적으로 건드리지 않는다)

| 항목 | 이유 |
|------|------|
| 빈 상태에 "이 프로젝트에서 `/dashboard init` 을 실행하세요" 류 **행동 안내** 추가 | 전제 6. 문구 신설은 별건이며 티어 3 에는 맞지 않는 안내가 된다(YAGNI 보류 4) |
| 열린 프로젝트가 **강등**되면 패널을 자동으로 빈 상태로 전환 | 패널 IIFE 가 스냅샷을 알아야 한다 → 결정 SP1·검사 T25-89 위반. 재클릭 한 번으로 해소된다(엣지 E7, YAGNI 보류 1) |
| 딥링크·뒤로가기로 패널 닫기 | SP 초판 YAGNI 보류 5 그대로 |
| 여러 카드 동시 표시(멀티 선택) | 패널이 싱글턴 하나다. 표시도 하나다 |
| 카드에서 대시보드 **생성**을 시작하는 버튼 | 허브는 읽기 전용이다(`hub-dashboard.md` 의 최상위 원칙) |
| 티어 2·3 카드에 `data-tooltip` 추가로 대체 | 툴팁은 hover 전용이라 **키보드·터치에서 얻을 수 없다.** 그리고 이미 `.badge.tier` 가 같은 문구를 툴팁으로 갖고 있다(910행) — 같은 정보를 세 번째로 복제하는 일이다 |

---

## 영향 범위

### 수정 파일 (5개 + 이 문서)

| 파일 | 무엇을 | 왜 |
|------|--------|-----|
| `hub/bin/hub_template.html` | ① 머리 주석 **조항 ③ 개정**(33~35행) + 패널 문단 개정(66~69행) + 조항 ④ 변수명(38~39행) ② `.card[data-dashboard-key]` 셀렉터 2줄 교체(119~122행) + `.card.card-open` 규칙 신설 ③ `.project-name` 주석 개정(145~147행, OC11 채택 시) ④ 빈 상태 CSS 3줄 신설(`.detail-frame` 338행 뒤) ⑤ 패널 DOM 에 `<p id="dzh-detail-empty-reason">` 1줄 추가(417행 뒤) ⑥ 렌더 IIFE: `RENDERED_EVENT_NAME` 상수 1개 · `panelEmptyReason()` 신설 · `renderProjectNameEl` 개정(878~884행) · `renderProject` 속성 1개 추가(896~902행) · `render()` 말미 발행 1줄(1144행 뒤) ⑦ 패널 IIFE: 상수 5개 · 상태 변수 개명 · `readCardTarget`·`findCardByProjectPath`·`applyOpenCardMarking` 신설 · `openDetailPanel`·`closeDetailPanel`·클릭 위임 개정(1263~1380행) | R1·R2 전부 |
| `tests/run.sh` | **T25-80 개정**(3587행, 토큰 `openDashboardKey` → `openPanelTarget`), **신규 T25-105~T25-109**, 함수 설명 문자열 2곳(2346·2349행) `T25-1~T25-104` → `T25-1~T25-109` | 회귀 검사 |
| `hub/README.md` | 「프로젝트 상세 패널」 절 195~196·202~204행(클릭 대상 서술) + 열린 카드 표시 1문장 추가 | 화면과 문서의 어긋남 방지(T25-109) |
| `docs/prps/hub-card-interactions-and-usage.md` | 결정 **N2·N6·N7**, 리스크 **R-11** 에 이관 표기 | 정본 이관 |
| `docs/prps/hub-detail-side-panel.md` | 결정 **SP6(H1⁗ 조항 ③)·SP8·SP15**, 초판 **전제 3**, **YAGNI 보류 2**, 리스크 **P-6** 에 이관 표기 | 정본 이관 |

> 표기 관례: **원문을 지우지 않고 화살표만 덧붙인다**(선례 — `hub-detail-side-panel.md:282~294`,
> `hub-onboarding-statusline-and-stale-usage.md` 의 결정 EX7). 단 **코드 주석은 예외**로,
> 사실과 어긋나면 고친다(선례 — `hub-panel-polish-and-icons.md` 리스크 P-6).

### 미영향 — 건드리지 않는 이유 (직접 확인함)

| 대상 | 근거 |
|------|------|
| `hub/bin/*.py` 전부 | 이 PRP 가 만드는 어떤 식별자도 파이썬에 없다. `dashboard_key` 파생 규칙(`hub_project.py:198`)과 그 단위 테스트(`tests/hub/test_hub_project.py::test_u6_only_tier1_view_has_dashboard_key`, 269~290행)는 **무변경으로 계속 통과**한다 |
| 머리 주석 **정적 노드 목록**(20~24행) | `#dzh-detail-panel(과 그 자식 전부)` 가 이미 새 `<p>` 를 포함한다 — 목록을 늘리지 않는다 |
| 불변식 H1⁗ 의 **이름** | 조항 ③ 만 개정한다(결정 OC10, 선례 PV5). T25-81 이 요구하는 `H1⁗` 토큰이 그대로 남는다 |
| `#dzh-detail-note`(안내 줄, GN6·PV3·PV4) | 빈 상태에서는 **항상 숨김**이다 — `tier1_is_previous_task` 는 티어 1(=`tier1` 객체 존재)에서만 참이 될 수 있다(`hub_project.py:181~183`). 자리 예약은 그대로라 헤더 높이도 그대로(PV2·PV3) |
| 드래그·순서(DG1~DG7) | 핸들 가드(`closest('.card-drag-handle')`, 1371행)를 그대로 둔다. `dragover` 의 라이브 이동은 카드를 **재생성하지 않으므로**(1207행 `insertBefore`) 표시가 살아 있다 |
| 사용량 패널 바깥 클릭 가드(1167행) | `closest('#dzh-detail-panel')` 그대로. 카드 클릭이 패널을 접는 기존 동작(엣지 E9)도 그대로 |
| ESC·`matchMedia`·`applyBackgroundInert` 배선 | 변수명 1곳만 바뀐다. 닫기 경로는 여전히 `closeDetailPanel()` 하나다 |
| **미커밋 발췌 작업**(`.excerpt[data-tooltip]`·`renderSessionExcerpt`) | 겹치는 줄이 0줄이다(위 기준선 주의문). 새로 생긴 발췌 툴팁 트리거는 **카드 안**에 있으므로 클릭 시 카드 클릭으로 처리된다 — 결정 N6 표가 이미 정한 동작이다(*"티어 배지·에이전트 칩(툴팁 트리거) 클릭 → 카드 클릭으로 처리된다(의도된 동작)"*). 새 가드를 만들지 않는다 |
| T25-63·76·77·78·79·82·83·84·85·88·89·96·98 | 각각 마크업 id·레이아웃 토큰·브레이크포인트·전이 세트·타이머 이름·`.detail-close`·색 리터럴·안내 줄만 본다. **전수 대조 결과 무수정**(아래 테스트 계획 표) |
| `new MutationObserver` 2곳(602·609행) | 툴팁 전용이다. 이 PRP 는 **옵저버를 늘리지 않는다**(결정 OC8) — G7 이 그 증거다 |

---

## 결정 기록

### 결정 OC1 — 패널의 신원 키를 `data-project-path` 로 바꾸고, 상태를 **`openPanelTarget` 한 변수**로 둔다

**문제(고치지 않으면 같은 버그가 재발한다).** `openDetailPanel` 첫 줄은
`if(dashboardKey === openDashboardKey) return;`(1329행)이다. 티어 2·3 의 `dashboardKey` 는 전부
`null` 이므로 **대시보드 없는 모든 프로젝트가 서로 같은 것으로 판정된다.** R2 만 구현하고 이 줄을
그대로 두면:

- B(빈 상태)가 열린 상태에서 C 를 클릭 → `null === null` → **조기 반환 → 제목이 계속 B 다.**
  신고된 버그와 **완전히 같은 형태**가 다른 자리에서 재발한다.
- 게다가 **닫는 도중(180ms)** 에 티어 2·3 카드를 클릭하면 `openDashboardKey` 도 `null` 이라
  역시 조기 반환한다 → **패널이 나가 버리고 다시 열리지 않는다.** SP15 가 시나리오 ①②로
  풀어 둔 재진입 처리가 이 한 줄 때문에 티어 2·3 에서만 깨진다.

| 안 | 평가 |
|----|------|
| A. `dashboardKey` 유지 + 티어 2·3 용 가짜 키 발급(예: `'none:' + path`) | 신원 문제는 풀리지만 **`dashboardKey` 가 "대시보드 경로 조립용 값"이라는 의미를 잃는다.** `projectDashboardUrl()` 에 들어가면 안 되는 값이 같은 변수에 산다 → 다음 사람이 그것을 URL 로 만든다 |
| B. `openProjectPath` 문자열 하나로 교체 | 신원은 유일해진다(경로는 항상 유일하다). 그러나 **"지금 대시보드를 띄우고 있는가"** 를 알 수 없어 티어 변화가 재클릭에 반영되지 않는다(결정 OC2 가 필요로 하는 정보가 없다) → 두 번째 변수를 부르게 된다 |
| **C. 상태를 `openPanelTarget: PanelTarget \| null` 레코드 하나로** | **변수는 여전히 1개다.** 신원(`projectPath`)·내용(`dashboardKey`)·표시값(`displayName`·`emptyReason`)이 한 레코드에 모여 있고, "닫힘 ⇔ `null`" 이라는 SP15 의 형태가 그대로 유지된다. 레이어 간 통신을 명시적 타입으로 한다는 프로젝트 원칙과도 맞는다 |

> **결정 OC1 — 안 C.** `openDashboardKey`(문자열) → **`openPanelTarget`(레코드)** 로 개명·승격한다.
> - **신원은 `projectPath` 다.** 카드는 이미 전부 `data-project-path` 를 갖고 있고(898행,
>   `project.path` 는 서버가 경로를 키로 스냅샷을 만들므로 항상 비어 있지 않다), **드래그 순서
>   기능이 이미 그 속성을 프로젝트 신원으로 쓰고 있다**(1182·1184·1195행). 새 신원 개념을
>   도입하는 것이 아니라 **파일 안에 이미 있는 신원을 패널도 쓰게** 하는 것이다.
> - **`openPanelTarget === null` ⇔ 닫힘.** `body.dzh-panel-open` 은 여전히 그 값의 CSS 투영이며
>   JS 는 그 클래스를 읽지 않는다(SP 계약 유지).
> - `applyBackgroundInert()` 의 판정도 `openPanelTarget !== null` 로 바뀐다(1319행).
>
> **개명 범위는 아래 표가 전부다.** 머리 주석 2곳, JS 6곳, 검사 1곳.

| # | 지점 | 현재 | 변경 후 |
|---|------|------|---------|
| 1 | 머리 주석 38~39행(조항 ④) | `openDashboardKey 는 반드시 null 이다` | `openPanelTarget 은 반드시 null 이다` |
| 2 | 머리 주석 68~69행 | `열림 상태의 유일한 진실은 패널 IIFE 의 openDashboardKey` | `…의 openPanelTarget(PanelTarget \| null)` + 신원이 `data-project-path` 라는 한 문장 |
| 3 | 1291행 선언 | `var openDashboardKey = null;` | `var openPanelTarget = null;` |
| 4 | 1319행 | `openDashboardKey !== null && !pushModeQuery.matches` | `openPanelTarget !== null && …` |
| 5 | 1329·1333행 | 조기 반환·`wasClosed` | 결정 OC2 로 재작성 |
| 6 | 1352~1353행 | `if(openDashboardKey === null) return; openDashboardKey = null;` | `if(openPanelTarget === null) return; openPanelTarget = null;` |
| 7 | `tests/run.sh` T25-80(3587행) | 정방향 토큰 `'openDashboardKey'` | `'openPanelTarget'` |

### 결정 OC2 — SP8 을 개정한다: 조기 반환의 범위를 **「iframe 재로딩」으로 좁힌다**

결정 SP8 은 *"같은 카드 재클릭은 무동작(early return)"* 이고 그 **근거는 하나뿐**이다 —
*"재로딩으로 만들면 읽던 대시보드의 스크롤 위치가 초기화된다"*(`hub-detail-side-panel.md:313`).
즉 **보호 대상은 iframe 문서 하나**다. 제목·안내 줄·사유 문구·카드 표시를 다시 쓰는 것은
**비용이 0**(문자열 대입 1회 + 클래스 토글 2회)이고 스크롤과 무관하다.

함수 전체를 조기 반환하면 그 공짜 갱신까지 함께 막혀 **화면이 사실과 어긋난 채 굳는다**:

| 상황 | 현행(함수 전체 조기 반환) | OC2(재로딩만 건너뛴다) |
|------|---------------------------|------------------------|
| 열어 둔 프로젝트에서 `/dashboard init` 실행(티어 2→1 승격) | 배지는 `대시보드 추적` 으로 바뀌는데 패널은 계속 "기록 없음" 이라고 말한다. **재클릭도 무동작** → 닫고 다시 열어야 한다 | 재클릭 1회로 대시보드가 뜬다 |
| 열어 둔 프로젝트의 `dashboard.html` 삭제(1→2 강등) | 패널이 계속 옛 대시보드를 띄운다. 재클릭 무동작 | 재클릭 1회로 빈 상태로 넘어간다 |
| 티어 3→2 (사유 문구만 바뀜) | 옛 사유가 남는다 | 재클릭 1회로 새 사유 |
| **같은 카드 재클릭(정상)** | 무동작 | **iframe 을 건드리지 않는다 → 스크롤 유지.** 포커스도 옮기지 않는다(`wasClosed` 가드) |

> **결정 OC2 — 조기 반환을 없애고 `isSameDocument` 판정으로 바꾼다.**
> ```js
> var isSameDocument = !wasClosed
>   && openPanelTarget.projectPath === target.projectPath
>   && openPanelTarget.dashboardKey === target.dashboardKey;
> ```
> `dashboardKey` 를 함께 비교하는 이유: 경로가 같아도 **티어가 바뀌면 띄울 문서가 바뀐다**
> (`null` ↔ 키). 경로→키는 서버에서 순수 함수이므로(`hub_project.project_dashboard_key`) 이 두
> 필드 비교가 "같은 문서인가"의 **필요충분조건**이다.
> **SP8 의 목적(스크롤 보존)은 100% 유지되고, SP8 의 수단(함수 전체 반환)만 좁아진다.**
> 사용자에게 보이는 회귀는 없다 — 재클릭이 하는 일이 "아무것도 안 함"에서 "**보이는 값을 사실과
> 맞춤**"으로 바뀔 뿐이다. → **승인 항목 5.**

### 결정 OC3 — 불변식 SP15 재검증: **논리 무변경, 재진술만 필요**

SP15 의 불변식은 *"대기 중인 blank 타이머가 존재하면 `openDashboardKey === null` 이다"* 이고,
증명은 두 문장이다. 이번 변경이 그 증명의 어느 문장이라도 건드리는지 **한 줄씩 확인**했다.

| SP15 증명 문장 | 이번 변경의 영향 | 판정 |
|----------------|------------------|------|
| ① 타이머를 예약하는 곳은 `closeDetailPanel()` **한 곳**이고, 그 함수는 예약 직전에 상태를 `null` 로 만든다 | `scheduleBlankAfterSlide()` 호출부는 그대로 1곳. 상태 대입도 그 앞에 그대로 | **성립** |
| ② 상태가 `null` 이 아니게 되는 곳은 `openDetailPanel()` **한 곳**이고, 그 함수는 상태를 바꾸기 전에 타이머를 취소한다 | `clearTimeout` 은 함수 첫 줄로 **더 앞당겨진다**(OC2 로 조기 반환이 사라지므로 그 앞에 아무 분기가 없다) | **보존(오히려 강화)** |

**빈 상태로 교체할 때 `about:blank` 를 싣는 것은 불변식과 무관하다.** 그 로드는
`openDetailPanel()` 안에서 **동기 실행**이고 **타이머를 만들지 않는다.** 불변식이 말하는 것은
"대기 중인 **타이머**"이지 "iframe 이 blank 인지"가 아니다.

> **결정 OC3 — 불변식은 다음과 같이 재진술한다(논리 동일, 변수명만 교체).**
>
> > **대기 중인 blank 타이머가 존재하면 `openPanelTarget === null` 이다.**
>
> 여기에 **파생 사실 1개를 명시적으로 덧붙인다**(빠뜨리면 다음 사람이 두 번째 타이머를 만든다):
>
> > **`about:blank` 로 가는 경로는 둘이다** — 닫기(타이머, `PANEL_CLOSE_ANIMATION_MS` 지연)와
> > **대시보드 없는 프로젝트로의 교체(즉시, 타이머 없음)**. 지연이 필요한 쪽은 "나가는 패널이
> > 흰 화면으로 번쩍이지 않게" 하는 것뿐이므로(SP14), **열린 채 내용을 바꾸는 경로는 지연하지
> > 않는다** — 오히려 즉시 비워야 옛 대시보드가 1프레임도 남지 않는다(요구 S4).

**닫기 슬라이드 지연(180ms) 중에 들어온 클릭**도 다시 확인했다(1332행 경로):

| # | 지연 창 안 입력 | 결과 |
|---|-----------------|------|
| ① | 티어 1 카드 클릭 | 타이머 취소 → 재오픈. SP15 ①② 와 동일 |
| ② | **티어 2·3 카드 클릭** | `openPanelTarget === null` 이고 신원은 `projectPath`(비어 있지 않은 문자열)라 **조기 반환 대상이 아니다** → 정상 재오픈. **OC1 없이 R2 만 하면 여기서 패널이 영구히 나가 버린다**(OC1 의 두 번째 근거) |
| ③ | ESC·닫기 연타 | `closeDetailPanel()` 첫 줄이 잡는다 → 타이머 2중 예약 없음(SP15 ③④ 그대로) |
| ④ | 빈 상태를 닫는 도중 타이머 만료 | iframe 이 **이미 `about:blank`** 인데 한 번 더 `location.replace('about:blank')` 한다 → **무해**(같은 URL, `replace` 라 히스토리 없음, 시각 변화 없음). "이미 비어 있는가"를 추적하는 상태를 만들지 **않는다** — 새 변수와 새 동기화 규칙의 대가가 이득(무해한 호출 1회 회피)보다 크다(엣지 E9) |

### 결정 OC4 — 카드→패널 인계는 **`data-panel-empty-reason` 속성 1개**. 문구의 단일 출처는 `TIER_DESCRIPTION`

패널 IIFE 는 **스냅샷을 모른다**(결정 SP1). 이것은 취향이 아니라 **검사 T25-89 가 기계적으로
강제**하는 계약이다(`var PANEL_OPEN_BODY_CLASS` 줄부터 파일 끝까지 `snapshot` 문자열 0건).
따라서 "이 프로젝트에 대시보드가 없는 사유"를 패널까지 나르는 방법을 골라야 한다.

| 안 | 평가 |
|----|------|
| A. 카드에 `data-tier="2"` + **패널 IIFE 안에 티어→문구 맵** | 요구서가 제안한 형태. 그러나 **`TIER_DESCRIPTION` 과 같은 문구가 두 IIFE 에 두 벌 존재**한다 → 한쪽만 고치면 배지 툴팁과 패널 본문이 어긋나고, 그 어긋남을 잡을 수단이 없다. 게다가 **`file://` 의 티어 1**(대시보드는 있는데 서버가 꺼져 있다)을 표현할 수 없다 — 티어는 1 인데 사유는 티어와 무관하다 |
| B. `hub_model.py` 에 `panel_empty_reason` 필드 신설 | 문구가 파이썬으로 올라가 단일 출처가 된다. 그러나 **표현 계층 문구를 데이터 계약에 넣는 일**이고, 전제 1(파이썬 무변경)을 깬다. `isServed` 는 클라이언트만 아는 값이라 `file://` 사유는 여전히 서버가 만들 수 없다 |
| **C. 카드가 사유 문구 자체를 속성으로 나른다** | 렌더 IIFE 가 `TIER_DESCRIPTION`(이미 910행에서 배지 툴팁으로 쓰는 그 값)과 `FILE_MODE_DASHBOARD_HINT` 에서 문구를 만들어 속성에 싣는다. **맵이 한 벌뿐**이고, `file://` 티어 1 도 자연히 표현된다. 패널 IIFE 는 속성을 **읽어 그대로 표시**한다 — 티어라는 개념조차 몰라도 된다 |

> **결정 OC4 — 안 C.** 선례가 파일 안에 이미 있다: `data-tier1-previous`(902행)에 대해 머리 주석
> 44~46행이 스스로 이렇게 적고 있다 — *"그 값의 출처는 스냅샷이 아니라 클릭된 카드의
> `data-tier1-previous` 속성이다 — 패널 IIFE 가 스냅샷을 모른다는 결정 SP1 을 유지하기 위한
> 우회다."* **같은 우회를 같은 이유로 한 번 더 쓴다**(결정 GN4 의 "속성 하나가 CSS 스코프와 패널
> 인계를 겸한다"는 관례도 그대로 적용된다 — 이 속성은 `cursor:pointer` 스코프도 겸한다).
>
> ```js
> // 빈 상태 사유(결정 OC4) — 대시보드를 띄울 수 없는 이유를 카드에 실어 패널로 넘긴다.
> // 문구를 새로 만들지 않는다: 티어 사유는 배지 툴팁(910행)과 같은 TIER_DESCRIPTION 을,
> // 서버 미가동 사유는 카드 툴팁(900행)과 같은 FILE_MODE_DASHBOARD_HINT 를 쓴다 —
> // 한 화면에서 같은 사실을 두 문구로 말하지 않기 위한 단일 출처다.
> function panelEmptyReason(project, isClickable){
>   if(isClickable) return '';                                  // 띄울 대시보드가 있다
>   if(project.dashboard_key) return FILE_MODE_DASHBOARD_HINT;   // 티어 1 인데 서버가 꺼져 있다
>   return TIER_DESCRIPTION[project.tier] || '';                 // 대시보드 자체가 없다
> }
> ```
>
> **불변식 OC-A(이 함수가 유일한 강제 지점이다):**
> > 카드에서 **`data-dashboard-key` 의 부재 ⇔ `data-panel-empty-reason` 의 존재**.
> > 그래서 패널은 `dashboardKey === null` 하나만 보고 분기할 수 있고, "키도 없고 사유도 없는
> > 카드"는 만들어지지 않는다(만들어지면 `CLICKABLE_CARD_SELECTOR` 에 걸리지 않아 **클릭
> > 대상이 아니게** 되므로, 실패가 조용하지 않고 즉시 관측된다 — 승인 항목 2 를 기각할 때
> > 이 성질을 그대로 이용한다).
>
> `file://` 의 티어 1 카드에는 같은 문구가 `data-tooltip`(900행)과
> `data-panel-empty-reason` 두 자리에 실린다 — **상수 1개, 자리 2개, 목적 2개**(hover 즉답 /
> 패널 본문)이므로 문구 중복이 아니다.

### 결정 OC5 — 클릭 대상 판정은 **두 속성의 OR**. 이 식은 승인 결과와 무관하게 같다

```js
var CLICKABLE_CARD_SELECTOR = '[' + DASHBOARD_KEY_ATTRIBUTE + '],['
                            + PANEL_EMPTY_REASON_ATTRIBUTE + ']';
```

| 안 | 평가 |
|----|------|
| A. `'.card'` 또는 `'[data-project-path]'`(모든 카드) | 가장 짧다. 그러나 **승인 항목 2 를 기각하면**(`file://` 는 클릭 대상이 아니다) 식을 다시 바꿔야 하고, 그때 `.card` 는 "클릭되지만 아무 일도 안 하는 카드"를 되살린다 — 이 PRP 가 고치는 바로 그 실패 |
| B. 새 boolean 속성 `data-panel-openable` 신설 | 셀렉터가 1개로 짧아진다. 그러나 **다른 두 속성에서 파생되는 값**을 세 번째 속성으로 중복 기록한다 — 결정 GN4 가 세운 "속성 하나가 여러 역할을 겸한다"의 반대 방향이고, 세 값이 어긋날 자리를 만든다 |
| **C. 두 속성의 OR** | **"패널이 보여줄 것이 있는 카드"** 라는 뜻이 셀렉터에 그대로 적혀 있다(대시보드 또는 사유). 그리고 **승인 항목 2 의 채택/기각이 이 식을 바꾸지 않는다** — 바뀌는 것은 `panelEmptyReason()` 안의 한 줄뿐이다 |

> **결정 OC5 — 안 C.** CSS 도 같은 형태로 맞춘다(현재 119~122행 교체):
> ```css
>   /* 클릭 가능 ⇔ 패널에 보여줄 것이 있다(결정 OC5) — 대시보드(data-dashboard-key) 또는
>      대시보드가 없는 사유(data-panel-empty-reason). 옛 규칙은 전자만 봤고, 그래서 티어 2·3
>      카드 클릭이 조용히 실패했다(결정 N7 이 금지한 형태 — 이 PRP 의 출발점). 속성 존재만으로
>      커서를 바꿔 별도 클래스 없이 화면 상태와 데이터가 항상 일치하게 한다(결정 N2 의 관례). */
>   .card[data-dashboard-key],.card[data-panel-empty-reason]{cursor:pointer}
>   .card[data-dashboard-key]:hover,.card[data-panel-empty-reason]:hover{border-color:var(--accent)}
> ```
> 셀렉터가 3곳(cursor·hover·JS)에서 두 항이 되는 것이 유일한 비용이다. 그 대가로 **`.card` 를
> 통째로 클릭 대상이라고 선언하지 않는다** — 빈 허브의 `.empty`·`.warnings` 는 `.card` 가
> 아니므로 어차피 무관하지만(1124·1131·1140행), "무엇이 클릭되는가"를 데이터가 정하게 두는
> 편이 다음 변경에 안전하다.

### 결정 OC6 — `file://` 모드도 **빈 상태를 띄운다** (결정 N7 부분 대체, SP 초판 전제 3 대체)

N7 의 결론은 *"`file://` 은 클릭 대상이 아니다 + 툴팁으로 사유를 알린다"* 였고, **기술적 전제는
하나**였다 — *"`file://` 부모가 다른 디렉토리의 `file://` 문서를 iframe 으로 띄우는 동작은
브라우저마다 다르고, **조용히 빈 모달이 뜨는 것이 가장 나쁜 결과**다"*
(`hub-card-interactions-and-usage.md:716~718`).

**그 전제는 이번 설계에서 소멸한다.** 빈 상태에서는 iframe 을 `display:none` 으로 내리고
`about:blank` 만 싣는다(결정 OC7) — **애초에 `file://` 문서를 iframe 에 넣지 않는다.** N7 이
두려워한 "조용히 빈 프레임"은 **구조적으로 발생할 수 없다.**

| 안 | `file://` 티어 1 | `file://` 티어 2·3 | 평가 |
|----|------------------|--------------------|------|
| A. N7 유지 (클릭 대상 아님) | 툴팁만(현행) | **아무 반응 없음** — 사유도 툴팁도 없다 | **N7 이 금지한 조용한 실패가 `file://` 에 그대로 남는다.** 그리고 "서버 모드에서는 모든 카드가 답하는데 `file://` 에서는 일부만 답한다"는 **모드별 상호작용 차이**가 생긴다 — N7 자신이 새 탭 안을 기각한 이유가 바로 *"두 모드가 다른 상호작용을 갖는 것 자체가 학습 비용"* 이었다 |
| A′. N7 유지 + 티어 2·3 에도 툴팁 추가 | 툴팁 | 툴팁 | 조용한 실패는 없어진다. 그러나 **툴팁은 hover 전용**이라 키보드·터치에서 얻을 수 없고, 티어 사유는 이미 `.badge.tier` 툴팁(910행)에 있어 **같은 화면에 같은 문구가 3번** 생긴다 |
| **B. 모든 카드가 항상 클릭 대상 — 답은 대시보드이거나 사유다** | **빈 상태**(사유 = `FILE_MODE_DASHBOARD_HINT`, 즉 "서버를 켜세요") | **빈 상태**(사유 = `TIER_DESCRIPTION`) | 규칙이 **하나이고 예외가 없다.** `file://` 에서도 클릭이 항상 답을 준다. 툴팁은 그대로 남아 hover 즉답을 겸한다 |

> **결정 OC6 — 안 B 를 권고한다. → 승인 항목 2.**
> 근거를 셋으로 정리한다.
> 1. **N7 의 원칙(조용한 실패 금지)을 전면 관철하는 유일한 안이다.** 안 A 는 원칙을 절반만 지킨다.
> 2. **N7 의 기술적 전제가 사라졌다.** 빈 프레임이 뜰 수 없으므로 "가장 나쁜 결과"가 없다.
> 3. **비용이 사실상 0 이다.** `panelEmptyReason()` 의 두 번째 줄 하나가 이 결정 전부이고,
>    기각하면 그 줄을 `return '';` 로 바꾸는 것으로 끝난다(셀렉터·CSS·패널 코드는 무변경 —
>    결정 OC5 의 성질).
>
> **정직한 단서**: 안 B 를 채택하면 `file://` 로 허브를 연 사용자는 **어떤 카드를 눌러도 대시보드를
> 볼 수 없는 패널**을 만난다. 그 패널이 하는 일은 "서버를 켜세요"를 550px 폭으로 말하는 것뿐이다.
> 이것을 "쓸모없는 패널"로 볼 수도 있고 "왜 안 되는지 확실히 알려주는 패널"로 볼 수도 있다 —
> **그래서 이 항목은 사용자 판단으로 올린다.**

### 결정 OC7 — 빈 상태는 **패널 상태 클래스 1개 + CSS 가 무엇을 보일지 고른다**

| 안 | 평가 |
|----|------|
| A. JS 가 `panelFrameEl.hidden = true` / `panelEmptyEl.hidden = false` 두 곳을 토글 | 상태 하나를 **두 노드에 손으로 분배**한다. 한쪽을 빠뜨리면 사유 문구 아래에 빈 프레임이 남거나(로딩 중처럼 읽힌다) 대시보드 위에 사유가 겹친다 |
| B. 기존 `#dzh-detail-note` 를 재활용해 사유를 넣는다 | 노드가 늘지 않는다. 그러나 그 노드는 **한 줄 클램프 + 자리 항상 예약**이라는 다른 목적의 기하(PV2·PV3·PV4)를 갖고 있고, "이전 작업 안내"와 "대시보드 없음"이라는 **두 의미가 한 노드에** 산다. `--attention` 색도 사유에는 과하다 |
| **C. 패널에 `detail-empty` 상태 클래스 1개 → CSS 가 iframe 을 내리고 사유를 올린다** | JS 가 만지는 것은 **클래스 1개 + textContent 1개**다. "무엇을 보이게 할지"는 CSS 한 곳에 모인다 — `.theme-icon-moon`/`.theme-icon-sun` 을 CSS 가 고르는 결정 PV10 과 **같은 관례**다 |

> **결정 OC7 — 안 C.**
> ```css
>   /* 빈 상태(결정 OC7) — 대시보드가 없는 프로젝트를 열었을 때 iframe 대신 사유를 보인다.
>      상태 클래스는 패널에 붙이고 무엇을 보일지는 CSS 가 고른다(테마 아이콘 2개 중 하나를
>      CSS 가 고르는 결정 PV10 과 같은 관례) — JS 는 클래스 1개와 문구 1개만 만진다.
>      iframe 을 display:none 으로 내리는 것이 필수다: about:blank 만 실으면 --bg 와 다른
>      빈 면이 사유 아래에 남아 "로딩 중"으로 읽히고, 접근성 트리에도 빈 프레임의
>      aria-label("프로젝트 진행 대시보드")이 남아 낭독된다.
>      문구를 마크업에 고정하지 않는 것이 안내 줄(.detail-note, PV3)과 다른 점이다 —
>      사유가 프로젝트마다 다르므로 유일한 출처는 카드 속성이다(결정 OC4). */
>   .detail-empty-reason{display:none;margin:0;padding:0 16px;font-size:13px;color:var(--muted)}
>   .detail-panel.detail-empty .detail-frame{display:none}
>   .detail-panel.detail-empty .detail-empty-reason{display:block}
> ```
> - **좌측 정렬 · 헤드 바로 아래**에 둔다. 중앙 정렬은 "오류 화면"의 문법인데 이것은 오류가
>   아니라 **사실 보고**다. 좌우 패딩 `16px` 은 헤드·안내 줄과 같은 정렬 원점(SP11 ⑤)이다.
> - **`data-tooltip` 을 주지 않는다.** 사유는 클램프하지 않고 자유롭게 줄바꿈되므로 잘림이
>   없다 → 툴팁이 보존할 것이 없다. *"패널 안에는 원칙적으로 툴팁 트리거를 두지 않는다"*
>   (결정 UT1, 머리 주석 54~56행)는 규칙의 예외를 **하나에서 둘로 늘리지 않는다.**
> - **`aria-live`·`role="status"` 를 붙이지 않는다.** 패널을 열 때 포커스가 이미 패널 안(닫기
>   버튼)으로 들어가고 패널은 `role="dialog" aria-labelledby=…` 이므로 스크린리더는 제목→사유를
>   순서대로 읽는다. 라이브 영역을 얹으면 **같은 문장을 두 번** 말한다.
> - 빈 상태에서 `#dzh-detail-note` 는 **항상 숨김**이다(`tier1_is_previous_task` 는 티어 1 에서만
>   참이 될 수 있다) → 예약된 자리가 헤드-사유 간 여백을 그대로 겸한다(PV2 승계, 높이 불변).
>
> → **표현(평문 vs 카드 표면)은 승인 항목 3.**

### 결정 OC8 — 열린 카드 표시의 재적용은 **`dzh:rendered` 커스텀 이벤트**. `MutationObserver` 를 쓰지 않는다

카드는 폴링(60초)·틱(30초)·`dragend` 마다 `app.innerHTML` 대입으로 **전멸·재생성**된다
(1136행). 따라서 표시는 렌더 뒤에 **다시 붙어야** 한다. 후보는 셋이다.

| 안 | 얻는 것 | 잃는 것 |
|----|---------|---------|
| **A. 렌더 IIFE 가 `dzh:rendered` 를 발행하고 패널 IIFE 가 듣는다** | 발행 시점이 **DOM 이 최종 상태가 된 직후 정확히 1회**다(`insertAdjacentHTML` 다음). 계약이 **이름을 갖고 grep 되며**(T25-107), 렌더 IIFE 는 이벤트 이름만 알고 **패널을 모른다**. `isReordering` 조기 반환 구간에서는 발행되지 않는다 — 그 구간은 DOM 을 건드리지 않으므로 표시가 그대로 살아 있다 | 렌더 IIFE 에 1줄 추가. **같은 문자열 리터럴이 두 IIFE 에 각각 1개**(공유 스코프가 없다) → 검사가 "정확히 2회"를 세어 잠근다 |
| B. 패널 IIFE 에서 `#dzh-app` 을 `MutationObserver({childList:true})` 로 관찰 | 렌더 IIFE 를 **한 글자도 건드리지 않는다.** 이 파일에 옵저버 선례가 이미 있다(602·609행) | ① **렌더 1회에 2번 이상 발화한다**(`innerHTML` 대입 + `insertAdjacentHTML`) ② **드래그 중 `dragover` 의 `insertBefore` 마다 발화한다**(1207행) — 초당 수십 회, 전부 헛일이다(그 구간은 카드를 재생성하지 않는다) ③ 결합이 **렌더 쪽에서 보이지 않는다** — 나중에 렌더를 제자리 갱신(diff) 방식으로 바꾸면 발화 시점이 조용히 달라진다 ④ 이 파일의 기존 옵저버 둘은 *"표시 중인 툴팁을 닫는다"* 처럼 **정확히 "DOM 이 바뀌었다"가 관심사인** 경우다. 여기서 관심사는 "**렌더가 끝났다**"이고 그것은 옵저버가 표현할 수 없는 개념이다 |
| C. 패널이 `body` 에 `data-open-project-path` 를 쓰고 **렌더 IIFE 가 그것을 읽어 클래스를 직접 박는다** | 재렌더 시 표시가 **첫 페인트부터** 들어 있다 | 열거나 닫는 **그 순간**에는 렌더가 돌지 않으므로 패널 IIFE 도 표시를 붙여야 한다 → **한 가지 사실에 적용 지점이 둘**이 된다. 결정 SP14 가 `transitionend`+타이머 조합을 기각한 이유(*"하나의 일에 메커니즘이 둘이 된다"*)가 그대로 적용된다 |

> **결정 OC8 — 안 A.**
> **안 B 에 대한 정직한 정정**: "옵저버는 비동기라 표시가 한 프레임 깜빡인다"는 흔한 주장은
> **틀렸다** — `MutationObserver` 콜백은 마이크로태스크로 **같은 태스크 끝(페인트 전)** 에
> 실행되므로 깜빡임은 없다. 안 B 를 기각하는 이유는 깜빡임이 아니라 **헛발화(②)와 보이지 않는
> 결합(③④)** 이다.
>
> ```js
> // 렌더 IIFE (render() 말미, app.insertAdjacentHTML 다음)
> var RENDERED_EVENT_NAME = 'dzh:rendered';   // 패널 IIFE 가 같은 문자열로 듣는다(T25-107 이 2회를 센다)
> …
>   // 카드가 방금 전멸·재생성됐다는 사실만 알린다(결정 OC8). 누가 듣는지 이 IIFE 는 모르며
>   // detail 도 싣지 않는다 — 열린 카드가 무엇인지의 유일한 출처는 카드 DOM 이다(결정 OC4).
>   // CustomEvent 가 아니라 Event 인 이유: 실어 보낼 값이 없다.
>   document.dispatchEvent(new Event(RENDERED_EVENT_NAME));
> ```
> ```js
> // 패널 IIFE
> document.addEventListener(RENDERED_EVENT_NAME, applyOpenCardMarking);
> ```
> **이름에 콜론을 쓰는 이유**: 이 파일의 `dzh-` 접두사는 **id·class** 의 것이다(`dzh-app`·
> `dzh-panel-open`). `dzh:rendered` 는 셀렉터 문자열과 눈으로 구분되고, 커스텀 이벤트라는 점이
> 이름에서 드러난다.
>
> **불변식 H1⁗ 조항 ③ 이 이 결정 때문에 개정된다** → 결정 OC10.

### 결정 OC9 — 표시 채널은 **표면 틴트(`--accent-soft`) + `aria-current="true"`** (권고)

이 허브는 상태를 **두 채널 이상**으로 겹쳐 표현하는 관례가 확립돼 있다(결정 D4 — 배지의
텍스트+글리프+색). 열린 카드도 **시각 1채널 + 보조기술 1채널**로 만든다. 시각 채널의 후보는
이미 쓰이고 있는 채널을 피해야 한다:

| 이미 점유된 채널 | 사용처 |
|------------------|--------|
| `border-color:var(--accent)` | 카드 hover(122행) **와** `card-working`(128행) |
| `box-shadow` glow + `animation` | `card-working`(128~129행) |
| `outline:2px solid var(--accent);outline-offset:2px` | `.project-name:focus-visible`(153행)·`.detail-close:focus-visible`(336행) = **포커스 링** |
| `opacity:.55` | `card-dragging`(135행) |

| 시안 | CSS | 얻는 것 | 잃는 것 |
|------|-----|---------|---------|
| **A. 표면 틴트(권고)** | `.card.card-open{background:var(--accent-soft)}` | 선언 **1개**. 3열 격자를 훑을 때 **면**이 바뀌므로 가장 빨리 눈에 든다. hover·working·focus 어느 채널과도 충돌하지 않는다(속성이 겹치지 않아 소스 순서 의존도 없다) | 카드 **안쪽** 요소의 배경 대비가 바뀐다 — `.badge.tier{background:var(--soft)}`(160행)와 `.sessions li{border-top:1px solid var(--soft)}`(183행)가 라이트 테마에서 `--accent-soft`(#E4F0F8) 위의 `--soft`(#F4F7FB)가 되어 **세션 구분선이 흐려진다** |
| B. 좌측 액센트 바 | `.card.card-open{background:linear-gradient(var(--accent),var(--accent)) left/4px 100% no-repeat,var(--surface)}` | 안쪽 대비를 전혀 건드리지 않는다. 방향(좌→우 패널)과 의미가 맞는다 | 선언이 **읽기 어렵다**(그라디언트로 사각형을 그리는 관용구). 카드의 `border-radius:14px` 가 바를 상·하단에서 깎는다 |
| C. 굵은 outline | `.card.card-open{outline:3px solid var(--accent);outline-offset:2px}` | 레이아웃 영향 0, 안쪽 대비 영향 0 | **포커스 링과 같은 시각 언어**다 — 키보드 사용자가 "포커스"와 "열림"을 구분할 수 없다. 굵기만 다른 링은 구분 단서가 되지 못한다 |

> **결정 OC9 — 시안 A 를 권고한다. → 승인 항목 3.**
> A 의 비용을 감수하는 근거: 대비가 흐려지는 것은 **지금 패널로 그 내용을 보고 있는 그 카드
> 하나**뿐이다. 세션 목록·티어 배지는 우측 패널에서 더 크고 정확하게 읽히는 중이므로, 그 카드
> 안쪽의 미세 대비가 이 순간 가장 낮은 우선순위다. **후퇴 경로는 선언 1줄 교체**(A→B)다.
> 시각 채널과 별개로 **보조기술 채널은 무조건 붙인다**:
>
> ```js
> nameEl.setAttribute('aria-current', 'true');
> ```
> `aria-current="page"` 가 아닌 이유: 페이지 내비게이션이 아니라 **집합 안의 현재 항목**이다
> (`true` 가 그 뜻의 일반값이다). 값은 문자열 `'true'` 다 — ARIA 속성에 boolean 은 없다.

### 결정 OC10 — 불변식 **H1⁗ 조항 ③ 개정**. 이름은 **바꾸지 않는다**

현재 조항 ③(머리 주석 33~35행)은 이렇게 적혀 있다:

> *"③ 밀어내기는 body 의 클래스 하나로만 표현한다 — `#dzh-app` 이나 카드에는 패널 열림 상태를
> 반영하지 않는다. 반영하는 순간 매 렌더마다 그 상태를 다시 계산해야 하고, 두 IIFE 의 독립성이
> 깨진다."*

이 PRP 는 그 금지를 **정면으로 뒤집는다.** 다만 조항이 근거로 든 두 결과를 그대로 받아들이지는
않는다 — **하나는 사실이고 하나는 사실이 아니다.**

| 조항 ③ 의 근거 | 이번 설계에서 |
|----------------|---------------|
| "매 렌더마다 그 상태를 다시 계산해야 한다" | **사실이다.** 렌더마다 `applyOpenCardMarking()` 이 한 번 돈다 — `querySelectorAll` 1회 + 카드 N개 순회 1회. 카드 N ≤ 수십이고 30초·60초에 1회다. 이 파일이 이미 매 렌더 카드 N개를 **문자열로 조립**하고 있으므로(1136행) 무의미한 증분이다 |
| "두 IIFE 의 독립성이 깨진다" | **사실이 아니다.** 렌더 IIFE 는 패널의 존재도 상태도 모른다 — *"내가 다시 그렸다"* 만 말한다(OC8). 패널 IIFE 는 스냅샷을 여전히 모른다(T25-89 가 계속 강제). **공유 변수 0개, 함수 호출 0개**이며 매개는 이 파일이 이미 쓰는 매체(DOM 속성·`document` 이벤트)뿐이다 |

| 안 | 평가 |
|----|------|
| A. 불변식을 **개명**한다(H1⁗ → 다음 세대) | 조항이 반전됐으니 새 이름이 정직하다. 그러나 ① **프라임 계열이 소진됐다** — `′ ″ ‴ ⁗` 다음 글자가 없다(`H1⁵` 같은 임의 표기를 새로 발명해야 한다) ② 검사 **T25-81 이 머리 주석의 `H1⁗` 토큰을 요구**하고, 다른 PRP 5개가 그 이름으로 서로를 참조한다 → 개명 비용이 조항 1개 개정 비용의 몇 배다 |
| **B. 이름을 유지하고 조항 ③ 만 개정한다** | **직접 선례가 있다** — 결정 **PV5** 가 *"불변식 H1⁗ 의 안내 줄 조항을 좁힌다"* 를 개명 없이 했다. 조항 단위 개정이 이 저장소의 확립된 방식이다 |

> **결정 OC10 — 안 B.** 조항 ③ 을 다음으로 교체한다(개정 사실과 정본을 문장 안에 박는다):
>
> > *"③ **레이아웃 상태와 선택 상태는 사는 곳이 다르다.** 밀어내기(레이아웃)는 `body` 의 클래스
> > 하나로만 표현한다 — `#dzh-app` 이나 카드에 밀어내기를 반영하지 않는다. 반면 **어느 카드가
> > 열려 있는지(선택)는 그 카드에 반영한다**(`.card-open` + `.project-name` 의
> > `aria-current="true"`, 정본: `docs/prps/hub-panel-empty-state-and-open-card.md` 결정 OC9).
> > 카드는 렌더마다 전멸하므로 그 표시는 렌더 뒤에 다시 붙어야 한다 — 렌더 IIFE 는 자기가
> > 다시 그렸다는 사실만 `dzh:rendered` 로 알리고(값을 싣지 않는다), 패널 IIFE 가 카드 DOM 을
> > 보고 표시를 복원한다(결정 OC8). 두 IIFE 는 여전히 서로의 변수를 모른다 — 렌더 IIFE 는
> > 패널을, 패널 IIFE 는 스냅샷을 모른다."*
>
> 원칙 문장(35~36행 *"사용자 상태(포커스·접힘·선택·순서·열린 상세 패널)를 가진 것은 재렌더
> 대상 밖에 둔다"*)은 **그대로 둔다** — 카드의 선택 표시는 재렌더 대상 **안**에 있지만
> **재렌더 직후 복원된다**는 점을 조항 ③ 이 이미 설명하므로 원칙과 충돌하지 않는다.

### 결정 OC11 — 티어 2·3 의 `.project-name` 을 `<span>` → `<button>` 으로 올린다 (결정 N6 키보드 행 개정, **사용자 판단 항목**)

N6 은 두 행으로 키보드 접근을 정했다:
- *"키보드 — `.project-name` 을 `<button class="project-name">` 으로 바꾼다. Tab 도달·Enter/Space
  동작·포커스 링을 공짜로 얻는다"*
- *"티어 2·3 카드(`dashboard_key` 없음) — 클릭 핸들러가 조기 반환. `cursor:pointer` 를 주지 않고
  `.project-name` 도 버튼이 아닌 `<span>` 으로 렌더한다(**없는 기능을 있는 척하지 않는다**)"*

R2 가 채택되면 **두 번째 행의 전제가 사라진다** — 티어 2·3 에도 이제 "있는 기능"이 생긴다.
그러면 `<span>` 을 유지하는 것은 **마우스로는 되는데 키보드로는 안 되는 기능**을 만드는 일이다.

| 안 | 얻는 것 | 잃는 것 |
|----|---------|---------|
| **A. 모두 `<button>`(권고)** | **카드당 Tab 정지점이 정확히 1개**로 통일된다 — 키보드 사용자가 "이 카드는 되고 저 카드는 안 되는" 예측 불가를 겪지 않는다. Enter/Space·포커스 링을 공짜로 얻는다. `aria-current` 가 **포커스 가능한 요소**에 붙으므로 스크린리더 사용자가 순회 중 "현재 항목"을 실제로 만난다. `renderProjectNameEl` 의 `isClickable` 매개변수가 사라져 함수가 3줄에서 1줄로 줄어든다 | **Tab 정지점이 카드 수만큼 늘어난다.** 티어 3("세션 없음") 프로젝트가 많은 사용자에게는 헤더 버튼 2개 뒤로 정지점이 20~40개 이어진다 — 사용량 패널·닫기 버튼까지 Tab 으로 가려면 그만큼 지나가야 한다 |
| B. `<span>` 유지(마우스만) | Tab 정지점이 지금과 같다(티어 1 카드 수만큼) | **키보드 접근성 격차를 새로 만든다.** 티어 2·3 의 사유는 키보드·터치 사용자가 **얻을 방법이 전혀 없다** — `.badge.tier` 툴팁도 `<span>` 이라 포커스되지 않는다(910행). WCAG 2.1.1(키보드) 관점에서 "마우스 전용 기능"이 된다 |
| C. `<button>` + 티어 2·3 만 `tabindex="-1"` | 정지점이 안 늘고 클릭은 된다 | **가장 나쁜 조합이다.** 포커스 받지 못하는 `<button>` 은 보조기술에 "버튼이 있다"고 말하면서 도달을 막는다. 결정 DG2·DG3 이 드래그 핸들을 `<button>` 으로 만들지 않은 이유(*"포커스는 받지만 Enter/Space 에 반응하지 않는 컨트롤은 보조기술 사용자에게 거짓말"*)의 대칭 사례다 |

> **결정 OC11 — 안 A 를 권고하되 이것은 사용자 판단 항목이다. → 승인 항목 4.**
> 권고 근거: 이 PRP 의 목적이 *"클릭이 조용히 실패하지 않게 한다"* 인데, 안 B 는 **그 실패를
> 키보드 사용자에게 남겨 둔다**(마우스에서만 고쳐진다). 그리고 "정지점이 늘어난다"는 비용은
> **카드가 많을 때만** 발생하는데, 카드가 많을 때야말로 R1(어느 카드가 열렸는지)이 필요해지는
> 상황이므로 두 요구가 같은 사용자를 향한다.
> **안 B 를 택하면** 이 PRP 는 다음을 함께 확정해야 한다(빠뜨리면 문서가 화면과 어긋난다):
> ① `renderProjectNameEl` 의 `isClickable` 분기 유지 ② `.project-name` 주석 145~147행 유지
> ③ N6 키보드 행을 「**유지 — 단 티어 2·3 은 의도적으로 키보드 비대상**」으로 이관 표기하고
> **그 격차를 알려진 한계로 `hub/README.md` 에 적는다**(조용히 두지 않는다)
> ④ `aria-current` 는 `<span>` 에도 유효하므로 R1 은 두 안 모두에서 그대로 동작한다.

### 결정 OC12 — 주석·문서 정합 (거짓말을 남기지 않는다)

| 위치 | 현재 | 변경 후 |
|------|------|---------|
| 머리 주석 33~35행 | 조항 ③(카드에 반영 금지) | **교체** — 결정 OC10 의 문장 |
| 머리 주석 38~39행 | `openDashboardKey 는 반드시 null 이다` | `openPanelTarget 은 반드시 null 이다` |
| 머리 주석 66~69행 | `…열림 상태의 유일한 진실은 패널 IIFE 의 openDashboardKey 이며 body.dzh-panel-open 은 그 값의 CSS 투영이다.` | 문단 말미 교체 — `열림 상태의 유일한 진실은 패널 IIFE 의 openPanelTarget(PanelTarget \| null)이며, 신원은 data-project-path 다(대시보드 없는 프로젝트는 dashboard_key 가 전부 null 이라 신원이 될 수 없다 — 결정 OC1). body.dzh-panel-open 은 openPanelTarget !== null 의 CSS 투영이다. 대시보드가 없는 프로젝트는 iframe 을 내리고 사유를 보인다 — 사유의 유일한 출처는 카드의 data-panel-empty-reason 이다(결정 OC4·OC7).` |
| CSS 주석 119~120행 | `클릭 가능 ⇔ dashboard_key 존재(결정 N2)` | **교체** — 결정 OC5 의 주석 |
| CSS 주석 145~147행 | `티어 2·3 은 <span> 으로 렌더돼(R3) 이 리셋은 그때 아무 효과가 없다` | **OC11 안 A 채택 시 교체** — `모든 카드가 <button> 이므로 이 리셋은 항상 유효하다(결정 OC11).` / 안 B 채택 시 무수정 |
| JS 주석 878~879행 | `클릭 가능(티어 1 + 서버 모드)이면 <button>, 아니면 <span> — 없는 기능을 있는 척하지 않는다(결정 N6)` | **OC11 안 A 채택 시 교체** — `모든 카드가 패널을 열므로 제목은 항상 <button> 이다(결정 OC11). 티어 2·3 은 대시보드 대신 사유를 띄운다 — 없는 기능을 있는 척하는 것이 아니라 있는 기능을 키보드에도 준다.` |
| JS 주석 671행 | `프로젝트 대시보드 모달(R3, 결정 N7) — file:// 모드는 클릭 대상에서 빠지고 사유를 안내한다.` | `프로젝트 상세 패널(결정 N7 → OC6) — file:// 모드는 대시보드를 띄울 수 없고, 그 사유를 툴팁과 패널 빈 상태 두 곳에서 같은 문구로 알린다.` (**"모달" 이라는 낡은 낱말도 함께 사라진다** — T25-81 이 README 에서만 잡던 잔재다) |
| 패널 IIFE 머리 주석 1264~1267행 | `카드가 data-dashboard-key 를 가졌는지만으로 클릭 가능 여부가 정해지므로(결정 N2·N7)` | `카드가 data-dashboard-key(대시보드) 또는 data-panel-empty-reason(사유)을 가졌는지로 클릭 가능 여부가 정해지므로(결정 OC5)` |
| `hub/README.md` 195~196행 | `티어 1(대시보드 추적) 카드를 클릭하면…` | `**어느 카드든** 클릭하면 상세 패널이 열린다 — 티어 1 은 그 프로젝트의 .claude/dashboard.html 을, 나머지는 대시보드가 없는 사유를 보여준다.` |
| 〃 202~204행 | `file:// … 티어 2·3 카드는 서버 모드에서도 클릭 대상이 아니다(그 프로젝트에는 대시보드 자체가 없다).` | **교체**(OC6 채택 시) — `티어 2·3 카드에는 대시보드가 없으므로 패널이 그 사유를 보여준다. file:// 로 열었을 때는 어떤 대시보드도 띄울 수 없어 모든 카드가 "허브 서버를 켜면…" 안내를 패널과 툴팁 두 곳에서 같은 문구로 알린다.` |
| 〃 「프로젝트 상세 패널」 절 말미 | — | **1문장 추가** — `지금 패널에 열려 있는 프로젝트의 카드는 배경색으로 표시된다 — 다른 카드를 누르면 표시가 그 카드로 옮겨간다.` |

---

## 정본 이관 표기 (원문 삭제 없이 화살표만 덧붙인다)

| 파일 | 지점 | 덧붙일 표기 |
|------|------|-------------|
| `hub-card-interactions-and-usage.md` | 결정 **N2** | `→ **부분 대체(2026-08-19)** — "클릭 가능 ⇔ 티어 1" 은 「**대시보드를 띄울 수 있다** ⇔ 티어 1」로 좁혀진다. 클릭 가능 여부는 더 이상 티어가 정하지 않는다(모든 카드가 클릭 대상 — hub-panel-empty-state-and-open-card.md 결정 OC5·OC6). dashboard_key 를 티어 1 만 갖는다는 파생 규칙 자체는 **무변경**이다` |
| 〃 | 결정 **N6** | `→ **부분 개정(2026-08-19)** — 드래그 핸들·텍스트 선택 가드는 그대로 유지된다. 그러나 「티어 2·3 카드(dashboard_key 없음) → 클릭 핸들러가 조기 반환」 행은 **폐기된다** — 모달이 배경을 inert 로 만들던 시절에는 피해가 없었으나, 비모달 패널(SP1)에서는 그 조기 반환이 "다른 프로젝트의 대시보드가 남아 있는" 오해를 만든다. 키보드 행의 정본은 결정 OC11` |
| 〃 | 결정 **N7** | `→ **부분 대체(2026-08-19, 승인 항목 2 채택 시)** — "아무 반응 없는 클릭(조용한 실패)을 만들지 않는다"는 원칙은 **강화된다**. 바뀌는 것은 수단이다: 「클릭 대상에서 제외 + 안내 툴팁」 → 「클릭하면 패널이 사유를 보여준다(툴팁은 유지)」. N7 이 근거로 든 "조용히 빈 모달이 뜨는 것이 가장 나쁜 결과"라는 전제는 iframe 을 아예 내리는 결정 OC7 이 제거한다` |
| 〃 | 리스크 **R-11** | `→ **재평가(2026-08-19)** — "티어 1 → 2 강등 시 카드가 갑자기 클릭 불가가 된다"는 리스크 자체가 소멸한다(강등돼도 클릭되고 사유가 뜬다). 대신 새 한계가 생긴다: 열려 있는 패널은 강등·승격을 자동 반영하지 않고 재클릭 1회를 요구한다(엣지 E7 · 결정 OC2)` |
| `hub-detail-side-panel.md` | 결정 **SP6**(H1⁗) **조항 ③** | `→ **개정(2026-08-19)** — 카드에 **선택 상태**를 반영한다. 밀어내기(레이아웃)를 body 클래스 하나로만 표현하는 절반은 그대로다. 조항 ③ 이 근거로 든 "두 IIFE 의 독립성이 깨진다"는 성립하지 않는다(값을 싣지 않는 이벤트 1개로 해결된다). 정본은 hub-panel-empty-state-and-open-card.md 결정 OC10. **불변식 이름은 H1⁗ 로 유지된다**(조항 단위 개정 — PV5 선례)` |
| 〃 | 결정 **SP8** | `→ **개정(2026-08-19)** — 조기 반환의 범위가 「함수 전체」에서 「**iframe 재로딩**」으로 좁아진다. 보호 대상(읽던 대시보드의 스크롤 위치)은 그대로 지켜지고, 제목·사유·카드 표시는 재클릭마다 다시 쓰여 티어 변화가 1회 클릭으로 반영된다. 정본은 결정 OC2` |
| 〃 | 결정 **SP15** | `→ **재진술(2026-08-19, 논리 무변경)** — 불변식은 "대기 중인 blank 타이머가 존재하면 **openPanelTarget === null** 이다"로 변수명만 바뀐다. 대시보드 없는 프로젝트로 교체할 때 about:blank 를 **즉시**(타이머 없이) 싣는 경로가 새로 생기지만, 불변식이 말하는 것은 타이머의 존재이지 iframe 의 내용이 아니므로 증명 두 문장이 그대로 성립한다. 정본은 결정 OC3` |
| 〃 | 초판 **확정된 전제 3**(`file://` 비대상) | `→ **대체(2026-08-19, 승인 항목 2 채택 시)** — file:// 모드도 클릭 대상이며 패널은 빈 상태로 사유를 보여준다. 정본은 결정 OC6` |
| 〃 | **YAGNI 보류 2**(열린 프로젝트 카드 하이라이트) | `→ **해소(2026-08-19)** — 재방문 트리거가 발동했다(신고 형태는 예고보다 나빴다: "어느 걸 열었더라"가 아니라 "열린 것을 다른 프로젝트로 오해했다"). 보류의 기각 근거였던 "강조를 유지하려면 렌더 IIFE 가 패널 상태를 알아야 한다"는 **틀렸다** — 렌더 IIFE 는 값 없는 이벤트 1개만 발행하고 패널 상태를 모른다. 정본은 결정 OC8·OC9` |
| 〃 | 리스크 **P-6**(iframe 폴링 미정지) | `→ **승계·강화** — 대시보드 없는 프로젝트로 교체할 때 about:blank 를 즉시 싣기 때문에, 옛 프로젝트의 5초 폴링이 닫기를 기다리지 않고 그 자리에서 멈춘다` |
| `hub-panel-polish-and-icons.md` | 결정 **PV5** | `→ **선례 인용(2026-08-19)** — 불변식을 개명 없이 조항 단위로 개정한 전례. 결정 OC10 이 같은 방식을 쓴다` (이 파일은 수정하지 않는다 — 인용은 이 문서에서만 한다) |

---

## DOM / CSS / JS 계약 (변경 후)

### 새로 생기는 계약

| 이름 | 소유 | 의미 |
|------|------|------|
| `data-panel-empty-reason` | 카드(렌더 IIFE 가 쓴다) | **대시보드를 띄울 수 없는 사유 문구.** 존재 ⇔ `data-dashboard-key` 부재(불변식 OC-A). `cursor:pointer` 스코프와 패널 인계를 겸한다(결정 GN4 관례) |
| `.card.card-open` | 카드(패널 IIFE 가 쓴다) | 지금 상세 패널에 열려 있는 프로젝트의 카드. **렌더마다 사라지고 `dzh:rendered` 뒤에 복원된다** |
| `aria-current="true"` | `.project-name`(패널 IIFE 가 쓴다) | 위 시각 표시의 보조기술 채널 |
| `.detail-panel.detail-empty` | 패널(패널 IIFE 가 쓴다) | 빈 상태. iframe 을 내리고 사유를 올린다(CSS 가 고른다) |
| `#dzh-detail-empty-reason` | 정적 마크업 | 사유 문구 노드. `#dzh-detail-panel` 의 자식이므로 **정적 노드 목록(머리 주석 20~24행)에 이미 포함**돼 있다 |
| `dzh:rendered` | 렌더 IIFE 발행 / 패널 IIFE 수신 | *"`#dzh-app` 을 다시 그렸다"* 만 뜻한다. **`detail` 을 싣지 않는다** |

### 데이터 모델 — `PanelTarget` (카드 DOM ↔ 패널 상태의 유일한 통신 형식)

```js
/**
 * PanelTarget — 카드 하나가 패널에 넘기는 값 전부. 패널 IIFE 는 이 레코드만 보고 동작하며
 * 스냅샷·티어 개념을 모른다(결정 SP1 유지, 검사 T25-89).
 *
 * projectPath   : string          — 신원. 항상 유일하고 비어 있지 않다(결정 OC1)
 * dashboardKey  : string | null   — null 이면 띄울 대시보드가 없다(⇔ emptyReason !== '')
 * displayName   : string          — 패널 제목
 * emptyReason   : string          — 빈 상태 문구. '' 이면 빈 상태가 아니다
 * isPreviousTask: boolean         — 이전 작업 안내 줄 표시 여부(결정 GN6)
 * openerElement : Element         — 닫을 때 포커스를 되돌릴 요소
 */
```

### JS 상태 모델 (패널 IIFE 스코프, 전역 없음)

| 이름 | 타입 | 의미 |
|------|------|------|
| `openPanelTarget` | `PanelTarget \| null` | **열림 상태의 유일한 진실.** `null` 이면 닫힘. `body.dzh-panel-open` 은 이 값의 CSS 투영이다 |
| `panelOpenerElement` | `Element \| null` | 닫을 때 포커스를 되돌릴 요소(무변경) |
| `panelBlankTimerId` | `number \| null` | 닫기 슬라이드 뒤 iframe 을 비울 예약(무변경). **존재 ⇒ `openPanelTarget === null`**(불변식, 결정 OC3) |
| `pushModeQuery` | `MediaQueryList` | 무변경 |

### 공개 함수 시그니처 (템플릿 JS)

```js
// 렌더 IIFE
function panelEmptyReason(project: object, isClickable: boolean): string
    /** 대시보드를 띄울 수 없는 사유 문구를 만든다. 띄울 수 있으면 빈 문자열. 순수 함수(OC4). */

// 패널 IIFE
function readCardTarget(cardElement: Element): PanelTarget
    /** 카드 DOM 하나에서 패널이 필요한 값 전부를 읽어 PanelTarget 을 만든다(OC4). */

function findCardByProjectPath(projectPath: string): Element | null
    /** 그 경로의 카드를 찾는다. 없으면 null(스냅샷에서 사라진 프로젝트 — 엣지 E6). */

function applyOpenCardMarking(): void
    /** 열린 프로젝트의 카드에만 선택 표시를 남긴다. 상태 전체를 다시 그리는 단일 적용점(OC8·OC9). */

function openDetailPanel(target: PanelTarget): void
    /** 패널을 열거나 내용을 바꾼다. 같은 문서면 iframe 재로딩만 건너뛴다(OC2). */

// 시그니처 무변경: projectDashboardUrl · loadPanelDocument · scheduleBlankAfterSlide
//                  applyBackgroundInert · closeDetailPanel
```

---

## 구현 계획 (파일별)

### 1. `hub/bin/hub_template.html` — 머리 주석 (33~35 · 38~39 · 66~69행)

결정 OC10·OC12 의 문장으로 교체한다. **`H1⁗` 토큰은 그대로 남는다**(T25-81).

### 2. `hub/bin/hub_template.html` — CSS

**(2-a) 카드 클릭·선택 (119~122행 교체 + 규칙 1개 신설)**

```css
  /* 클릭 가능 ⇔ 패널에 보여줄 것이 있다(결정 OC5) — 대시보드(data-dashboard-key) 또는
     대시보드가 없는 사유(data-panel-empty-reason). 옛 규칙은 전자만 봤고, 그래서 티어 2·3
     카드 클릭이 조용히 실패했다(결정 N7 이 금지한 형태 — 이 PRP 의 출발점). 속성 존재만으로
     커서를 바꿔 별도 클래스 없이 화면 상태와 데이터가 항상 일치하게 한다(결정 N2 의 관례). */
  .card[data-dashboard-key],.card[data-panel-empty-reason]{cursor:pointer}
  .card[data-dashboard-key]:hover,.card[data-panel-empty-reason]:hover{border-color:var(--accent)}
  /* 지금 상세 패널에 열려 있는 카드(결정 OC9). 표면(background)을 쓰는 이유는 다른 채널이
     전부 점유돼 있기 때문이다 — border-color 는 hover 와 card-working, box-shadow+animation 은
     card-working, outline 2px 은 포커스 링(.project-name:focus-visible)이 이미 쓴다. 속성이
     겹치지 않으므로 아래 .card.card-working 규칙과의 소스 순서에 의존하지 않는다(둘 다 붙으면
     테두리·glow 는 working 이, 면색은 이 규칙이 담당한다). 보조기술 채널은 JS 가 붙이는
     aria-current="true" 다 — 색 하나에 의존하지 않는다(결정 D4 의 다중 채널 관례). */
  .card.card-open{background:var(--accent-soft)}
```

**(2-b) 빈 상태 (`.detail-frame` 338행 뒤 삽입)** — 결정 OC7 의 CSS 3줄 + 주석.

> `.detail-note`(347~350행) **뒤가 아니라 `.detail-frame` 뒤**에 둔다: 패널 CSS 블록이
> 「패널 → 헤드 → 제목 → 닫기 → 프레임 → 안내 줄」 순인데, 빈 상태 규칙은 **프레임과
> 사유의 관계**를 정의하므로 프레임 규칙에 붙는 편이 읽는 순서와 맞는다.

### 3. `hub/bin/hub_template.html` — DOM (417행 뒤, `<iframe>` 바로 앞)

```html
  <p id="dzh-detail-empty-reason" class="detail-empty-reason"></p>
```

문구를 마크업에 넣지 않는다(프로젝트마다 다르다 — 결정 OC7). `#dzh-detail-note` 다음, `<iframe>`
앞이라 빈 상태에서 프레임이 사라진 자리에 사유가 그대로 들어선다.

### 4. `hub/bin/hub_template.html` — 렌더 IIFE

**(4-a) 상수 1개** — `FILE_MODE_DASHBOARD_HINT`(672행) 다음:

```js
  // 카드를 다시 그렸다는 사실만 알리는 이벤트 이름(결정 OC8). 패널 IIFE 가 같은 문자열로
  // 듣는다 — 공유 스코프가 없어 리터럴이 두 곳에 살고, T25-107 이 "정확히 2회"를 센다.
  var RENDERED_EVENT_NAME = 'dzh:rendered';
```

**(4-b) `panelEmptyReason()` 신설** — `renderProjectNameEl`(880행) 앞. 결정 OC4 의 코드 그대로.

**(4-c) `renderProjectNameEl` 개정 (878~884행)** — OC11 안 A 채택 시:

```js
  // 모든 카드가 패널을 열므로 제목은 항상 <button> 이다(결정 OC11). 티어 2·3 은 대시보드 대신
  // 사유를 띄운다 — 없는 기능을 있는 척하는 것이 아니라, 있는 기능을 키보드에도 주는 것이다.
  // data-tooltip 은 R4 로 제거됐다: 보이는 텍스트와 같아 정보 손실이 없다(결정 X1).
  function renderProjectNameEl(project){
    return '<button class="project-name" type="button">'
      + escapeHtml(project.display_name) + '</button>';
  }
```

`isClickable` 매개변수가 사라지므로 **호출부(909행)도 함께 고친다** — 내 변경이 만든 고아를
남기지 않는다. 안 B 채택 시 이 항목은 **무수정**이다.

**(4-d) `renderProject` 속성 1개 추가 (896~902행)**

```js
    // 클릭 가능 ⇔ dashboard_key 존재 + 서버 모드(결정 N2·N7). file:// 에서는 안내 툴팁도 준다.
    var isClickable = Boolean(project.dashboard_key) && isServed;
    var emptyReason = panelEmptyReason(project, isClickable);      // 결정 OC4
    var cardAttrs = ' data-project-path="' + escapeHtml(project.path || '') + '"';
    if(isClickable) cardAttrs += ' data-dashboard-key="' + escapeHtml(project.dashboard_key) + '"';
    else cardAttrs += ' data-panel-empty-reason="' + escapeHtml(emptyReason) + '"';
    if(project.dashboard_key && !isClickable){
      cardAttrs += ' data-tooltip="' + escapeHtml(FILE_MODE_DASHBOARD_HINT) + '"';
    }
```

- `escapeHtml` 은 따옴표까지 이스케이프한다(921~924행) → 속성 자리에 안전하다(검수 m3 의 근거
  그대로).
- **`else` 로 붙이는 것이 불변식 OC-A 의 강제 지점이다** — 키가 없으면 사유가 반드시 붙는다.
  `emptyReason` 이 `''` 인 경우는 `isClickable` 이 참일 때뿐이므로 `else` 분기에서는 빈 문자열이
  나올 수 없다.
- 옛 `else if(project.dashboard_key)` 툴팁 분기를 **독립 `if` 로 분리**한다 — 사유 속성과 툴팁은
  이제 **함께** 붙어야 한다(`file://` 티어 1).

**(4-e) `render()` 말미 1줄 (1142행 `insertAdjacentHTML` ~ 1144행 다음)**

```js
    app.insertAdjacentHTML('beforeend', extra);
    renderConnectionStatus();
    renderUsagePanel(snapshot.usage, snapshot.rate_limit_resets);
    // 카드가 방금 전멸·재생성됐다(1136행). 누가 듣는지 이 IIFE 는 모르며 값도 싣지 않는다 —
    // 열린 카드가 무엇인지의 유일한 출처는 카드 DOM 이다(결정 OC8). 위 두 줄보다 뒤에 둘
    // 이유는 없지만 insertAdjacentHTML 보다는 반드시 뒤여야 한다(그때 DOM 이 최종이다).
    document.dispatchEvent(new Event(RENDERED_EVENT_NAME));
```

> **두 조기 반환에는 발행하지 않는다 — 의도된 것이다.**
> ① `isReordering`(1122행): DOM 을 **건드리지 않고** 반환하므로 표시가 그대로 살아 있다.
> ② `initialParseError`(1123~1128행): `.empty` 하나만 남으므로 **표시할 카드가 없다**
> (`applyOpenCardMarking()` 을 불러도 하는 일이 없다). 두 경우 모두 발행이 무의미하므로
> 분기를 만들지 않는다("일어날 수 없거나 무해한 상황에 코드를 넣지 않는다").

### 5. `hub/bin/hub_template.html` — 패널 IIFE (1263~1380행)

**(5-a) 상수 (1268~1279행 구간에 추가)**

```js
  var PROJECT_PATH_ATTRIBUTE = 'data-project-path';        // 패널의 신원 키(결정 OC1)
  var PANEL_EMPTY_REASON_ATTRIBUTE = 'data-panel-empty-reason';   // 결정 OC4
  var CLICKABLE_CARD_SELECTOR = '[' + DASHBOARD_KEY_ATTRIBUTE + '],['
                              + PANEL_EMPTY_REASON_ATTRIBUTE + ']';   // 결정 OC5
  var OPEN_CARD_CLASS = 'card-open';                       // 결정 OC9
  var OPEN_CARD_ARIA_ATTRIBUTE = 'aria-current';
  var OPEN_CARD_ARIA_VALUE = 'true';
  var PANEL_EMPTY_CLASS = 'detail-empty';                  // 결정 OC7
  var PROJECT_NAME_SELECTOR = '.project-name';
  var RENDERED_EVENT_NAME = 'dzh:rendered';                // 렌더 IIFE 가 같은 문자열로 발행한다
```

**(5-b) 노드·상태**

```js
  var panelEmptyReasonEl = document.getElementById('dzh-detail-empty-reason');
  …
  if(!panelEl || !panelTitleEl || !panelNoteEl || !panelEmptyReasonEl
     || !panelFrameEl || !panelCloseButton || !mainEl) return;
  …
  var openPanelTarget = null;   // 열림 상태의 유일한 진실. body 클래스는 이 값의 투영이다(OC1)
```

**(5-c) 카드 읽기 (`readCardTarget`)** — `projectDashboardUrl` 앞에 둔다.

```js
  // 카드 DOM 하나에서 패널이 필요한 값 전부를 읽는다(결정 OC4). 이 함수가 이 IIFE 의 유일한
  // 입력 경계다 — 여기서 아래로는 PanelTarget 만 흐르고, 티어라는 개념은 등장하지 않는다.
  // getAttribute 는 없는 속성에 null 을 준다 → dashboardKey 가 자연히 string|null 이 된다.
  function readCardTarget(cardElement){
    var nameEl = cardElement.querySelector(PROJECT_NAME_SELECTOR);
    return {
      projectPath: cardElement.getAttribute(PROJECT_PATH_ATTRIBUTE),
      dashboardKey: cardElement.getAttribute(DASHBOARD_KEY_ATTRIBUTE),
      displayName: nameEl ? nameEl.textContent : '',
      emptyReason: cardElement.getAttribute(PANEL_EMPTY_REASON_ATTRIBUTE) || '',
      isPreviousTask: cardElement.getAttribute(TIER1_PREVIOUS_TASK_ATTRIBUTE) === '1',
      openerElement: nameEl || cardElement
    };
  }
```

**(5-d) 카드 찾기·표시 (`applyBackgroundInert` 뒤)**

```js
  // 속성 셀렉터를 조립하지 않고 순회 비교한다 — 프로젝트 경로는 임의의 파일시스템 문자열이라
  // 큰따옴표·역슬래시가 들어오면 [data-project-path="…"] 가 깨진다. CSS.escape 를 쓰는 대신
  // 비교로 끝내면 이스케이프 규칙이 아예 없다. 카드 수는 수십 단위다.
  function findCardByProjectPath(projectPath){
    var cards = document.querySelectorAll('.card[' + PROJECT_PATH_ATTRIBUTE + ']');
    for(var i = 0; i < cards.length; i++){
      if(cards[i].getAttribute(PROJECT_PATH_ATTRIBUTE) === projectPath) return cards[i];
    }
    return null;
  }

  // 열린 프로젝트의 카드에만 표시를 남긴다(결정 OC9). 증분으로 붙이고 떼지 않고 매번 상태
  // 전체를 다시 그린다 — 적용 지점이 하나면 "표시가 두 카드에 남았다" 같은 상태가 존재할 수
  // 없다. 카드는 렌더마다 새 노드라 재렌더 직후에는 첫 루프가 0회 돈다(결정 OC8).
  function applyOpenCardMarking(){
    var markedCards = document.querySelectorAll('.' + OPEN_CARD_CLASS);
    for(var i = 0; i < markedCards.length; i++){
      markedCards[i].classList.remove(OPEN_CARD_CLASS);
      var markedNameEl = markedCards[i].querySelector(PROJECT_NAME_SELECTOR);
      if(markedNameEl) markedNameEl.removeAttribute(OPEN_CARD_ARIA_ATTRIBUTE);
    }
    if(openPanelTarget === null) return;
    var card = findCardByProjectPath(openPanelTarget.projectPath);
    if(!card) return;          // 스냅샷에서 사라진 프로젝트 — 패널은 강제로 닫지 않는다(엣지 E6)
    card.classList.add(OPEN_CARD_CLASS);
    var nameEl = card.querySelector(PROJECT_NAME_SELECTOR);
    if(nameEl) nameEl.setAttribute(OPEN_CARD_ARIA_ATTRIBUTE, OPEN_CARD_ARIA_VALUE);
  }
```

**(5-e) `openDetailPanel` 교체 (1326~1346행)**

```js
  // 패널을 열거나 이미 열린 패널의 내용을 바꾼다. 같은 문서면 iframe 재로딩만 건너뛴다 —
  // 재로딩하면 읽던 대시보드의 스크롤 위치가 사라진다(결정 SP8 → OC2 로 범위 축소). 제목·안내·
  // 사유·카드 표시는 비용이 0 이므로 매번 다시 쓴다: 그래야 티어가 바뀐 프로젝트를 다시 눌렀을
  // 때 화면이 사실을 따라온다.
  function openDetailPanel(target){
    // 닫는 중(지연 창)에 들어온 클릭 — 예약을 취소하지 않으면 방금 연 패널의 문서를 그 타이머가
    // 지운다. 이 한 줄이 "대기 타이머 ⇒ 닫힘" 불변식의 유일한 보존 지점이다(SP15 → OC3).
    if(panelBlankTimerId !== null){ clearTimeout(panelBlankTimerId); panelBlankTimerId = null; }
    var wasClosed = openPanelTarget === null;
    // 경로→키는 서버에서 순수 함수이므로 이 두 필드가 같으면 띄울 문서가 같다(결정 OC2).
    var isSameDocument = !wasClosed
      && openPanelTarget.projectPath === target.projectPath
      && openPanelTarget.dashboardKey === target.dashboardKey;
    openPanelTarget = target;
    panelTitleEl.textContent = target.displayName;
    // 안내 줄은 문구·data-tooltip 을 마크업에 고정해 두고 클래스만 토글한다(PV3·PV4).
    panelNoteEl.classList.toggle(DETAIL_NOTE_VISIBLE_CLASS, target.isPreviousTask);
    // 빈 상태는 상태 클래스 1개로 표현하고 무엇을 보일지는 CSS 가 고른다(결정 OC7).
    panelEmptyReasonEl.textContent = target.emptyReason;
    panelEl.classList.toggle(PANEL_EMPTY_CLASS, target.dashboardKey === null);
    // 대시보드가 없으면 about:blank 를 즉시 싣는다 — 지연하면(SP14) 옛 프로젝트의 대시보드가
    // 사유 문구 뒤에 남아 있는 프레임이 되어 R2 가 고치려는 오해를 그대로 재현한다(결정 OC3).
    if(!isSameDocument){
      loadPanelDocument(target.dashboardKey === null
        ? BLANK_DOCUMENT_URL : projectDashboardUrl(target.dashboardKey));
    }
    document.body.classList.add(PANEL_OPEN_BODY_CLASS);
    applyBackgroundInert();
    applyOpenCardMarking();
    if(!wasClosed) return;                  // 내용 교체일 뿐이면 포커스를 건드리지 않는다
    panelOpenerElement = target.openerElement;
    panelCloseButton.focus();
  }
```

> **`PANEL_EMPTY_CLASS` 토글의 조건이 `emptyReason !== ''` 가 아니라 `dashboardKey === null`
> 인 이유**: 불변식 OC-A 로 두 식은 동치인데, **"띄울 문서가 있는가"** 가 이 클래스의 진짜
> 의미다. `if(!isSameDocument)` 안의 분기와 **같은 술어를 쓰는 것**이 두 곳이 어긋날 수 없게
> 만든다(문구가 실수로 비어 있어도 프레임 상태는 옳다).

**(5-f) `closeDetailPanel` 교체 (1351~1359행)** — 변수명 + 1줄 추가

```js
  function closeDetailPanel(){
    if(openPanelTarget === null) return;   // 여기서 반환되므로 대기 타이머는 존재할 수 없다(OC3)
    openPanelTarget = null;
    document.body.classList.remove(PANEL_OPEN_BODY_CLASS);
    applyBackgroundInert();                 // 포커스 복귀보다 먼저 — inert 안의 요소는 포커스를 못 받는다
    // 표시는 즉시 지운다. 슬라이드아웃 180ms 를 기다리면 "닫혔는데 아직 열린 것으로 표시된"
    // 구간이 생기고, 백그라운드 탭에서 setTimeout 이 스로틀되면 그 거짓이 길어진다.
    applyOpenCardMarking();
    scheduleBlankAfterSlide();
    if(panelOpenerElement && panelOpenerElement.isConnected) panelOpenerElement.focus();
    panelOpenerElement = null;
  }
```

**(5-g) 배선 (1370~1379행 교체 + 1줄 추가)**

```js
  document.addEventListener('click', function(event){
    if(event.target.closest('.card-drag-handle')) return;         // 결정 N6 — 드래그 핸들은 별도 조작
    if((window.getSelection() || {}).toString()) return;          // 결정 N6 — 텍스트 선택 후 클릭 무시
    var card = event.target.closest(CLICKABLE_CARD_SELECTOR);
    if(!card) return;
    openDetailPanel(readCardTarget(card));
  });
  // 폴링·틱·dragend 가 #dzh-app 을 통째로 교체하면 표시도 함께 사라진다 — 그 직후 복원한다.
  // 이벤트는 값을 싣지 않으므로 여기서 읽는 것은 카드 DOM 뿐이다(결정 OC8).
  document.addEventListener(RENDERED_EVENT_NAME, applyOpenCardMarking);
```

### 6. `hub/README.md`

결정 OC12 표의 4개 문장(195~196 · 202~204 · 절 말미 추가). **OC11 안 B 채택 시** 「알려진 한계」에
키보드 격차 1항목을 추가한다.

### 7. PRP 이관 표기 2개 파일

「정본 이관 표기」 표 그대로. **원문을 지우지 않는다.**

---

## GOTCHA (구현자가 틀리기 쉬운 함정)

1. **패널 IIFE 안에 ASCII `snapshot` 을 한 글자도 쓰지 마라 — 주석도 포함이다.**
   검사 **T25-89** 가 `var PANEL_OPEN_BODY_CLASS` 줄부터 **파일 끝까지** `snapshot` 을 0건으로
   강제한다. 이 PRP 는 그 범위에 코드를 크게 더하므로 가장 밟기 쉬운 함정이다. 기존 관례대로
   **한글 "스냅샷"** 을 쓴다(1277행이 이미 그렇게 한다).
2. **`applyOpenCardMarking()` 을 `openDetailPanel` 의 `if(!wasClosed) return;` 뒤에 두지 마라.**
   그 뒤는 "처음 열 때만" 실행되는 구간이다 — **다른 카드로 갈아탈 때 표시가 옮겨가지 않는다.**
   반드시 그 가드 **앞**에 둔다.
3. **`data-panel-empty-reason` 을 `else if` 로 붙이지 마라.** 불변식 OC-A(키 부재 ⇔ 사유 존재)가
   깨지는 순간 그 카드는 `CLICKABLE_CARD_SELECTOR` 에서 빠져 **다시 조용한 실패가 된다.**
   `if(isClickable) … else …` 의 무조건 `else` 여야 한다.
4. **`[data-project-path="…"]` 속성 셀렉터를 조립하지 마라.** 경로에 큰따옴표·역슬래시가 있으면
   셀렉터가 깨지고 `querySelector` 가 예외를 던진다(그 자리에서 클릭 핸들러 전체가 죽는다).
   `findCardByProjectPath` 의 순회 비교를 그대로 쓴다.
5. **`.card-open` 규칙에 `border`·`box-shadow`·`animation` 을 넣지 마라.** 세 채널 모두
   `.card.card-working`(128~129행)·hover(122행)와 **같은 명세도(0,2,0)** 라 소스 순서 싸움이 된다.
   `background` 만 쓰는 것이 시안 A 의 전제다(결정 OC9).
6. **`aria-current` 를 카드(`.card`)에 붙이지 마라.** 카드는 `<div>` 라 접근성 트리에서 그룹조차
   아니다 — 사용자가 순회 중 만나는 요소(`.project-name`)에 붙어야 낭독된다.
7. **빈 상태에서 iframe 을 `visibility:hidden` 으로 숨기지 마라.** 상자가 남아 `flex:1 1 auto`
   (338행)로 남은 높이를 전부 먹고, 접근성 트리에도 빈 프레임이 남는다. `display:none` 이어야 한다.
8. **`panelTitleEl.textContent` 를 닫기에서 지우지 마라**(초판 GOTCHA 16 그대로 유효).
   빈 상태에서도 같다 — 나가는 180ms 동안 머리가 빈 패널이 보인다.
9. **`dzh:rendered` 를 `render()` 의 `insertAdjacentHTML` 앞으로 옮기지 마라.** 경고 블록이 아직
   붙지 않은 DOM 에서 표시를 복원하게 되고, `insertAdjacentHTML` 자체는 카드를 건드리지 않지만
   "발행 시점 = DOM 최종" 이라는 계약이 흐려진다. T25-107 이 줄 번호로 막는다.
10. **`new MutationObserver` 를 추가하지 마라.** 결정 OC8 안 B 로의 회귀다. G7·T25-107 이
    파일 전체 등장 횟수 **2**(툴팁 전용)를 고정한다.
11. **`isSameDocument` 에서 `emptyReason` 을 비교하지 마라.** 문구가 바뀌었을 때 iframe 을 다시
    로드할 이유가 없다(둘 다 `about:blank` 다). 비교는 **띄울 문서**를 정하는 두 필드만 본다.
12. **`openDetailPanel` 의 인자를 다시 4개로 풀지 마라.** 필요한 값이 6개가 되었으므로
    위치 인자로는 호출부에서 순서를 틀리기 쉽다. `PanelTarget` 레코드가 그 방어다(결정 OC1).
13. **`renderProjectNameEl` 의 `isClickable` 을 남겨 두지 마라**(OC11 안 A 채택 시). 쓰이지 않는
    매개변수는 내 변경이 만든 고아다 — 호출부(909행)와 함께 지운다.
14. **`.detail-empty-reason` 에 `data-tooltip` 을 붙이지 마라.** 결정 UT1 의 예외를 늘리는
    일이고, 잘리지 않는 문구에 툴팁은 보존할 것이 없다(결정 OC7).

---

## 엣지 케이스

| # | 상황 | 동작 |
|---|------|------|
| E1 | 티어 1 A 가 열린 상태에서 티어 2 B 를 클릭 | **신고된 버그의 정면 해소.** 제목이 B, iframe 이 `display:none`+`about:blank`, 사유가 표시된다. A 의 5초 폴링은 **그 자리에서** 멈춘다(닫기를 기다리지 않는다) |
| E2 | 빈 상태 B 에서 빈 상태 C 를 클릭 | 신원이 `projectPath` 라 조기 반환 대상이 아니다 → 제목·사유가 C 로 바뀐다. `isSameDocument` 가 참이라 **iframe 은 그대로 `about:blank`**(불필요한 재로딩 0회) |
| E3 | 빈 상태 B 에서 티어 1 A 를 클릭 | `dashboardKey` 가 달라 `isSameDocument` 거짓 → 대시보드를 싣고 `detail-empty` 를 뗀다 |
| E4 | 같은 카드 재클릭(티어 1) | iframe 을 건드리지 않는다 → **스크롤 위치 유지**(요구 S7, SP8 목적 달성). 제목·사유·표시만 다시 쓰인다. 포커스도 옮기지 않는다 |
| E5 | 폴링·틱이 `#dzh-app` 을 교체 | `dzh:rendered` 로 표시가 복원된다. 패널·iframe 은 무관(불변식 H1⁗) |
| E6 | 열린 프로젝트가 스냅샷에서 사라진다 | **패널을 강제로 닫지 않는다**(결정 N8 → SP 엣지 E3 승계). `findCardByProjectPath` 가 `null` 을 주고 표시는 어디에도 붙지 않는다 — 패널 제목이 "무엇이 열려 있는지"를 계속 말한다 |
| E7 | 열린 프로젝트가 **티어 1 → 2 로 강등**(대시보드 파일 삭제) | 카드는 다음 렌더에서 배지가 `세션 활동` 으로 바뀌고 표시는 유지된다. **패널은 자동으로 빈 상태가 되지 않는다** — iframe 안 대시보드가 자기 폴링에서 404 를 받아 스스로 "연결 끊김"을 표시한다(SP 엣지 E3 의 원칙: 읽던 화면을 빼앗지 않는다). **재클릭 1회로 빈 상태로 넘어간다**(결정 OC2) |
| E8 | 열린 프로젝트가 **티어 2 → 1 로 승격**(`/dashboard init`) | 배지가 `대시보드 추적` 으로 바뀐다. 패널은 여전히 빈 상태이지만 **재클릭 1회로 대시보드가 뜬다**(요구 S8). OC2 없이는 재클릭도 무동작이라 닫고 다시 열어야 했다 |
| E9 | 빈 상태를 닫는다 → 180ms 뒤 타이머 만료 | 이미 `about:blank` 인 프레임에 같은 URL 을 한 번 더 `replace` 한다 — **무해**(히스토리 없음, 시각 변화 없음). "이미 비었는가" 상태를 만들지 않는다(결정 OC3 ④) |
| E10 | 닫는 중(180ms)에 티어 2·3 카드를 클릭 | 타이머가 취소되고 패널이 되돌아 들어오며 빈 상태를 띄운다. **OC1 없이는 여기서 패널이 영구히 나가 버린다** |
| E11 | 드래그로 카드 순서를 바꾼다(패널 열린 채) | `dragover` 는 노드를 **이동**만 하므로 표시가 그대로 따라간다. `dragend` 의 `render()` 가 재생성한 뒤 `dzh:rendered` 로 복원된다 |
| E12 | `card-working`(진행 중) 카드가 열려 있다 | 테두리·glow·pulse 는 `card-working` 이, 면색은 `card-open` 이 담당한다 — 속성이 겹치지 않아 **둘 다 보인다** |
| E13 | 열린 카드에 마우스를 올린다 | hover 테두리 + open 면색이 함께 보인다. 충돌 없음 |
| E14 | 프로젝트가 하나도 없다(빈 허브) / `initialParseError` | `.card` 가 없어 클릭 대상이 없고 `applyOpenCardMarking()` 은 하는 일이 없다 |
| E15 | `file://` 모드(승인 항목 2 **채택** 시) | 모든 카드가 클릭 대상이고 전부 빈 상태를 띄운다 — 티어 1 은 "서버를 켜세요", 티어 2·3 은 티어 사유. iframe 은 한 번도 로드되지 않는다 |
| E16 | `file://` 모드(승인 항목 2 **기각** 시) | 티어 1 카드는 `data-dashboard-key` 도 `data-panel-empty-reason` 도 없어 **셀렉터에서 빠진다**(툴팁만, N7 그대로). 티어 2·3 은 사유가 있으므로 클릭된다 — **모드에 따라 티어 1 만 예외가 되는 비대칭**이 남는다(결정 OC6 의 기각 대가) |
| E17 | 아주 긴 사유 문구 / 좁은 오버레이 모드(<1024px) | 사유는 클램프하지 않으므로 자유롭게 줄바꿈된다. 헤더 높이는 `.detail-note` 자리 예약으로 여전히 고정(PV2·PV3) |

---

## 테스트 계획 — `tests/run.sh`

> 이 저장소의 `hub_template.html` JS 에는 단위 테스트가 없다(브라우저 런타임이 필요하고 하니스가
> 없다). 확립된 방식은 **`tests/run.sh` 의 grep 기반 구현 게이트**(T22-nn·T25-nn)이며, 그 방식이
> 실제로 잡아 온 것은 "결정이 코드에서 조용히 뒤집히는 것"이다. 여기서도 같은 방식을 쓰되,
> **단순 토큰 존재가 아니라 기계적 관계**(등장 횟수·줄 번호 순서·범위 안 0건)를 우선 검사한다.

### Python 검사 판정 — **추가·수정 없음**

| 항목 | 판정 |
|------|------|
| `hub/bin/*.py` | **무변경**(전제 1) → 새 pytest 없음 |
| `tests/hub/test_hub_project.py::test_u6_only_tier1_view_has_dashboard_key`(269~290행) | **무수정으로 계속 통과.** 이 PRP 는 `dashboard_key ⇔ 티어 1` 규칙을 **유지**하고, 그 값을 "클릭 가능 여부"로 겸용하는 것만 그만둔다 — 그래서 이 테스트가 **여전히 옳은 것을 지킨다** |
| `tests/hub/*.py` 나머지 | 스냅샷 계약에 필드가 추가되지 않으므로 무영향(`snapshot_content_key` 도 불변) |

### 기존 검사에 대한 영향 (전수 확인 결과)

| 검사 | 위치 | 처리 |
|------|------|------|
| **T25-80** | 3587~3599행 | **개정.** 정방향 토큰 `'openDashboardKey'` → `'openPanelTarget'`. `'about:blank'`·`'contentWindow.location.replace('` 와 역방향 `panelFrameEl.src` 는 **무수정** |
| **T25 헤더 문구** | 2346·2349행 | **갱신.** `T25-1~T25-104` → `T25-1~T25-109` |
| T25-58 | 3184~3190행 | **무수정.** `project-name" data-tooltip` 부재만 본다 — 새 마크업도 `data-tooltip` 을 붙이지 않는다 |
| T25-63 | 3259~3275행 | **무수정.** `<aside id="dzh-detail-panel"`·`id="dzh-detail-frame"`·`.icon-btn`·`THEME_CYCLE`·`'system'` 부재 — 전부 그대로 |
| T25-76·77·78·79 | 3489~3585행 | **무수정.** 마크업 id·레이아웃 토큰·브레이크포인트·전이 세트만 본다. 패널 폭·애니메이션 무변경 |
| T25-81 | 3601~3620행 | **무수정.** README 정방향 `상세 패널`·`밀`·`덮`·`나간다` 4개는 개정 후에도 남고, 머리 주석의 `H1⁗` 도 유지된다(결정 OC10). 역방향 `모달` 0건도 유지 — 새 README 문장에 그 낱말을 쓰지 않는다 |
| T25-82·83·84 | 3622~3671행 | **무수정.** `PANEL_CLOSE_ANIMATION_MS`·`panelBlankTimerId`·`scheduleBlankAfterSlide`·`clearTimeout(panelBlankTimerId)` 토큰과 그 **위치 관계**(open/close 사이)·시간 3중 일치를 본다. **T25-83 이 결정 OC3 의 불변식을 계속 지키는 것이 G9 의 근거다** |
| T25-85 | 3673~3700행대 | **무수정.** `.detail-close`·`border-bottom:` 0건. 새 CSS 는 `border-bottom` 을 쓰지 않는다 |
| T25-88 | 3749~3763행 | **무수정.** `.tier1-prev-label`·`.detail-note` 규칙에 `#` 색 리터럴 0건 — 새 규칙도 토큰만 쓴다. **새 규칙(`.card-open`·`.detail-empty-reason`)의 색 리터럴 금지는 T25-108 이 담당한다** |
| **T25-89** | 3765~3787행 | **무수정이지만 가장 위험한 검사다.** 패널 IIFE 범위 `snapshot` 0건 — 이 PRP 가 그 범위에 코드를 크게 더한다(GOTCHA 1). **통과가 곧 결정 SP1 이 유지됐다는 증거**이므로 토큰을 늘리지 않고 그대로 둔다 |
| T25-96 | 3924~3959행 | **무수정.** `.detail-note` 의 `white-space:nowrap`·`visibility:hidden`·`detail-note-visible`·`panelNoteEl.hidden` 0건 — 안내 줄 로직 무변경 |
| T25-98·99 | 3992~4032행 | **무수정.** 아이콘·글리프·README 아이콘 서술만 본다 |
| T25-104 | 4106~4120행 | **무수정.** README 의 "15개 파일" 고지 |
| T22 계열 전부 | — | **무수정.** `commands/dashboard.md` 를 건드리지 않는다(전제 2) |

### 신규 검사 (정방향 + 역방향 쌍) — `test_hub_docs_and_constants()` 말미에 추가

현재 최대 번호는 **T25-104** 이므로 **T25-105** 부터 쓴다.

| # | 대상 | 정방향(있어야) | 역방향(없어야) |
|---|------|----------------|----------------|
| **T25-105** | OC4·OC5 카드 인계 계약 (G1·G2·G3) | `data-panel-empty-reason="` · `PANEL_EMPTY_REASON_ATTRIBUTE` · `function panelEmptyReason(` · `.detail-panel.detail-empty .detail-frame{display:none}` · `.detail-panel.detail-empty .detail-empty-reason{display:block}` · `id="dzh-detail-empty-reason"` | ① 옛 단일 속성 셀렉터 `CLICKABLE_CARD_SELECTOR = '[' + DASHBOARD_KEY_ATTRIBUTE + ']';` ② **패널 IIFE 범위(`var PANEL_OPEN_BODY_CLASS` ~ EOF)에 티어 문구 리터럴 `세션 활동은 있지만`·`파일시스템에서만` 이 0건**(문구 이중화 금지 — G3) ③ `.detail-frame{display:none}`(상태 클래스 없는 무조건 숨김) |
| **T25-106** | OC1 신원 키 교체 (G4) | `openPanelTarget` · `PROJECT_PATH_ATTRIBUTE` · `isSameDocument` · `function readCardTarget(` | **`openDashboardKey` 가 파일 전체에 0건**(머리 주석 잔재 포함 — 옛 진실이 살아 있는 것처럼 읽히는 것을 막는다) |
| **T25-107** | OC8 재적용 경로 (G6·G7·G8 — **가장 값진 기계적 검사**) | ① `dzh:rendered` 등장 횟수가 **정확히 2** ② `applyOpenCardMarking` · `RENDERED_EVENT_NAME` ③ **발행 줄 번호**가 `app.insertAdjacentHTML('beforeend', extra);` 보다 **크고** `var PANEL_OPEN_BODY_CLASS` 보다 **작다**(= DOM 최종 이후 · 렌더 IIFE 안) | **`new MutationObserver` 등장 횟수가 여전히 2**(툴팁 전용 — 옵저버 회귀 금지) |
| **T25-108** | OC9·OC11 표시 채널 (G5) | `.card.card-open{` · `card-open` · `OPEN_CARD_CLASS` · `aria-current` · `.card[data-panel-empty-reason]{cursor:pointer}` 를 포함한 셀렉터 목록 | ① `.card.card-open{` 규칙 안에 `#` 색 리터럴 0건(T25-88 관례) ② 옛 단독 셀렉터 `.card[data-dashboard-key]{cursor:pointer}` 0건 ③ (OC11 안 A 채택 시) `? 'button' : 'span'` 0건 |
| **T25-109** | 문서·주석 정합 (G10) | `hub/README.md` 에 `어느 카드든`·`사유`·`배경색` · 머리 주석에 `dzh:rendered`·`data-panel-empty-reason`·`aria-current` | ① README 에 `클릭 대상이 아니다` 0건 ② 머리 주석에 `카드에는 패널 열림 상태를 반영하지 않는다` 0건 ③ JS 주석에 `프로젝트 대시보드 모달` 0건 |

> **T25-105 ②·T25-107 의 구현 형태**(선례: T25-89 의 범위 grep, T22-99 의 등장 횟수, T25-66·79·97
> 의 줄 번호 비교):
> ```bash
> panel_iife_start_line=$(grep -n 'var PANEL_OPEN_BODY_CLASS' "$hub_template_file" | head -1 | cut -d: -f1)
> tier_text_leak_count=$(tail -n "+$panel_iife_start_line" "$hub_template_file" \
>                          | grep -c '세션 활동은 있지만\|파일시스템에서만')
> [[ "$tier_text_leak_count" -eq 0 ]] || record_failure …
>
> rendered_event_count=$(grep -c "dzh:rendered" "$hub_template_file")
> observer_count=$(grep -c 'new MutationObserver' "$hub_template_file")
> dom_final_line=$(grep -n "app.insertAdjacentHTML('beforeend', extra);" "$hub_template_file" | head -1 | cut -d: -f1)
> dispatch_line=$(grep -n 'dispatchEvent(new Event(RENDERED_EVENT_NAME))' "$hub_template_file" | head -1 | cut -d: -f1)
> [[ "$rendered_event_count" -eq 2 && "$observer_count" -eq 2 \
>    && -n "$dispatch_line" && "$dispatch_line" -gt "$dom_final_line" \
>    && "$dispatch_line" -lt "$panel_iife_start_line" ]] || record_failure …
> ```
> `grep -c` 가 **줄 수**를 세는 것에 주의한다 — `dzh:rendered` 는 두 상수 선언에 각 1회씩
> **서로 다른 줄에** 있어야 하며, 한 줄에 둘을 쓰면 이 검사가 1을 보고 실패한다(의도된 강제다).

### 뮤테이션 검증 (구현 중 1회 — 검사가 **실제로 실패할 수 있는지** 확인)

이 저장소가 실측한 실패 유형이다: *"검증을 추가할 때 그 검증이 실제 환경에서 성립하는지 먼저
확인한다"*(전역 지침 「실패 사례」). 각 변형 후 `bash tests/run.sh` 가 **빨간불**인지 확인하고 되돌린다.

| 검사 | 변형 | 기대 |
|------|------|------|
| T25-105 | `else cardAttrs += ' data-panel-empty-reason=…'` 를 `else if(project.tier !== 3)` 로 좁힌다 | 실패 |
| T25-105(역) | 패널 IIFE 안에 `'세션 활동은 있지만 …'` 문구를 상수로 복제한다 | 실패 |
| T25-106 | `openPanelTarget` 선언 1곳만 `openDashboardKey` 로 되돌린다 | 실패 |
| T25-107 | `dispatchEvent` 줄을 `app.insertAdjacentHTML` **앞으로** 옮긴다 | 실패 |
| T25-107(역) | `applyOpenCardMarking` 배선을 `new MutationObserver(...)` 로 바꾼다(결정 OC8 안 B 회귀) | 실패 |
| T25-108 | `.card.card-open{background:var(--accent-soft)}` → `background:#E4F0F8` | 실패 |
| T25-108(역) | `.card[data-dashboard-key]{cursor:pointer}` 옛 줄을 되살린다 | 실패 |
| T25-109 | `hub/README.md` 에 「클릭 대상이 아니다」 한 문장을 되살린다 | 실패 |

### 수동 확인 (자동화 불가 — 브라우저 실측)

| # | 절차 | 기대 |
|---|------|------|
| **M1** | 1440px(밀어내기)에서 티어 1 A 클릭 → 티어 2 B 클릭 | **신고 재현이 사라진다.** 제목이 B, 대시보드가 사라지고 사유가 보인다. DevTools Network 에서 A 의 `dashboard.html` 요청이 **B 클릭 직후 멈춘다** |
| **M2** | 티어 3 카드 클릭 | 사유가 「파일시스템에서만 발견됨 — 세션 활동 없음」이고 **iframe 이 DOM 에서 `display:none`**(Elements 패널로 확인) |
| **M3** | 빈 상태 B → 빈 상태 C 클릭 | 제목만 바뀐다. Network 에 **새 요청 0건** |
| **M4** | 각 클릭마다 카드 격자 확인 | 열린 카드에만 면색이 들어오고 이전 카드에서 빠진다. Elements 에서 `.card-open` 이 **정확히 1개**, 그 `.project-name` 에 `aria-current="true"` |
| **M5** | 패널을 열어 두고 **60초 이상** 대기(폴링 통과) | 카드가 갱신돼도 표시가 유지된다. 표시가 **깜빡이지 않는다** |
| **M6** | 티어 1 카드를 열고 대시보드를 아래로 스크롤 → **같은 카드 재클릭** | 스크롤 위치 유지, Network 새 요청 0건(결정 SP8 목적 유지, 요구 S7) |
| **M7** | 열린 프로젝트에서 `/dashboard init` 실행 → 배지가 바뀐 뒤 **재클릭** | 대시보드가 뜬다(요구 S8, 결정 OC2). 재클릭 없이는 빈 상태가 유지되는 것도 함께 확인(엣지 E8) |
| **M8** | 닫기 직후 180ms 안에 티어 2 카드를 클릭(연타) | 패널이 되돌아 들어오며 빈 상태를 띄운다. 흰 화면·빈 제목이 없다(엣지 E10) |
| **M9** | 닫기 후 DevTools Network 를 5분 관찰 | `/project/…/dashboard.html` 요청이 오지 않는다(리스크 R-6 승계, 0.5초 이후 기준 — SP 엣지 E15) |
| **M10** | 라이트·다크 두 테마에서 열린 카드 확인 | 면색이 두 테마 모두에서 인접 카드와 구분된다. **세션 구분선이 얼마나 흐려지는지 함께 기록한다**(리스크 P-2 의 실측 게이트) |
| **M11** | `card-working`(진행 중) 카드를 연다 | pulse·테두리·glow 와 면색이 **함께** 보인다(엣지 E12) |
| **M12** | 키보드만으로: Tab 으로 카드 제목 도달 → Enter | 패널이 열리고 포커스가 닫기 버튼에 있다. ESC 로 닫으면 그 카드로 복귀. **티어 2·3 카드에서도 같다**(OC11 안 A 채택 시) |
| **M13** | 스크린리더(VoiceOver)로 카드 순회 | 열린 프로젝트에서 "현재 항목"류 안내가 들린다. 패널 진입 시 제목 → 사유 순으로 낭독되고 사유가 **두 번 읽히지 않는다** |
| **M14** | 900px(오버레이)에서 티어 2 카드 클릭 | 패널이 화면을 덮고 사유가 보인다. 배경 Tab 차단(`inert`) 그대로 |
| **M15** | 패널 열린 채 드래그로 카드 순서 변경 | 드래그 중·후 모두 표시가 그 카드를 따라간다(엣지 E11) |
| **M16** | `file://` 로 `hub.html` 열고 카드 클릭 | **승인 항목 2 채택 시**: 빈 상태로 "허브 서버를 켜면…" · **기각 시**: 티어 1 은 툴팁만, 티어 2·3 은 빈 상태(엣지 E16 의 비대칭을 눈으로 확인) |
| **M17** | 사용량 패널을 펼친 상태에서 티어 2 카드 클릭 | 사용량 패널이 접히고(기존 동작) 상세 패널이 빈 상태로 열린다 |

---

## 리스크와 완화책

| # | 리스크 | 완화 |
|---|--------|------|
| **P-1** | **`dzh:rendered` 발행을 빠뜨린 렌더 경로가 생긴다** — 나중에 누군가 `#dzh-app` 을 다른 곳에서 갱신하면 표시가 조용히 사라진다 | `#dzh-app` 을 쓰는 곳은 `render()` 하나이고(1124·1131·1136·1142행 전부 그 함수 안), 드래그의 `insertBefore` 는 **노드를 이동만** 한다(재생성 아님). T25-107 이 발행 지점을 `insertAdjacentHTML` 뒤·패널 IIFE 앞으로 **줄 번호로 고정**해 함수 밖으로 새는 것을 막는다. **후퇴 경로**: 결정 OC8 안 B(옵저버)로 갈아타면 발행 지점 문제 자체가 없어진다 — 교체 비용은 2줄 |
| **P-2** | **면색 틴트가 카드 안쪽 대비를 떨어뜨린다** — 라이트 테마에서 `.sessions li` 의 `--soft` 구분선이 `--accent-soft` 위에서 흐려진다(미커밋 발췌 작업이 병합되면 `.excerpt{color:var(--muted)}` 도 같은 면 위에 선다) | 영향받는 것은 **지금 우측 패널로 그 내용을 보고 있는 카드 하나**뿐이다. 수동 **M10** 이 실측 게이트이며, 미달 판정이면 **후퇴가 선언 1줄**이다(시안 A → 시안 B 좌측 바). **미리 예비안을 넣지 않는다**(결정 PV1 의 P-2 가 세운 태도 승계) |
| **P-3** | **Tab 정지점이 카드 수만큼 늘어난다**(OC11 안 A 채택 시) | 승인 항목 4 로 사용자가 고른다. 채택 시 얻는 것은 "카드당 정지점 정확히 1개"라는 **예측 가능성**이며, 정지점을 줄이는 별도 수단(카드 격자 건너뛰기 링크 등)은 요구에 없어 만들지 않는다(YAGNI 보류 5) |
| **P-4** | **열린 패널이 티어 변화를 자동 반영하지 않는다**(엣지 E7·E8) | 자동 반영은 패널 IIFE 가 스냅샷을 알아야 하므로 결정 SP1·검사 T25-89 를 깬다. 대신 **재클릭 1회**로 해소되도록 SP8 을 좁혔다(OC2) — 그리고 강등 시에는 iframe 자신이 "연결 끊김"을 표시하므로 화면이 거짓말을 하지 않는다. YAGNI 보류 1 에 재방문 트리거를 남긴다 |
| **P-5** | **`file://` 에서 패널이 "서버를 켜세요"만 말하는 표면이 된다**(OC6 채택 시) | 툴팁이 hover 즉답을 계속 준다. 기각 경로가 **한 줄**(`panelEmptyReason` 의 두 번째 줄 → `return '';`)이며 다른 결정은 흔들리지 않는다(결정 OC5 의 셀렉터 불변 성질). 승인 항목 2 로 분리했다 |
| **P-6** | **사유 문구가 카드 속성으로 나가면서 HTML 이 커진다** — 카드 N개 × 약 40바이트 | 티어 배지 툴팁이 **이미 같은 문구를 카드마다** 싣고 있다(910행) → 증가율은 그 두 배가 아니라 **기존과 같은 자릿수**다. 상주 서버는 내용이 바뀌지 않으면 `hub.html` 을 다시 쓰지 않으므로 I/O 영향도 없다 |
| **P-7** | **불변식 이름을 유지한 탓에 옛 문서를 읽는 사람이 조항 ③ 을 반대로 이해한다** | 머리 주석의 조항 ③ **본문 안에** 정본 경로(`…결정 OC9`·`OC8`)를 박고, 이관 표기표가 `hub-detail-side-panel.md` SP6 에 화살표를 남긴다. T25-109 가 옛 문장(`카드에는 패널 열림 상태를 반영하지 않는다`)의 잔존을 0건으로 막는다 |
| **P-8** | **`readCardTarget` 이 카드 구조에 결합된다** — `.project-name` 이 사라지면 제목이 빈 문자열이 된다 | 지금도 같은 결합이다(1375행이 이미 `.project-name` 을 찾는다). `nameEl` 이 없을 때의 낙착(`''` + `openerElement = cardElement`)도 **기존 코드와 같은 처리**를 그대로 옮긴 것이라 회귀가 아니다 |
| **P-9** | **속성 셀렉터가 3곳(cursor·hover·JS)에서 두 항으로 늘어 한 곳만 고치는 실수가 생긴다** | T25-108 이 정방향으로 셀렉터 목록을, 역방향으로 옛 단독 셀렉터 0건을 검사한다. 두 항이 늘어나는 대가로 **승인 결과와 무관하게 같은 식**을 쓴다는 이득을 얻는다(결정 OC5) |

---

## YAGNI 보류 (지금 만들지 않는다 + 재방문 트리거)

| # | 보류 항목 | 이유 | 재방문 트리거 |
|---|-----------|------|---------------|
| 1 | **열린 패널이 티어 변화를 자동 반영** | 패널 IIFE 가 스냅샷을 알아야 한다 → 결정 SP1 폐기 + T25-89 폐기. 대안(렌더 IIFE 가 "열린 카드가 바뀌었다"를 이벤트로 알린다)은 이벤트에 값을 싣기 시작하는 순간 두 IIFE 사이에 데이터 계약이 하나 더 생긴다 | 재클릭 1회 요구가 실제 불편으로 보고될 때(그때는 `dzh:rendered` 에 `detail` 을 싣는 대신 **패널이 카드 속성을 다시 읽는** 형태를 먼저 검토한다 — 열린 카드는 `findCardByProjectPath` 로 이미 찾을 수 있다) |
| 2 | **면색 전이 애니메이션**(`.card{transition:background 180ms ease-out}`) | 표시가 즉시 켜지고 꺼진다. 패널 슬라이드(180ms)와의 대칭은 SP16 이 **레이아웃**에 대해 말한 것이고, 500px 떨어진 카드의 면색은 "두 번째 사건"으로 읽히지 않는다. 도입하면 `prefers-reduced-motion` 블록에 셀렉터가 하나 더 붙는다 | "표시가 깜빡인다"는 보고가 올 때(선언 1줄 + 저감 블록 셀렉터 1개) |
| 3 | **빈 상태 사유를 카드 표면(`--surface`) 위에 올려 대시보드와 같은 시각 리듬 주기** | 요구는 "사유를 보여준다"이지 "사유를 카드로 만든다"가 아니다. 규칙 1개(`background`·`border`·`radius`·`padding`)가 늘고 다크 대비를 다시 검증해야 한다 | 승인 항목 3 에서 사용자가 카드 표면을 고를 때 |
| 4 | **행동 안내 문장 추가**("이 프로젝트에서 `/dashboard init` 을 실행하면…") | 전제 6(문구 신설 금지). 그리고 티어 3(세션 활동 없음)에는 맞지 않는 안내가 되어 **티어별 문구 분기**가 생긴다 | 사용자가 "그래서 어떻게 하면 되는데"를 물을 때. 그때는 `TIER_DESCRIPTION` 을 건드리지 않고 **티어 2 전용 두 번째 문장**을 `panelEmptyReason` 에서만 붙인다 |
| 5 | **카드 격자 건너뛰기 링크(skip link)** | Tab 정지점 증가(P-3)의 정석 해법이지만 요구에 없고, 허브 전체의 랜드마크 구조를 함께 설계해야 한다 | OC11 안 A 채택 후 "Tab 이 너무 길다"가 보고될 때 |
| 6 | **빈 상태에서 프로젝트 경로·마지막 활동 시각 등 부가 정보 표시** | 그 정보는 **카드에 이미 전부 있다**(`.path`·`.last-activity`·세션 목록). 패널이 카드를 베끼면 두 곳을 함께 고쳐야 한다 | 카드에 없는 정보를 패널에 넣을 이유가 생길 때 |
| 7 | **여러 카드 동시 선택 표시**(최근 본 프로젝트 이력 등) | 패널이 싱글턴 하나다(SP 비목표 그대로) | — |

---

## 검토했으나 채택하지 않은 대안 (요약)

| 대안 | 기각 사유 |
|------|-----------|
| `dashboardKey` 를 신원으로 유지하고 티어 2·3 에 가짜 키 발급 | `dashboardKey` 가 URL 조립용 값이라는 의미를 잃는다 — 다음 사람이 그것을 `projectDashboardUrl()` 에 넣는다(결정 OC1 안 A) |
| `openProjectPath` 문자열 하나로만 교체 | "지금 대시보드를 띄우고 있는가"를 알 수 없어 티어 변화가 재클릭에 반영되지 않고, 결국 두 번째 변수를 부른다(결정 OC1 안 B) |
| 카드에 `data-tier` 를 싣고 패널 IIFE 가 티어→문구 맵을 갖는다 | **문구가 두 IIFE 에 두 벌** 존재해 배지 툴팁과 패널 본문이 어긋날 자리를 만든다. `file://` 의 티어 1(티어는 1 인데 사유는 서버 미가동)을 표현할 수 없다(결정 OC4 안 A) |
| `hub_model.py` 에 `panel_empty_reason` 필드 신설 | 표현 계층 문구를 데이터 계약에 넣는 일이고 전제 1 을 깬다. `isServed` 는 클라이언트만 아는 값이라 `file://` 사유는 서버가 만들 수 없다(결정 OC4 안 B) |
| `MutationObserver` 로 표시 복원 | 렌더 1회에 2회 이상, 드래그 중에는 초당 수십 회 헛발화한다. 결합이 렌더 쪽에서 보이지 않아 렌더 방식이 바뀌면 조용히 어긋난다(결정 OC8 안 B). **깜빡임 때문이 아니다 — 옵저버 콜백은 페인트 전에 실행된다** |
| `body[data-open-project-path]` + 렌더가 직접 클래스를 박는다 | 열고 닫는 순간에는 렌더가 돌지 않아 패널도 표시를 붙여야 한다 → 한 사실에 적용 지점이 둘(결정 SP14 의 "하나의 일에 메커니즘이 둘" 기각 논거)(결정 OC8 안 C) |
| 클릭 대상 셀렉터를 `.card` 로 단순화 | 승인 항목 2 를 기각하면 식을 다시 바꿔야 하고, 그때 "클릭되지만 아무 일도 안 하는 카드"가 되살아난다 — 이 PRP 가 고치는 그 실패(결정 OC5 안 A) |
| `data-panel-openable` boolean 속성 신설 | 다른 두 속성에서 파생되는 값을 세 번째 자리에 중복 기록한다. 결정 GN4 의 "속성 하나가 여러 역할을 겸한다"의 반대 방향(결정 OC5 안 B) |
| 빈 상태를 `#dzh-detail-note` 재활용으로 만든다 | 한 줄 클램프·자리 예약이라는 다른 목적의 기하(PV2~PV4)와 `--attention` 색을 물려받고, 두 의미가 한 노드에 산다(결정 OC7 안 B) |
| iframe 을 `visibility:hidden` 으로 숨긴다 | 상자가 남아 `flex:1 1 auto` 로 높이를 먹고 접근성 트리에 빈 프레임이 남는다(GOTCHA 7) |
| 사유를 화면 중앙에 배치 | 중앙 정렬은 "오류 화면"의 문법이다. 이것은 오류가 아니라 사실 보고다(결정 OC7) |
| 사유에 `aria-live="polite"` 부여 | 패널 열 때 포커스가 이미 안으로 들어가고 `role="dialog"`+`aria-labelledby` 가 낭독을 보장한다 — 라이브 영역은 같은 문장을 두 번 말한다(결정 OC7) |
| 표시를 굵은 `outline` 으로 | 포커스 링(`.project-name:focus-visible`)과 같은 시각 언어라 키보드 사용자가 "포커스"와 "열림"을 구분할 수 없다(결정 OC9 시안 C) |
| 티어 2·3 만 `<button tabindex="-1">` | 포커스 못 받는 `<button>` 은 보조기술에 "버튼이 있다"고 말하며 도달을 막는다 — 결정 DG2·DG3 이 드래그 핸들을 버튼으로 만들지 않은 이유의 대칭(결정 OC11 안 C) |
| 티어 2·3 에 `data-tooltip` 을 주어 사유를 알린다 | hover 전용이라 키보드·터치가 얻을 수 없고, `.badge.tier` 가 이미 같은 문구를 갖고 있어 한 화면에 세 번째 복제가 된다(비목표) |
| 불변식 H1⁗ 을 개명한다 | 프라임 계열이 소진돼 임의 표기를 발명해야 하고, T25-81 과 다른 PRP 5개의 참조가 함께 깨진다. 조항 단위 개정이 이 저장소의 방식이다(결정 OC10 안 A, 선례 PV5) |
| SP8 의 조기 반환을 그대로 두고 티어 변화만 별도 처리 | 처리 지점이 둘로 갈리고, 그 둘이 "같은 카드인가"를 각자 판정한다. 조기 반환의 **범위를 좁히는 것**이 코드가 줄어드는 유일한 방향이다(결정 OC2) |

---

## 구현 마일스톤

| # | 내용 | 검증 |
|---|------|------|
| 1 | **신원 키 교체만 먼저**(결정 OC1·OC2·OC3) — `openPanelTarget` 승격, `readCardTarget`, `isSameDocument`. **기능 추가 없음** | 기존 동작 무회귀: 수동 M6·M8·M9 + `bash tests/run.sh`(T25-80 개정 포함) 전 항목 통과 |
| 2 | 빈 상태(결정 OC4·OC5·OC6·OC7) — `panelEmptyReason`, 카드 속성, 셀렉터 2항, CSS 3줄, DOM 1줄 | 수동 M1·M2·M3·M14·M16·M17, T25-105 |
| 3 | 열린 카드 표시(결정 OC8·OC9) — `dzh:rendered`, `applyOpenCardMarking`, `.card-open`, `aria-current` | 수동 M4·M5·M10·M11·M13·M15, T25-107·T25-108 |
| 4 | 제목 버튼화(결정 OC11, **승인 항목 4 채택 시**) — `renderProjectNameEl` 단순화 + 호출부 + 주석 | 수동 M12, T25-108 |
| 5 | 주석·불변식·README(결정 OC10·OC12) | T25-81(무수정 통과)·T25-109, 이관표 대조 |
| 6 | `tests/run.sh` — T25-80 개정, 헤더 문구 2곳, 신규 T25-105~109, **뮤테이션 8건** | `bash tests/run.sh` 전 항목 통과 |
| 7 | PRP 이관 표기 2개 파일 | 이관표 대조(원문 삭제 0건) |

> **마일스톤 1 을 분리하는 이유**: 신원 키 교체는 **기능이 아니라 버그의 뿌리**이고, 그 단계에서
> 기존 동작이 하나도 바뀌지 않아야 한다. 여기서 회귀가 나면 이후 두 기능의 문제와 섞여
> 원인 분리가 어려워진다.

---

## 승인 요청 항목

> 아래 5건은 취향·트레이드오프가 갈리거나 **이미 승인된 결정을 개정**하는 지점이다.
> 각 항목의 권고안대로 진행해도 되는지 확인해 주면 그대로 구현에 들어간다.

### 한눈에 보기

| 항목 | 내용 | 권고 | 기각 시 비용 |
|------|------|------|--------------|
| **1** | 모든 카드를 클릭 대상으로 만들고 대시보드 없는 프로젝트는 **패널 빈 상태**로 사유를 보인다(R2 자체) | **채택** | R2 전체가 사라진다 — 신고된 버그는 R1(표시)만으로 절반만 완화된다 |
| **2** | **`file://` 모드도** 빈 상태를 띄운다(결정 N7·SP 전제 3 부분 대체) | **채택** | `panelEmptyReason` 한 줄. 티어 1 만 모드에 따라 예외가 되는 비대칭이 남는다(엣지 E16) |
| **3** | 열린 카드 표시 = **면색 틴트 `--accent-soft`** + `aria-current` | **채택(시안 A)** | 선언 1줄 교체(시안 B 좌측 바) |
| **4** | 티어 2·3 의 `.project-name` 을 **`<button>`** 으로 올린다(결정 N6 키보드 행 개정) | **채택(안 A)** | 마일스톤 4 를 건너뛴다 + README 「알려진 한계」에 키보드 격차 1항목 추가 |
| **5** | 결정 **SP8 개정** — 재클릭 시 iframe 재로딩만 건너뛰고 제목·사유·표시는 다시 쓴다 | **채택** | 티어 변화 후 재클릭이 무동작으로 남는다(엣지 E7·E8). 닫고 다시 열어야 한다 |

### 승인 항목 1 — 모든 카드를 클릭 대상으로, 대시보드 없는 프로젝트는 **빈 상태** (결정 OC5·OC7)

**권고: 채택.** 이것은 새 기능이 아니라 **결정 N7 이 이미 선언한 원칙**("아무 반응 없는 클릭을
만들지 않는다")을 코드에 관철하는 일이다. 지금은 티어 2·3 클릭이 조용히 실패하는 데다 **틀린
콘텐츠(다른 프로젝트의 대시보드)가 남아** `file://` 케이스보다 나쁘다.
**대가:** 패널이 "대시보드를 보여주는 표면"에서 "그 프로젝트에 대해 지금 말할 수 있는 것을
보여주는 표면"으로 의미가 한 칸 넓어진다. 문구는 새로 만들지 않으므로(전제 6) 그 확장은 최소다.
**대안:** R1(표시)만 하고 티어 2·3 은 계속 클릭 불가 — 이 경우 "왜 이 카드는 눌러도 안 되나"라는
질문이 남고, 표시가 있어도 **클릭이 무반응이라는 사실 자체**는 그대로다.

### 승인 항목 2 — `file://` 모드도 빈 상태를 띄운다 (결정 OC6)

**권고: 채택.** N7 이 근거로 든 *"조용히 빈 모달이 뜨는 것이 가장 나쁜 결과"* 라는 **기술적
전제가 소멸한다** — 빈 상태는 iframe 을 아예 내리므로 빈 프레임이 뜰 수 없다. 그리고 N7 자신이
새 탭 안을 기각한 이유가 *"두 모드가 다른 상호작용을 갖는 것 자체가 학습 비용"* 이었는데, 기각
시에는 **`file://` 에서만 티어 1 이 클릭 불가**라는 새 비대칭이 생긴다.
**대가:** `file://` 사용자는 어떤 카드를 눌러도 "서버를 켜세요"만 말하는 패널을 만난다.
"쓸모없는 패널"로 볼 수도 있고 "왜 안 되는지 확실히 알려주는 패널"로 볼 수도 있다.
**대안:** 기각 — `panelEmptyReason` 의 `if(project.dashboard_key) return FILE_MODE_DASHBOARD_HINT;`
한 줄을 `return '';` 로 바꾼다. **셀렉터·CSS·패널 코드는 한 글자도 바뀌지 않는다**(결정 OC5).
그 경우 `file://` 의 티어 1 은 지금처럼 툴팁만 갖는다.

### 승인 항목 3 — 열린 카드 표시는 **면색 틴트**(`--accent-soft`) (결정 OC9)

**권고: 시안 A 채택.** 선언 1개이고, 3열 격자를 훑을 때 **면**이 바뀌는 것이 가장 빨리 눈에
든다. `border-color`·`box-shadow`·`animation`·`outline` 네 채널은 hover·`card-working`·포커스 링이
**이미 점유**하고 있어 재사용하면 의미가 겹친다.
**대가:** 그 카드 **안쪽**의 미세 대비가 바뀐다 — 라이트 테마에서 세션 구분선(`--soft`)이 흐려진다.
영향은 **지금 패널로 그 내용을 읽고 있는 카드 하나**에 국한된다. 수동 M10 이 실측 게이트다.
**대안:** 시안 B(좌측 4px 액센트 바 — 안쪽 대비 무영향, 선언이 읽기 어렵다) / 시안 C(굵은
`outline` — **비권고**, 포커스 링과 구분되지 않는다).

### 승인 항목 4 — 티어 2·3 의 `.project-name` 을 **`<button>`** 으로 올린다 (결정 OC11)

> **이 항목은 권고가 아니라 사용자 선택이 필요한 트레이드오프다.** 어느 쪽도 명백히 우월하지 않다.

| | 안 A — 모두 `<button>` (권고) | 안 B — 티어 2·3 은 `<span>` 유지 |
|---|---|---|
| 키보드 | **모든 카드 도달 가능.** 카드당 정지점 정확히 1개 | 티어 1 카드만 도달. 티어 2·3 의 사유를 **키보드로 얻을 방법이 없다** |
| Tab 정지점 | **카드 수만큼**(티어 3 이 많으면 20~40개) | 지금과 같다(티어 1 카드 수만큼) |
| 스크린리더 | `aria-current` 가 포커스 가능한 요소에 붙어 순회 중 실제로 만난다 | `aria-current` 는 붙지만 순회로 도달하지 않는다(요소 탐색 모드로만) |
| 일관성 | 카드 클릭 가능성이 **티어와 무관**해진 사실이 마크업에 그대로 반영된다 | 마우스는 되고 키보드는 안 되는 **채널 간 불일치**가 남는다 |
| 코드 | `renderProjectNameEl` 이 3줄 → 1줄, `isClickable` 매개변수 소멸 | 무변경 |
| 추가 작업 | 없음 | README 「알려진 한계」에 키보드 격차 1항목 명시(조용히 두지 않는다) + N6 키보드 행 이관 표기를 「유지 — 의도적 격차」로 |

**권고 사유(약한 권고):** 이 PRP 의 목적이 "클릭이 조용히 실패하지 않게 한다"인데 안 B 는 그
실패를 키보드 사용자에게 남긴다. 그리고 정지점이 늘어나는 비용은 **카드가 많을 때**만 발생하는데,
카드가 많을 때야말로 R1 이 필요해지는 상황이므로 두 요구가 같은 사용자를 향한다.
**안 B 를 고를 근거도 정당하다:** 티어 3 프로젝트가 수십 개인 허브에서 "세션 없음" 카드마다 Tab 이
멈추는 것은 실제 소음이고, 그 카드에서 얻는 정보는 한 문장이다.

### 승인 항목 5 — 결정 **SP8 개정**: 재클릭은 **iframe 재로딩만** 건너뛴다 (결정 OC2)

**권고: 채택.** SP8 이 보호하려던 것은 *"읽던 대시보드의 스크롤 위치"* 하나이고 그것은 **그대로
지켜진다**. 함수 전체를 반환하는 현재 형태는 **비용 0 인 갱신(제목·사유·표시)까지 함께 막아**,
티어가 바뀐 프로젝트를 다시 눌러도 화면이 사실을 따라오지 못하게 만든다(엣지 E7·E8).
**대가:** 재클릭이 "완전한 무동작"에서 "보이는 값을 사실과 맞춤"으로 바뀐다 — 사용자에게 보이는
회귀는 없다(iframe·포커스·스크롤 전부 불변).
**대안:** SP8 을 그대로 두고 티어 변화만 별도 경로로 처리 — 판정 지점이 둘로 갈리고 각자 "같은
카드인가"를 판정한다. 코드가 늘고 어긋날 자리가 생긴다.
